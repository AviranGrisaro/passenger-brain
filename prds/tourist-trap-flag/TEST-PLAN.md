# Tourist-Trap Flag & Local QA (T-035 / PAS-26) — Test Plan

**Built from:** `TRD.md` §9 verification table (one row per P0). Each case traces to a PRD requirement and a TRD §9 row. Note: this document did not exist before this pass — it should have been written at `trd`/`build` per the `qa` agent's standing "prepare early" rule; written retroactively here, against the already-built and already-code-reviewed feature. First verdict recorded 2026-08-03 against `passenger-code 952cf2f` (main) — the commit that fixed `ios-code-reviewer`'s two APPROVE-WITH-MINORS findings (D4 `.task` race, `PlaceDetailModal` `Label` a11y).

| # | Case | Traces to | Layer | Status |
|---|---|---|---|---|
| 1a | `FlagStroke` has exactly 3 cases, `treatment(for:band:)` total over the 3×4 matrix | Req 1, §9 row 1a | unit (`TouristFlagTests`) | PASS |
| 1b | Zero case-insensitive matches for "tourist trap"/"local"/"mix"/"touristy"/"super local" as a tag value across all `FlagCopy`/`HoodSpeech` outputs | Req 1, §9 row 1b | unit (`FlagCopyTests`) | PASS |
| 1c | No function takes `[Place]` and returns a Hood flag (Hood flag never aggregated from places) | Req 1, §9 row 1c | review + grep | PASS — grepped `Flag/`, `Hoods/`; `Hood.isTouristTrap` is read directly off the decoded model, nothing derives it from `Place` |
| 2a | Fill is `HeatPalette`-only, stroke is `Color("Flag")`-only, in one file (`HoodLayer.swift`), no shared expression | Req 2, §9 row 2a | review | PASS — read `HoodLayer.swift` in full; `foregroundStyle(fillColor)` and the three `.stroke(...)` branches never share a value |
| 2b | Three stroke treatments distinguishable by weight/dash alone at greyscale | Req 2, §9 row 2b | unit (`FlagContrastTests`) | PASS |
| 2c | Set of Hoods carrying any non-`.none` stroke is identical at every hour 0–12; only `.plain`↔`.busyWarning` may differ (D3's restated form) | Req 2/5 (D3), §9 row 2c | unit (`TouristFlagTests`) | PASS — confirmed by the total-function structure: `treatment` only ever returns `.none` for `.notFlagged`/`.unrated` regardless of band, so the flagged/not-flagged set is band-invariant by construction |
| 3a | `MapZoomTier.tier(forLatitudeDelta:)` — three distinct tiers, boundary values swept | Req 3, §9 row 3a | unit (`MapZoomTierTests`) | PASS |
| 3b | `.cityWide` → no stroke, no label on any Hood (forced via `HoodLayer.effectiveStroke`); `.neighborhood` → stroke+label on flagged only; `.close` → stroke, no label | Req 3, §9 row 3b | unit + review | PASS — read `HoodLayer.swift`: `effectiveStroke` hard-forces `.none` at `.cityWide` regardless of the pure `treatment()` result, matching D1's correction to the design reference |
| 3c | `PlaceLayer.swift` unmodified by this task; no pin carries a flag encoding | Req 3, §9 row 3c | review | PASS — `git show 952cf2f`'s full diff and the original build commit touch no file under `Places/PlaceLayer` |
| 4a | `.notFlagged` and `.unrated` identical in all 4 band columns (`FlagStroke`) | Req 4, §9 row 4a | unit (`TouristFlagTests`) | PASS |
| 4b | `HoodSpeech` gives different strings for `.notFlagged` vs `.unrated` | Req 4, §9 row 4b | unit (`FlagCopyTests`) | PASS |
| 4c | `HoodSheet`'s three-state line ("Not a tourist-heavy spot" vs "No local rating yet") resolves the tap (D5) | Req 4, §9 row 4c | review | PASS — `HoodSheet.swift` carries the always-present three-state line per D5's call |
| 5 | Busy+flagged is one enum value, never stacks; own combined label; busy-not-flagged gets nothing | Req 5, §9 row 5 | unit (type-level guarantee) | **BLOCKED, named not a gap** — `DensityStore.band(for:hour:)` is `nil` for every Hood until T-032's C10 density seed lands (TRD §1); the type guarantee (`FlagStroke` single-value enum, D2) is verified, but no seeded Hood can actually be observed at `.busy` in this build. Same accepted gap `ios-code-reviewer`/`architect` already named |
| 6a | `PlaceDetailModal.touristTrapSlot` — one line, icon+text, only when `== true`; zero height otherwise | Req 6, §9 row 6a | unit + review | PASS — read `PlaceDetailModal.swift:80-87` post-fix: `Label(FlagCopy.placeLine, systemImage: "camera.fill")`, `@ViewBuilder` renders nothing for `false`/`nil` |
| 6b | Exactly one call site for `FlagCopy.placeLine`; `permanentlyClosed` (T-036) referenced nowhere in this task's diff | Req 6, §9 row 6b | grep | PASS |
| 6c (minor fix verify) | `touristTrapSlot` uses `Label`, not a hand-built `HStack{Image;Text}` — one VoiceOver stop, matching the sibling `categoryRow` | code-review minor 2 | review | PASS — confirmed by direct read: `categoryRow` (line 71-74) and the fixed `touristTrapSlot` (line 80-87) now use the identical `Label(text, systemImage:)` construction |
| 7a | `HoodSpeech.label` — 12 non-empty outputs (3 flag × 4 band), `.flagged`+`.busy` is one combined sentence, not a concatenation | Req 7, §9 row 7a | unit (`FlagCopyTests`) | PASS |
| 7b | City-wide 1×1 accessibility element still carries the full label at the zoom where no stroke renders | Req 7, §9 row 7b | review | PASS — `HoodLayer`'s `Annotation` + `voiceOverLabel` is unconditional on zoom tier; only the *visible* capsule text is gated by `showsName` |
| 7c | Pins never announce the flag — `PlaceLayer`/`PlaceHitTester` accessibility labels unchanged | Req 7, §9 row 7c | review | PASS — unmodified files (same as 3c) |
| 8a | `LocalQAGate.decide` precedence matches §4.3 exactly across triggers/auth/answered/cap | Req 8, §9 row 8a | unit (`LocalQAGateTests`) | PASS |
| 8b | Answer Yes, relaunch same place → suppressed, `.alreadyAnswered` | Req 8, §9 row 8b | UI test (`LocalQAToastInteractionTests.testAnsweringYesIsNeverAskedAgainForTheSamePlace`) | PASS |
| 8c | Ignore → auto-dismiss ~5s, no record, `lastAskedAt` written, cap applies | Req 8, §9 row 8c | UI test (`LocalQAToastInteractionTests.testIgnoringTheToastAutoDismissesWithNoReminderThisVisit`) | PASS on 2 of 3 runs — see report; the 1 failure is the same simulator-crash-and-restart signature documented for `ColdOpenPerformanceTests` across multiple prior worklog entries, not a logic failure (see below) |
| 8d | Exactly one `LocalQAToast` presenter; zero references from `Detail/` | Req 8, §9 row 8d | grep | PASS |
| 8e | Queue after an answer with `DisabledLocalQASync` — exactly one queued, unsent record | Req 8, §9 row 8e | unit (`LocalQAAnswerStoreTests`, `LocalQASyncTests`) | PASS |
| 8f | `UIAccessibility.focusedElement` unchanged across toast appearance; `.announcement` posted, never `.screenChanged`; window never key, `accessibilityViewIsModal` false | Req 8, §9 row 8f (D4/C12) | grep + **manual (VoiceOver on, Accessibility Inspector)** | **PARTIAL** — grep confirms the implementation (`LocalQAPresenter.swift`: `isHidden = false`, comment "Never `makeKeyAndVisible`", `accessibilityViewIsModal = false`, exactly one `.announcement` post, zero `.screenChanged`/`.layoutChanged`/`makeKeyAndVisible` occurrences). **No automated test asserts the runtime focus behavior**, and a live VoiceOver/Accessibility Inspector run was not done this pass (same class of gap as T-034's X3) — named as an open gap, not claimed done |
| 8-trigger | Geofence/notification leg | Req 8 bullet 1 | — | **Not verifiable in Build Phase 1 — not built here** (TRD §1), per the TRD's own explicit carve-out |
| 9a | Neither `FlagStroke.treatment(for:band:)` nor `HoodSpeech.label(name:band:flag:)` takes any `LocalQA/`-sourced type as a parameter or stored property; `LocalQA/` imports neither `Hoods/` nor `Map/` | Req 9, §9 row 9a | grep + review | PASS — signatures match §4.1/§4.2 verbatim; `import` lists checked directly in every `LocalQA/*.swift` file |
| 9b | Answer Yes on a place whose flag is already `true` — a record still exists | Req 9, §9 row 9b | unit (`LocalQAAnswerStoreTests`) | PASS |
| 9c | Rendered flag byte-identical before/after any answer | Req 9, §9 row 9c | unit + review | PASS — structurally guaranteed by 9a (the render functions cannot see `LocalQAAnswerStore` at all) |
| X1 (D4 race, minor fix 1) | `localQACoordinator.loadPersistedState()` then `.start()` sequenced in one `.task`, not two independent (racing) ones | code-review minor 1 | review + full test run + manual | PASS-with-caveat — confirmed by direct read of `MapScreen.swift:296-303`: single `.task { await loadPersistedState(); await start() }`. **Not independently unit- or UI-testable as a race**: `MapScreen` is a View, and the two calls are both fast, in-process async calls with no injectable delay to force the old race to manifest either way. Circumstantial behavioral confirmation instead: manually launched the app repeatedly with `-simulateLocalQAVisit` and observed the daily-cap (`lastAskedAt`) correctly suppressing repeat asks across relaunches within the rolling 24h window — this only holds if `loadPersistedState()`'s disk read completes and populates `lastAskedAt`/`answeredPlaceIDs` before `start()`'s gate check runs on the ~1s-delayed debug event, which is exactly what the fix guarantees and the pre-fix two-`.task` ordering did not |
| X2 | Full `xcodebuild build` + `xcodebuild test` (`PassengerTests` + `PassengerUITests`) against `passenger-code 952cf2f` | QA lifecycle standing rule (L-005) | full suite run | PASS — **BUILD SUCCEEDED**, zero errors. `PassengerTests`: 343/343 passed, zero failures, including `DebugVisitSourceTests` (both tests) and `LocalQAInstallIdentityTests` — the crash flagged against this exact suite in T-034's 2026-08-03 worklog entry (`task_aed5ee1f`) did **not** reproduce this run. `PassengerUITests`: 3 failures out of the full run — `ColdOpenPerformanceTests.testColdOpenToInteractiveSignpost()`, `EventDetailSheetInteractionTests.testTappingEventMarkerOpensEventDetailSheetNotAHoodSheet()`, `LocalQAToastInteractionTests.testIgnoringTheToastAutoDismissesWithNoReminderThisVisit()` — all three show the identical "Restarting after unexpected exit, crash, or test timeout" signature at the exact moment of failure, none an assertion failure. Triaged, not left as a worklog line: `LocalQAToastInteractionTests` (T-035's own) was re-run in isolation 2 more times — once as the full class (crashed identically, same signature, plus a duplicate-`UIAccessibilityLoaderWebShared`-class simulator warning that IS a documented Apple/simulator-runtime issue, not app code) and once as the single method alone (passed clean, 13.4s). 2 of 3 runs of this exact test are clean; the failing run's signature matches, verbatim, the already-documented `ColdOpenPerformanceTests` flake class cited across at least 4 prior `PROGRESS.md` entries — same simulator-resource-pressure attribution, now confirmed to also hit UI test classes beyond the one it was previously pinned to |
| X3 | `DebugVisitSourceTests` crash flagged in T-034's worklog (`task_aed5ee1f`) | out-of-scope carry-forward, not a T-035 finding per se but touches T-035's own test file | full + isolated run | RESOLVED-AS-FLAKE for this run — green in the full run (343/343) and not reproduced; `task_aed5ee1f` should be closeable on this evidence, though it isn't this session's task to close it |

**Known, out of scope / accepted gaps for this pass:** row 5 (busy+flagged) — blocked on T-032's C10 density seed, named in the TRD itself, not a QA gap. Row 8f's manual VoiceOver run — code-level implementation confirmed correct by grep, runtime focus behavior not confirmed live (same environment-access class of gap as prior QA passes' X3 rows). Row 8-trigger — explicitly out of Build Phase 1 per TRD §1.
