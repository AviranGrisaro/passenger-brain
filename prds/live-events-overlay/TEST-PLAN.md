# Live Events Overlay (T-034 / PAS-25) — Test Plan

**Built from:** `TRD.md` §9 verification table (one row per P0). Each case below traces to a PRD requirement and a TRD §9 row. Verdict recorded 2026-08-03 against `passenger-code 24eb533` (main, `6139f13` + the already-landed `EventSeed` CodingKeys fix).

| # | Case | Traces to | Layer | Status |
|---|---|---|---|---|
| 1a | `EventSelection.selected` over the fixture at each hour offset 0…12 (injected `now`) returns exactly the events overlapping that bucket, independently recomputed in the test | Req 1 bullets 1/3, §9 row 1a | unit (`EventSelectionTests`) | PASS |
| 1b | Hour change re-selects the event set within the 400ms `HourRepaint` signpost budget, events layer on | Req 1 bullet 2, §9 row 1b | UI/perf (`ColdOpenPerformanceTests`/`HourRepaint`) | PASS (perf test green; re-confirmed in isolation, avg 0.675s) |
| 1c | A fixture event disjoint from `[anchor, anchor+13h)` never renders at any offset | Req 1 bullet 3, §9 row 1c | unit | PASS |
| 1d | A rendered event marker vs. a rendered place pin, forced to greyscale — silhouettes must differ with zero colour information | Req 1 bullet 4, §9 row 1d | unit (`EventLayerContrastTests`) + manual visual | PASS (unit green; visually confirmed rounded-rect+sparkles vs. circle silhouette in screenshot) |
| 2a | `EventLayer`'s emitted `MapContent` — no `MapPolygon`, no Hood-boundary stroke, no area `foregroundStyle` | Req 2 bullet 1, §9 row 2a | review + grep | PASS |
| 2b | `Color("EventMarker")` vs. `HeatPalette.fill(for:)` at every `HeatBand`, both `UIUserInterfaceStyle`s — distinct, ≥3:1 | Req 2 bullet 1, §9 row 2b | unit (`EventLayerContrastTests`) | PASS |
| 2c | Clustering by the same screen-distance rule as place pins | Req 2 bullet 2, §9 row 2c | **not verifiable this task (D4) — no pass condition, T-041 unowned** | N/A (by design, L-018) |
| 3a | `selected` over the fixture bucket holding `cap+2` events with distinct ranks — exactly `markerCap`(12) returned, top-cap under `(rank desc, startAt asc, id asc)` | Req 3 bullets 1/3, §9 row 3a | unit | PASS |
| 3b | Same call with input array shuffled — byte-identical output (sort stability not input-order-dependent) | Req 3 bullet 2, §9 row 3b | unit | PASS |
| 3c | `EventsAPI`'s built URL — identical for any two devices, no location/device-id/user field | Req 3 bullet 4, §9 row 3c | unit (`EventsAPITests`) | PASS |
| 3d | Grep of `Events/` diff — no `LocationStore` import, `rank` used nowhere but the sort comparator | Req 3 bullets 2/4, §9 row 3d | review + grep | PASS |
| 4a | Tap an `eventMarker-<id>` in a UI test — sheet appears in one tap, title carries the event's name, `.medium` detent, map visible behind, **no Hood sheet appears** (D7 race) | Req 4 bullet 1, §9 row 4a | UI test (`EventDetailSheetInteractionTests`) + **manual on-device confirmation (code-reviewer's ask)** | PASS (UI test green on a live iPhone 17 simulator — real touch synthesis on the rendered marker, confirms D7 end-to-end; a fully interactive manual tap was blocked this session, see report) |
| 4b | `EventDetailRows.rows(for:)` on a fixture event with `venue_name`/`category` null — returned array contains no `.venue`/`.category` case | Req 4 bullet 3, §9 row 4b | unit | PASS |
| 4c | Directions button call path — `closeEvent()` runs before `DirectionsService.open`, walking mode, correct name/coordinate | Req 4 bullet 2, §9 row 4c | unit | PASS |
| 5a | `EventStore.load` with a throwing fake API, constant off — `events == []`, `source == .unavailable`, no banner/modal/indicator constructed, map still renders heat+pins | Req 5 bullet 1, §9 row 5a | unit | PASS |
| 5b | The fixture's already-ended event — never renders at any `k`, sampled after hour change and after `scenePhase` cycle | Req 5 bullet 2, §9 row 5b | unit | PASS |
| 5c | Shipped T-031/T-033 UI suites pass unmodified with `events == []` | Req 5 bullet 3, §9 row 5c | UI test | PASS |
| 6a | `isLayerVisible` toggled off — no `EventLayer` in map content, event tap branch skipped | Req 6 bullet 2, §9 row 6a | unit + manual — **untestable, C12 held on T-032's `HeatModalCard`** | BLOCKED (unchanged, not a gap) |
| 6b | `HeatComposition.fills`/`placeCatalog.allPlaces` byte-identical before/after toggle | Req 6 bullet 2, §9 row 6b | unit — **untestable, same block** | BLOCKED |
| 6c | Toggle state survives `MapChromeState.toggle(.heat)` → `.search` → `.heat` | Req 6 bullet 3, §9 row 6c | manual — **untestable, same block** | BLOCKED |
| X1 | Full `xcodebuild test` (`PassengerTests` + `PassengerUITests`) green, given the `EventSeed.swift` CodingKeys bug shipped in `6139f13` and fixed downstream at `24eb533` | code-reviewer ask #4 | full suite run | PASS — every T-034-owned suite green (`EventSelection`, `EventHitTester`, `EventDetailRows`, `EventStore`, `EventSeed decode`, `EventSeed shipped seed §3.4`, `EventsAPI`, `EventLayer contrast`, `DetailRouter`, `EventDetailSheetInteractionTests`). 2 failures elsewhere in the 270ish-test full run, both "crashed with signal kill" (crash pattern, not assertion failures), neither in T-034 scope: `ColdOpenPerformanceTests` (re-ran isolated, passed — the same flake already documented across multiple prior PROGRESS.md entries) and `DebugVisitSourceTests.argumentEmitsOneDebugEvent()` (T-035's LocalQA test, new, not reproduced in isolation this session — spawned as a background task for its owner, not a T-034 finding) |
| X2 | §3.4 fixture's 8-clause authoring rule spot-checked visually across a few hour offsets — plausible/varied, not degenerate | code-reviewer ask #2 | manual | PASS WITH MINOR — all 8 authoring clauses independently re-derived and confirmed against the shipped `events-tel-aviv-seed.json` (6 distinct buckets: 0,1,2,3,5,6; empty buckets exist; bucket 5 holds 14 events with distinct ranks; a 3-bucket-spanning event; null `venue_name`/`category` cases; one already-ended event; fictional names throughout). **Minor finding:** the cap-stress bucket (hour offset 5) is 14 near-identical placeholder rows ("Fixture event 1"–"14", identical coordinates, identical category) — satisfies the letter of the authoring rule (clause 3) but is visually degenerate at that one hour, not "plausible" the way the rest of the fixture is. See report. |
| X3 | Dynamic Type / VoiceOver pass on `EventDetailModal`, incl. exact spoken label format `"\(event.name), event, \(event.timeLabel)"` | code-reviewer ask #3 | manual | PARTIAL — code read confirms system fonts throughout (Dynamic Type intact) and the exact label string construction; no automated test asserts the literal spoken string (pre-existing, already-flagged gap, not new). A live VoiceOver/Accessibility-Inspector run could not be completed this session — see report for why. |

**Known, out of scope for this pass:** rows 6a/6b/6c (C12, toggle) — blocked on T-032's `HeatModalCard`, not a gap in this QA pass. Row 2c (clustering) — TRD D4, no pass condition by design, tracked at T-041. Marker cap=12 and the `sparkles` glyph — both `[ASSUMPTION]`s flagged for the post-ship `designer` pass, not QA's to resolve.
