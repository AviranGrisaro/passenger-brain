# Time Slider — now → +12h — PRD

**Status:** Draft v9 — **acceptance REJECT 2026-08-03** (`product`); v9 adds req 5's rendered-legibility bullet (L-009), no scope change. v8 added the Heat button's icon-only treatment
**Phase:** [Phase 1 — Build to launch](../../strategy/passenger-strategy.md#rollout-sequence)
**Owner:** Aviran Grisaro
**Last updated:** 2026-08-08
**Scope note:** the feature is unchanged by the 2026-07-30 founders-meeting lock — the one V1 item strategy marks "Unchanged." **v2 and v3 change how the control is reached and moved, not what it does** (founder-direct, 2026-07-31; verbatim quotes in the two FOUNDER-DIRECT STUBs of that date in `agent-os/PROGRESS.md`). v3 corrects v2's gesture model and closes two of its four open calls; the delivered design spec and [`TRD.md`](./TRD.md) (`2f955fe`) turn out **less superseded than v2 assumed**. **v4 records what the `designer` pass resolved** — Q5 (button/modal axis) closed, Q2 (per-idiom edge availability) given concrete numbers, Q6 formally adopted with the design's own reasoning added — against the new spec at [`design/phase-1/time-slider-design.md`](../../design/phase-1/time-slider-design.md) (v2 of that doc) and its extended mockup. No requirement text changes at v4 — only the Open questions/Decisions log below. **v5 records the `design-approval` REJECT of that pass** (`product`, 2026-07-31): one blocking contrast defect in the new edge track (no PRD change needed — req 6 already stated the bar it missed), plus req 6's touch-target bullet split per path because it had no objective pass condition for the edge zone, and a new Q7 routing that departure to Aviran at `design-review`. **v6 records a second, independent `design-approval` REJECT of the same pass** (`product`, concurrent session, reconciled by `chief-of-staff` into one dispatch, `PROGRESS.md` 2026-07-31) — 3 further blocking findings plus 6 further should-fix items, all fixed in `designer`'s consolidated fix pass over [`design/phase-1/time-slider-design.md`](../../design/phase-1/time-slider-design.md) (now v3 of that doc) and its republished mockup. No requirement text changes at v6 either, except this line and the new **Q8** below — the fixes are design-spec-side (contrast, citation corrections, sheet-fallthrough clarification) plus one new open question the fix pass deliberately did not resolve itself.

> **Read this before anything below it. 2026-08-08: Q1 is reversed.** The in-modal hour path is **deleted** — the edge control is the only way to change the hour (founder-direct 2026-08-07, shipped as `T-081`/`PAS-76`, `passenger-code 2a2a4ac`). Every mention of "the heat button", "the modal", "the button path" or "two paths" below is **history**, kept because v3–v9 reasoned from it, not a live instruction. See **Q1**, the new **Q9**, and `strategy/decisions.md` #47/#48. Requirement text is corrected to the single-path reality; the correction is documentation only and changes no verdict — see the Decisions log's 2026-08-08 row for why the `Status:` line above is deliberately untouched.

## Description

- A single control that moves the whole map forward in time, from now to +12 hours.
- Snaps to whole hours; there is no sub-hour position.
- **One way to reach it: either screen edge.** A finger touching down on either screen edge and sliding up or down moves the hour directly — one continuous motion, no separate reveal step (founder-direct, 2026-07-31). *(Read "two ways to reach it — the heat button and the edge" through 2026-08-07; the heat-button/modal path is deleted, Q1.)* The active edge track itself is not permanent chrome; it comes with the slide and goes again. **One stated exception (Q8, ratified v7):** a minimal idle hint (5pt × 56pt capsule, per `design/phase-1/time-slider-design.md` §2) sits at rest on each live edge to make the gesture discoverable without an onboarding surface — this is deliberately persistent, unlike the active track.
- Every hour-bound layer on the map repaints for the selected hour; the map otherwise stays exactly where it is.
- Resets to "now" on every app launch.
- **Not in scope:** the heat layer's own rendering rules (see the map PRD); the layer toggles sharing that surface — each belongs to the layer it toggles; the rest of the nav row and its other buttons; anything past +12h or before now; scheduling, reminders, or notifications tied to a future hour.

## Motivation

- Strategy, verbatim: "Time slider, now → +12 hours. A place's relevance changes by the hour; the map knows that. Unchanged." A map fixed to "now" answers where to go this second, not where to go after dinner.
- **Re-checked at v2 and v3:** that line names the control and its range, not how a finger reaches it, so it still authorizes the feature after a gesture change. No strategy edit needed, and none was made.
- It is the only V1 control that makes the map a planning tool as well as a live read — [Phase 1](../../strategy/passenger-strategy.md#rollout-sequence) needs both the tourist who just landed and the resident planning tonight.
- The behaviour is already worked out in the old codebase (`SALVAGE.md`: `HeatmapControlsSheet.swift`, REFERENCE) — this PRD exists to pin the contract, not to invent it.

## Requirements

### Must-have (P0)

1. **Range is now → +12 hours, hour-snapped.**
   - [ ] Exactly 13 selectable positions: now, +1h … +12h.
   - [ ] The control cannot be moved past "now" at one end or "+12h" at the other — no wrap, no elastic overshoot past the ends.
   - [ ] Releasing a drag lands on a whole hour, never between two.
   - [ ] All 13 positions are reachable in one continuous edge slide, at the largest supported Dynamic Type size and **wherever on the edge the finger lands** — a slide starting near the bottom or the top of the edge still reaches every stop without lifting.
   - [ ] **This bullet is now the whole of the requirement, not one path of two.** With the in-modal control deleted (Q1), a stop the edge slide cannot reach is a stop the product cannot reach — there is no second path to pick up the difference. *(Added at v2; widened at v3 to any touch-down point; scoped to the edge path only at v4 by Q5, when the modal path still covered the rest; sole path since 2026-08-07.)*

2. **The map repaints for the selected hour.**
   - [ ] Changing the hour repaints every hour-bound layer for that hour.
   - [ ] Camera position, zoom, and any open sheet are unchanged by an hour change.
   - [ ] Repaint completes under 400ms (`design/design-principles.md` §2, Doherty Threshold).
   - [ ] With no data for the selected hour, affected areas render empty — no error banner, no modal.

3. **"Now" is the default, every launch.**
   - [ ] On every cold launch the selected hour is "now", regardless of where it was left last session.
   - [ ] "Now" re-resolves against the actual clock at launch, not against a cached value from the previous session.
   - [ ] The first invocation of the control in a session shows it at "now".

4. **Session persistence across dismissals.**
   - [ ] Dismissing the control and reaching it again within one session restores the hour the user left it at — from either edge. *(Read "and by the heat button" through 2026-08-07; that path is deleted, Q1.)*
   - [ ] Opening and closing any other nav surface in between does not reset the hour (`design/ux-flows.md` §2.1, exclusivity rule).
   - [ ] The hour survives every dismissal path the design defines, not only the primary one.

5. **The selected hour is readable as a number, not only as a position.**
   - [ ] A visible numeral or label states the selected hour at all times while the control is on screen.
   - [ ] An explicit "now" mark is drawn on the track as a fixed anchor, at the range end the control treats as "now".
   - [ ] Meaning is never carried by colour alone (`design/design-principles.md` §3).
   - [ ] **The whole readout — offset numeral, clock time, and the "next day" flag — renders unoccluded and unwrapped.** No nav-row button or other chrome overlaps any part of it, and no label breaks across lines or clips mid-token, at the default text size **and** at the largest supported Dynamic Type size. *(Added at acceptance 2026-08-03, L-009. The TRD's §2.3 z-table already required the card to sit "anchored a fixed distance above the nav row… never `bottom: 0`", but no PRD requirement, no §9 verification row and no TEST-PLAN case stated the **rendered** consequence — so `code-review` and `qa` both passed a build whose nav row draws on top of the readout. `XCUIElement.exists` is `true` for an occluded label and a source read cannot see an overlap; only a rendered check fails this.)*

6. **Accessibility.**
   - [ ] VoiceOver exposes a discrete, announceable step — each step announces the hour it lands on; the control is not continuous-drag-only. **UNMET as of 2026-08-07** — see the bullet below; the deleted modal control was what carried this.
   - [ ] **With VoiceOver or Switch Control on, all 13 hours are selectable without performing the edge slide.** VoiceOver claims the screen's swipe gestures and Switch Control users may not be able to produce an edge drag at all. **UNMET as of 2026-08-07, and stated as unmet rather than cut.** v3's own note said "the heat-button path is confirmed permanent and satisfies this, so removing that path is what would put the bullet back at risk" — that is exactly what happened. Nothing shipped satisfies it today: `EdgeHourTrack` and `EdgeHint` are `.accessibilityHidden(true)` and `EdgeHourZone` is a bare `DragGesture` on a `Color.clear` overlay with no adjustable action (`passenger-code b90ba83`). Aviran's instruction deleted a **duplicate** control, not this floor — the quote is conditional on "if we have this on the sides", and for these users the sides do not work. **Routed to Aviran as Q9; only he can cut a requirement.**
   - [ ] **Edge path (the only path):** the capture zone is ≥44pt on its acquisition axis (its width), **or** the design spec states the departure explicitly, with its reasoning, and Aviran ratifies it at `design-review`. Citing Fitts's Law does not satisfy a number below 44pt — that principle is where the 44pt floor comes from. *(Split per path at `design-approval` 2026-07-31, L-009; the button/modal half is retired 2026-08-08 with the path it measured. Ratified at v7, see Q7.)*
   - [ ] The control remains usable and legible at the largest supported Dynamic Type size.
   - [ ] **Every** text label rendered on the surface housing this control — today the edge track and its floating readout — meets 4.5:1 against the background it sits on, in **both** light and dark: the numeral, the "now" mark's label, the end-of-track labels and the "next day" flag included. There is no enumeration exception: a label no design pass happened to list is still covered, and a label on a surface this control moves to later is still covered.
   - [ ] The non-text parts that carry the control's state — the thumb, the boundary between filled and unfilled track, and the "now" mark — meet 3:1 against their adjacent colours in both themes. **The inactive rail is inside that bar too, with no exemption.** The exemption existed only "while the control is a platform-drawn one rendering it low-contrast by default", which was true of the modal's native `Slider` and never of the custom-drawn edge track (Q5); with the native control deleted, the condition can no longer hold anywhere in this feature (`design/design-principles.md` §5, WCAG AA). *(Exemption retired 2026-08-08. It bound nothing that ships — the edge track was already held to the full bar at v4 — so this removes a live trap for a future re-draw, not a shipped allowance.)*
   - *Both contrast bullets were added and then corrected at `design-approval` 2026-07-30 — req 6 originally had no contrast criterion at all, so no gate could fail a design that asserted the bar and missed it (L-009). Full history in the Decisions log.*

7. **Reached from either screen edge by one continuous vertical slide.** *(New at v2, gesture model corrected at v3 — founder-direct, 2026-07-31. The "one continuous motion, no reveal step" reading of that correction is inference, not the founder's words — see Open questions, Q4.)*
   - [ ] A finger touching down on a screen edge and sliding up or down changes the hour, and does so from the start of the slide. There is no separate reveal step and no second gesture — one finger down, one slide, the hour tracking it throughout.
   - [ ] The control is on screen for the length of the slide, showing the hour it is currently on (req 5).
   - [ ] Only vertical movement changes the hour; horizontal movement during the slide does not.
   - [ ] One direction consistently means later and the other earlier, identically from both edges. *(Which direction is "later" is the design's call to make and state — it is not in the founder's instruction.)*
   - [ ] The hour under the finger when it lifts is the selected hour, and it holds for the session (req 4). No further gesture is needed to commit it or to clear the control off the map.
   - [ ] Both edges drive one value — the same hour, not one per edge. Ratified at v7, see Q3. *(The companion bullet — "the edge slide and the heat button drive that one value too" — is retired 2026-08-08 with the heat-button path, Q1. One writer remains: `EdgeHourZone`.)*
   - [ ] The slide does not fire during ordinary map use — a pan or pinch starting away from the edge never brings the control up.
   - [ ] While any sheet is presented, the edge slide is unavailable. An edge drag over a presented sheet moves the sheet, as it does today. **The hour cannot be changed at all in that state as of 2026-08-07** — the heat button this bullet named as the fallback is deleted. *(Product call made at v3 on a premise that no longer holds — see Q6 and Q9.)*
   - [ ] Which edges are live is stated per device idiom rather than as one global rule: both edges on iPhone; on iPad the right edge is OS-reserved (Slide Over), so the design says what happens there instead of assuming both. *(Replaces the conditional left-edge bullet v2 carried — see Q2.)*

### Nice-to-have (P1)

- Haptic tick on each hour crossing.
- Absolute clock time alongside the relative offset ("+3h · 21:00").

## Technical design

- **Data model:** none new, and neither gesture change adds one — no new table, column, endpoint, or query parameter, and nothing to source or author. The control reads the `hood_density` hour buckets already shipped by `prds/map-hoods-heat/` and owns no persisted state; its position is in-memory session state only.
- **APIs / client-server contract:** all 13 hour buckets are fetched with the map's initial density load, so dragging the slider is a local re-read, not a network round trip. This is what makes the 400ms budget achievable; a per-hour fetch would not.
- **Architecture notes:** the selected hour is a single source of truth held above the map view and read by every hour-bound layer, so a layer added later (live events) subscribes to it rather than owning its own copy. `SALVAGE.md` marks `HeatmapControlsSheet.swift` REFERENCE — extract the hour-windowing model, discard the 1,069-line view; `Models/HeatTimeWindow.swift` is REUSE.
- **Dependencies:** the map PRD (heat area, density contract) must land first. The live-events PRD depends on this control existing.
- **How much of the TRD is superseded — rewritten 2026-08-08, was "narrowed at v3":** the modal half of [`TRD.md`](./TRD.md) is **dead, not primary.** The `ZStack` card, its nav-row heat button and its horizontal `Slider(in: 0...12, step: 1)` are deleted from the build (`T-081`/`PAS-76`; and `HeatButton` itself by T-078/`PAS-60`), so every TRD decision scoped to "the button path" is history. Named for `architect`, who owns that file and is amending it under this same task: **D5** ("Native `Slider` on the button path, custom control on the edge path") is half-moot; **§2.3 line 150 / §10's risk row** rest on "the heat modal and a system `.sheet` are never co-presented", which is now vacuously true and no longer makes req 2's "any open sheet is unchanged" hold structurally — with no modal, the guarantee comes from the edge zone simply not being in the hierarchy while a sheet is up (D7 rule c), which is a different argument for the same outcome; **§9 rows 5b/6(c) and build step C16** measure the deleted Hour segment. *(This bullet previously attributed the co-presentation claim to "D4" — that is where it sat in TRD `2f955fe`; current D4 is a different decision. Corrected here, flagged to `architect` there.)* Unchanged throughout: no new data, no fetch on an hour change (what makes 400ms real), `selectedHour` as a plain `Int` on `DensityStore`, the hour-format and contrast work.
- **Open technical questions:** (a) whether "now" re-resolves while the app sits foregrounded across an hour boundary, or only on launch and invocation; (b) the edge strip's map-surface cost has a design-pass number now (24pt wide, 64pt/40pt top/bottom insets — Q2, v4) but the exact iOS gesture-recognizer construction and a real-device confirmation of those inset values are still `architect`'s at the TRD.
- **Heat button, icon-only (added v8, founder-direct):** the nav-row button that opens this control renders **icon only — no visible text caption** (`Image(systemName: "flame.fill")`, no `Label` string alongside it), same as `NearMeButton` already ships. The rest of the nav row (Search, Profile) is this PRD's own stated "not in scope" line above; that same icon-only treatment for those two is tracked centrally in `design/ux-flows.md`'s 2026-08-02 addendum, not restated here, so a reader of this PRD doesn't get a partial picture of a container this feature doesn't own.

## Open questions & risks

*Q-numbers are carried over from v2 so `BOARD.md`'s T-032 row and Linear `PAS-15` still resolve against them.*

**Resolved at v3.** Q1 and Q4 are founder-direct, 2026-07-31 — both quotes verbatim from the FOUNDER-DIRECT STUB of that date in `agent-os/PROGRESS.md`, with anything inferred from them marked as inferred. Q6 is a product call and says so.

- **Q1 — replace or supplement the heat-button invocation? — answered SUPPLEMENT on 2026-07-31; SUPERSEDED on 2026-08-07, when Aviran replaced instead.** Both calls are recorded below, in order, because v3–v9 of this document all reason from the first one.
  - **Decision, 2026-07-31 (founder-direct, live chat).** Verbatim: **"Supplement (recommended)"** — Aviran accepting the recommendation v2 made. The heat-button/modal path stays exactly as designed; the edge slide is a second way to the same value. Recorded as `strategy/decisions.md` #47 (backfilled 2026-08-08 — it was never written there when it was made, which is why the reversal below went unnoticed).
  - **Reversal, 2026-08-07 (founder-direct, relayed via the coordinating session).** Verbatim: **"no need 12h slider on the modal if we have this on the sides, remove the 12h sliders from the search modal"** (`agent-os/PROGRESS.md`, 2026-08-07 FOUNDER-DIRECT STUB). Shipped as `T-081`/`PAS-76` — `passenger-code 2a2a4ac`, `product` ACCEPT WITH NOTES, closed `Done` on Aviran's "approve all three". `EdgeHourZone`/`EdgeHourTrack` ("the sides") are the sole hour-selection input; the `HeatButton` that opened the modal had already been folded into `SearchButton` by T-078/`PAS-60`, so no button opens an hour control either. Recorded as `strategy/decisions.md` #48.
  - **What the reversal does and does not authorize.** The quote is conditional — *"if we have this on the sides"* — so it authorizes deleting a **duplicate**, not deleting hour selection. Where the modal path was the only thing satisfying a requirement, that requirement stands **unmet**, not cut: req 6's VoiceOver/Switch Control bullets, and Q6's sheet-presented state. Both are Q9, and both are Aviran's to close — cutting a requirement to make a build pass is an escalation, not a `product` call.
  - **The original is kept, not deleted** — v3–v9 reason from it, and a reader needs the call that was live when they were written plus the date it stopped being live.
- **Q4 — one continuous gesture, or a reveal then a drag? — one continuous motion.** Verbatim: **"both edges. its not swipe from left to right..you slide the you finder from up to down on one of the edges"**. **The "single continuous motion, no separate reveal step" reading is inference, not his words** — he did not say "one motion" or "no reveal step"; it is read by contrast with the two-step model v2 was written against, and is recorded that way in the stub. Req 7 is written to that reading; if the reading is wrong, req 7's first and fifth bullets are what change.
- **Q6 (new at v3) — is the edge slide available while a sheet is presented? No.** Not a founder answer — a **product call, made here**. `architect`'s read (`f2920fe`) found this is the real gesture collision, not the back-gesture one, and asked `product` for the call rather than invent a workaround: an edge drag over a presented sheet (`HoodStubSheet` ships today, T-033/T-038 add more) drags the sheet. **Call: the gesture is simply unavailable while a sheet is up, and the heat button covers that state.** Because Q1's "supplement" guarantees a working fallback in exactly that state; because a gesture that beat the sheet's own drag would make every edge drag near a sheet ambiguous; and because it keeps the TRD's D4 guarantee behind req 2 intact rather than reopening it. One behaviour, not an architecture — cheap for `designer` or Aviran to overturn. **Adopted at v4** by `designer`'s own design pass (`design/phase-1/time-slider-design.md` §5b) rather than re-derived: the same three reasons re-confirmed, plus a flagged-but-rejected alternative (partial edge availability scoped to the sheet's own footprint) recorded and declined as adding a second per-sheet rule for a benefit the button already covers. Still stated as `product`'s call, still cheap to overturn — `designer` adopting it isn't a second, independent ratification, it's the same call carried forward on record. **Premise broken 2026-08-07:** reason (1) — *"Q1's 'supplement' guarantees a working fallback in exactly that state"* — is false since the modal was deleted, and it was the load-bearing reason (reasons 2 and 3 argue only that beating a sheet's own drag is worse, not that losing the hour entirely is acceptable). **The call is not re-made here.** What it now costs is stated instead: with any sheet up, the hour cannot be changed by any means. Routed to Aviran as Q9.

**Resolved at v4 (`designer`'s design pass, `design/phase-1/time-slider-design.md` v2).**

- **Q5 — which axis does the button/modal control use? — answered "STAYS HORIZONTAL" at v4; MOOT since 2026-08-07.** The control it decided the axis of is deleted (Q1), so the answer binds nothing. `designer`'s reasoning is kept in the design spec §5a; not restated here. **Its two consequences are what matter now, and both collapse to the single edge path:** req 1's reachability bullet was scoped "edge path only" because the modal covered the rest — it is now the whole requirement (see req 1); and req 6's inactive-rail 3:1 exemption applied only to the modal's platform-drawn `Slider`, so it is retired rather than inherited (see req 6). Neither consequence survives as a *split* — that is the trap this note exists to close.
- **Q2 — which edges are actually available: designer's per-idiom answer given; the map-band cost is priced at the design pass, not deferred whole to the TRD.** The left-edge/back-gesture question this was opened for stays closed (`architect`, v3). What v3 surfaced instead is now answered concretely: **both edges live on iPhone; iPad claims the left edge only** (right edge is system Slide Over, permanently excluded, with a neutral "OS-reserved" mark in the mockup rather than a silent dead zone). The vertical squeeze from Notification/Control Center and the home indicator is resolved with named insets — **64pt top / 40pt bottom** — leaving a 708pt usable band on an 812pt-tall reference device, which spaces 13 stops roughly 59pt apart (comfortably past the 44pt Fitts minimum) even before considering Dynamic Type doesn't reflow this control's geometry. The MapKit-pan-arbitration band each live edge permanently claims is sized at **24pt wide** (half the 44pt touch-target minimum), justified against beating MapKit's own pan recognizer while claiming the smallest band that still does that reliably — visualized directly in the extended mockup via a toggleable reviewer overlay, not just asserted as a number. **What's still `architect`'s at the TRD, not resolved here:** the exact iOS-side gesture-recognizer construction (`UIGestureRecognizer` vs. `DragGesture` composition) and confirming the 64pt/40pt insets against real iOS 26 device geometry rather than this design pass's own estimate — both flagged explicitly in the design spec's Open items (§8, items 6–7).

**Opened at v5 (`product`, `design-approval` 2026-07-31). Resolved at v7 — Aviran, founder-direct, live chat with coordinating session, 2026-08-02.**

- **Q7 — is a sub-44pt edge capture zone an accepted departure from req 6's touch-target floor? — APPROVED.** Verbatim: **"approve edge zone Q7"**. The 24pt zone stands as a stated, ratified departure — the design spec's argument (an edge-anchored target cannot be overshot past the bezel; the ~688–708pt gesture axis is far above the floor; req 6's own bullet keeps all 13 hours reachable without the edge slide as a fully-conforming fallback) is accepted rather than requiring the zone to widen to 44pt. Req 6's edge-path bullet is satisfied as written in the design spec; no PRD requirement text changes.

**Opened at v6 (`product`, second `design-approval` REJECT, reconciled by `chief-of-staff`, 2026-07-31). Resolved at v7 — Aviran, founder-direct, live chat with coordinating session, 2026-08-02.**

- **Q8 — is the edge idle hint an acceptable form of "permanent chrome"? — KEEP IT.** Verbatim: **"keep hint Q8"**. Adopts `product`'s own recommendation as the decision: the hint stays, and the Description line above now states it as an explicit exception rather than a contradiction. Reasoning on record (unchanged by this ruling, now load-bearing): dropping the hint would ship a founder-requested gesture fully undiscoverable, since strategy's no-onboarding stance leaves no other surface to teach it; Q1's "supplement" answer means the heat button always works regardless, so the hint is a discoverability aid, not the only path. **Second clause broken 2026-08-07** — the heat button is deleted, so the hint is now the only thing in the product that teaches the only gesture there is. The ruling itself is unaffected (it kept the hint), but the "safety net exists anyway" reasoning behind it no longer holds; flagged as Q9(c), not re-opened.

**Still open at v4, resolved at v7 — Aviran, founder-direct, live chat with coordinating session, 2026-08-02.**

- **Q3 — does "either screen edge" mean one value from both sides, or two sliders? — ONE SHARED VALUE.** Verbatim: **"one shared hour Q3"**. Confirms the mockup's existing demonstration (edge drag then heat-button readback shows the same value) as the actual, ratified behavior — no longer an **[ASSUMPTION]**. Req 7's "both edges drive one value" bullet is confirmed as written; no PRD requirement text changes.

**Opened at the 2026-08-08 reversal pass (`product`, T-090/`PAS-88`). Aviran's — nobody else can close it.**

- **Q9 — the deleted modal path was the fallback three requirements leaned on. Which of them do you want back, and how?** Not a request to reverse your call — the duplicate is gone and stays gone. Three specific holes it left, each stated with what it costs:
  - **(a) Accessibility, P0, currently unmet.** No VoiceOver- or Switch-Control-operable way to change the hour exists (req 6, bullets 1–2). Cheapest fix that keeps a single visible control: give the edge zone a real accessibility element with an adjustable action (increment/decrement announcing the hour), instead of the `.accessibilityHidden(true)` it carries today — no new visible UI, no second control, `HourFormat.voiceOverValue` already assembles the spoken string. **[ASSUMPTION]** that this is sufficient for Switch Control as well as VoiceOver — not verified on a device, and it should be before it's called done.
  - **(b) While any sheet is presented, the hour cannot be changed at all** (Q6, req 7's last bullet). Options: accept it (the user closes the sheet first — one extra tap, no new surface), or overturn Q6. Accepting is the smaller change and matches your own "we have this on the sides" framing; it just isn't what Q6 decided, so it should be decided rather than inherited.
  - **(c) Nothing in the product teaches the gesture except the 5pt idle hint** (Q8). Unchanged in itself — but Q8's own recorded reasoning leaned on the heat button always working as the guaranteed path, and that guarantee is gone. Flagged, not re-opened.

Also open, smaller:

- The design spec and mockup (`design/phase-1/time-slider-design.md`, v2 of that doc) are **additive over the approved v1**, not a re-do: the modal architecture and exit paths stand unchanged; the edge gesture, the Q5 axis decision, and the Q6 adoption are new. Queued for `design-approval` → `design-review` next.
- Synthetic density means every future hour is a simulation, not a forecast (strategy, Key risks). The control will feel precise before the data behind it is. Nothing in V1 tells the user that; worth an explicit call before launch.
- Hour buckets past midnight cross a day boundary — confirm the data model keys on absolute time, not hour-of-day, or +12h from 8pm silently reads the wrong day.

## Decisions log

| Date | Decision / change | Why |
|---|---|---|
| 2026-08-08 | **Q1's "Supplement" call recorded as reversed; reqs 1/4/6/7, Q5, Q6 and the Technical-design TRD bullet rewritten to the single-path (edge-only) reality; new Q9.** Both decisions kept, in order, with dates — the original is not deleted, because v3–v9 all reason from it. Backfilled into `strategy/decisions.md` as **#47** (the original "Supplement" call) and **#48** (the 2026-08-07 reversal); their absence there is why the reversal went unrecorded for a day. Req 6's VoiceOver/Switch-Control bullets and Q6's sheet-presented state are marked **unmet**, not cut, and routed to Aviran as **Q9**. Req 6's inactive-rail 3:1 exemption retired with the native `Slider` it was conditioned on. **`Status:` line deliberately untouched and no version number assigned:** that line records T-032's 2026-08-03 acceptance REJECT, which is still the live verdict on this PRD; this is a documentation correction, not a review round, and flipping it would assert something untrue (`product`, T-081 acceptance, same reasoning). If Aviran wants this read as v10, that's a one-line edit at `aviran-review` | Founder-direct, 2026-08-08, verbatim **"fix the PRD reversal"** (`agent-os/PROGRESS.md`, that date's FOUNDER-DIRECT STUB); everything past that quote is the coordinating session's scoping, relayed and labelled as such, not his words. Found by `ios-developer` + `ios-code-reviewer` at T-081, severity raised by `product` at T-081's acceptance: Q1 read as a live founder instruction to keep a path that had already been deleted — the same failure shape this workspace's scope gate exists to prevent, pointed backwards. `prds/time-slider/TRD.md` (§9 rows 5b/6(c), build step C16, D5, §2.3/§10's co-presentation claim) carries the same drift and is `architect`'s, handed over in the same pass |
| 2026-08-03 | **v9 — acceptance REJECT (`product`). Req 5 gains a rendered-legibility bullet; no other requirement text changed, no scope change.** Two blocking findings, both rendered-only: (F1) the nav row draws on top of the modal card's readout at the **default** text size, hiding most of the "next day" flag; (F2) at AX5 the readout wraps mid-token ("+12" / "h") and the "next day" pill breaks across two lines | F1 is a straight build deviation from the TRD's own §2.3 z5 row ("anchored a fixed distance above the nav row… never `bottom: 0`") — `HeatModalCard` ships bottom-anchored at 8pt while `MapNavRow` floats 96pt up, so the two share pixels. It is *also* a spec gap, which is why the new bullet exists: §2.3 stated the layout rule as architecture and it never became a §9 verification row or a TEST-PLAN case, so no gate had a rendered pass condition to fail. Every prior gate checked source or element existence, and neither can see an overlap. Verified live this pass on a rendered app at `passenger-code d537ca5` |
| 2026-08-02 | **v8 — Heat button confirmed icon-only, no text caption.** Technical design gains a stated bullet; no P0/P1 requirement text changed (this feature's own requirements never claimed a caption existed). The nav row's other two buttons (Search, Profile) are out of this PRD's scope per its own "not in scope" line — that half of the request is recorded centrally in `design/ux-flows.md` instead | Founder-direct, live hilos `@chief` chat, verbatim "remove the name from the icons in the nav bar, show only icons" — durable record in `agent-os/PROGRESS.md`'s 2026-08-02 FOUNDER-DIRECT STUB, per L-002. No code exists for any of the three nav buttons yet (T-032 is at `trd-review`, building the Heat button only per its TRD's D1), so this is captured in the spec ahead of build rather than dispatched as a code change |
| 2026-08-02 | **v7 — Q7/Q8/Q3 ratified.** Q7: 24pt edge capture zone approved as a stated departure from the 44pt floor. Q8: idle hint kept; Description line widened to state it as an explicit exception. Q3: one shared hour value across both edges and the heat button, confirmed (no longer **[ASSUMPTION]**). No requirement text changes beyond the Description line | Founder-direct, live chat with coordinating session, verbatim "approve edge zone Q7, keep hint Q8, one shared hour Q3" — durable record in `agent-os/PROGRESS.md`'s 2026-08-02 FOUNDER-DIRECT STUB, per L-002. All three questions were routed to Aviran at `design-review` (Q7/Q8 at v5/v6, Q3 still open since v2) and no other agent had standing to answer them |
| 2026-07-30 | PRD created | First PRD pass over the 2026-07-30 V1 lock (PAS-10); slider is the one item strategy marks unchanged |
| 2026-07-30 | Req 6 gains an explicit contrast bullet (4.5:1 text / 3:1 non-text, both themes) | `design-approval` found the design spec asserting that bar and the mockup missing it at 3.22:1 light / 3.61:1 dark on the end-of-track and "now"-tick labels. The PRD had no contrast bullet at all, so no gate — QA included — had anything objective to fail it on. Adds a testable criterion; does not change scope (L-009, applied at design-approval rather than acceptance) |
| 2026-07-30 | That bullet split in two and corrected at the second `design-approval` pass | Two defects in my own criterion, found by re-checking the fixed artifact against it. (1) It enumerated three labels, so `.sheet-eyebrow` — the modal's own "Time" header, 10.5px on `--fg-faint`, 3.22:1 light / 3.61:1 dark — sat outside a bar it plainly belongs inside. Now stated as "every text label in the modal," no enumeration. (2) It held "the track line" to 3:1; the inactive rail computes to 1.29:1 light / 1.28:1 dark and cannot clear 3:1 on the native `Slider` req 6's VoiceOver bullet depends on. WCAG's non-text bar applies to what identifies the component and its state — thumb (3.93:1 / 6.62:1), filled-vs-unfilled boundary (3.05:1 / 5.16:1) and the "now" tick (17.99:1 / 14.57:1), all of which clear it. Rejecting a design against an infeasible criterion would have been rejecting against a stale spec |
| 2026-07-31 | **v2 — swipe in from either screen edge, vertical drag.** Description and reqs 1/3/4/5/6 rewritten off the heat-button/modal + horizontal-drag model; new req 7 (edge invocation, vertical axis, dismissal); new req 1 bullet (13 stops fit one vertical drag at max Dynamic Type); new req 6 bullet (operable without the edge gesture, for VoiceOver/Switch Control); req 6's contrast bullets made surface-neutral, rail exemption re-conditioned on a platform-drawn control | Founder-direct, live `chief-of-staff` chat, 2026-07-31 — verbatim quote in `agent-os/PROGRESS.md`'s FOUNDER-DIRECT STUB that date. The feature, range, 400ms budget and data contract are unchanged; only how a finger reaches and moves it. Strategy's authorizing line re-checked and still covers it — it names the control, not the gesture. Three calls deliberately left open, not answered here (replace-vs-supplement, the iOS left-edge collision, one-slider-vs-two). Supersedes the design spec and `TRD.md` (`2f955fe`) on invocation and manipulation only |
| 2026-07-31 | **v3 — gesture model corrected, 2 of the 4 open calls closed.** Req 7 rewritten to one continuous motion (touch down on either edge, slide up/down, the hour moving from the start — no reveal step, no second gesture); req 1's reachability bullet widened to any touch-down point on the edge; req 4 and req 7 gained two-entry-path consistency bullets; req 6's VoiceOver bullet simplified now the button path is permanent; Technical design narrowed what the TRD supersedes; two new questions surfaced, one answered — **Q5** (which axis the button/modal control uses, designer's) and **Q6** (no edge slide while a sheet is presented, product's own call, made here at `architect`'s request) | Founder-direct, live chat, 2026-07-31. "Supplement (recommended)" resolves replace-vs-supplement, so the heat-button path is permanent and v2's premise that the whole design spec was dead is wrong. "both edges. its not swipe from left to right..you slide the you finder from up to down on one of the edges" corrects the two-step gesture v2 was written against. **The "one continuous motion" reading of that quote is `chief-of-staff`'s inference, labelled as such in Open questions rather than presented as his words.** Q3 (one value vs two) stays open and stays **[ASSUMPTION]**, not confirmed — Aviran did not re-confirm it when asked. Q2 was narrowed rather than closed: `architect`'s read (`f2920fe`) landed mid-pass and retired the left-edge/back-gesture worry outright, leaving the iPad right edge, the top/bottom inset squeeze and MapKit's gesture-swallowing bug in its place |
| 2026-07-31 | **v5 — `design-approval` REJECT (`product`). Req 6's touch-target bullet split per path; new Q7.** No other requirement text changed. The single "≥44pt" bullet had never been re-scoped after v2/v3 added the edge control, so nothing objective existed to pass or fail the 24pt capture zone on — the same gap shape L-009 was written for, caught here rather than at acceptance | The design spec ships a 24pt edge capture zone citing Fitts's Law, while `design-principles.md` §2 states that law *as* the 44pt floor. Rather than lower the floor myself (an escalation, not a `product` call) or wave the number through, the bullet now states a testable per-path condition and routes the departure to Aviran's own `design-review` gate as Q7. The blocking finding that actually sent this back to `design` is separate and needs no PRD change — req 6's rail bullet already stated the bar the edge track missed, which is the bullet working as designed |
| 2026-07-31 | **v6 — second, independent `design-approval` REJECT (`product`, concurrent session), reconciled by `chief-of-staff` into one dispatch. New Q8; no other requirement text changed.** A second `product` review ran in parallel with v5's and reached the same REJECT verdict with different findings — 3 further blocking issues (edge-track rail contrast against the exempted horizontal rail's own token; the sheet-open bullet's ambiguity between "captures the touch" and "falls through to it"; the persistent idle hint's tension with this PRD's own "not permanent chrome" line) plus 6 should-fix items, none overlapping v5's findings except the Fitts's-Law miscite both passes caught independently. `chief-of-staff` reconciled both into one 5-blocking/7-should-fix dispatch back to `design` (`PROGRESS.md` 2026-07-31, "reconciled two independent design-approval REJECTs") | All contrast/citation/ambiguity findings are `designer`'s to fix in the design spec and mockup, not requirement changes — done in `design/phase-1/time-slider-design.md` v3. The one requirement-adjacent item, the idle hint's persistence, is deliberately **not** resolved by widening or narrowing this PRD's text — that's a scope call on what counts as permanent chrome, Aviran's alone (same posture as Q7) — so it's routed as **Q8** instead. `design/ux-flows.md`'s chrome enumeration is updated to name the hint as a pending item rather than leaving it invisible to that doc |
| 2026-07-31 | **Second `design-approval` over the consolidated fix pass (`product`): APPROVE WITH MINORS.** No requirement text changed. Q8 gains a "what each answer costs" paragraph plus an explicit `product` recommendation (keep the hint, widen the Description line) — the question itself stays open and stays Aviran's | Q8 as first drafted named the tension and routed it correctly but stated neither consequence, so Aviran could have ruled "no permanent chrome" without seeing that it makes a founder-requested gesture undiscoverable (no onboarding surface is permitted to teach it). Stating both costs and a recommendation is not resolving the question — it's the same shape `designer`'s §5c gave Q3, applied to Q8. All 5 blocking and 7 should-fix items from the reconciled dispatch verified fixed against the artifact source and the spec text, contrast recomputed independently from the artifact's own `:root` hex values |
| 2026-07-31 | **v4 — designer's additive design pass. Q5 closed (stays horizontal); Q2 given concrete numbers; Q6 adopted with added reasoning.** No requirement text changed — only Open questions & risks and this log. Q5: the button/modal keeps the native horizontal `Slider`, unchanged; req 1's widened reachability bullet is confirmed to bind the edge path only, req 6's rail exemption confirmed to survive on the button path and not extend to the new (custom-drawn) edge track. Q2: both edges live on iPhone, left-only on iPad (right is system Slide Over); usable vertical band insets stated as 64pt top / 40pt bottom (708pt usable on an 812pt reference device, ~59pt per stop); the MapKit-pan-arbitration capture band priced at 24pt wide per live edge, visualized in the extended mockup via a toggleable reviewer overlay rather than only asserted. Q6: `product`'s existing call (edge gesture unavailable while any sheet is presented) adopted as the design's own working assumption, with one flagged-and-rejected alternative (partial edge availability scoped to a sheet's own footprint) recorded rather than silently considered. Q3 explicitly re-confirmed as still open and still **[ASSUMPTION]** — not resolved by this pass, demonstrated as one shared value in the mockup pending Aviran's actual answer | `designer`'s additive design pass over the v3 PRD, per `architect`'s feasibility read and the two open designer-owned questions (Q2's per-idiom half, Q5) it named. Full reasoning in `design/phase-1/time-slider-design.md` v2, §5a (Q5) and §5b (Q6); mockup extended in place at the same artifact URL, verified functionally in a live browser session (edge drag writes the same `selectedHour` the button reads, sheet-open correctly suppresses the gesture, iPad idiom correctly disables the right edge) rather than only reasoned about. Queued for `design-approval` → `design-review` next |
