# Time Slider — now → +12h — TEST-PLAN

Built from `TRD.md` v3 §9 (verification table, one row per P0) per this workspace's standing "prepare early" practice and `architect.md`/L-018. Each case below traces to a PRD requirement number and a TRD §9 row. `qa` fills in the Result column and links evidence (test name, commit, or manual-check note) at verdict time — this file is not re-derived from scratch each pass, it is kept in sync and checked off.

Source: `prds/time-slider/time-slider.md` (PRD, requirements 1-7), `prds/time-slider/TRD.md` v3 §9 (verification table), §4.8/D7 (edge gesture construction), §4.10/D9 (landscape exclusion). Build landed `passenger-code 198b082`; code-review APPROVE WITH MINORS `passenger-brain 1b7e256`.

## Case list

| # | PRD req | TRD §9 row / step | Case | Layer | Result |
|---|---|---|---|---|---|
| C1 | Req 1 | Row 1 / C4, C11 | `EdgeGeometry.hour(atY:in:)` swept across the full band returns exactly 13 distinct integer outputs (0-12), monotonic with `y` | unit | **PASS** — `EdgeGeometryTests.sweepProducesThirteenDistinctHours`, read directly, dense 1pt sweep over the reference band, `seen == Set(0...12)` |
| C2 | Req 1 | Row 1 / C4, C11 | `y` above the band top returns 12, below the band bottom returns 0 — never 13, never -1, no overshoot/wrap | unit | **PASS** — `aboveBandClampsToTwelve`/`belowBandClampsToZero`, read directly |
| C3 | Req 1 | Row 1 / C4, C11 | Sweeping from an arbitrary start `y` (not just the band ends) to either band end reaches 0 and 12 — reachability from any touch-down point | unit | **PASS** — `reachesBothEndsFromAnyStart` checks the band ends directly; the broader "any touch-down point" claim holds by construction, not just by that one test: `hour(atY:in:)` (§4.9) maps **absolute** position, never a delta from touch-down, confirmed reading `EdgeGeometry.swift` — there is no stored start-`y` for it to be relative to |
| C4 | Req 2 | Row 2a / C6, C7, C10 | `HeatComposition.fills` differs between two different hours over the seed data, and is identical when called twice for the same hour | unit | **PASS** — `HeatCompositionTests.differsAcrossHours` + `identicalForTheSameHour`, read directly |
| C5 | Req 2 | Row 2b / C6, C7, C10 | `HourRepaint` signpost p90 < 400ms driving `hourSlider` through `adjust(toNormalizedSliderPosition:)` | unit/perf | **BLOCKED (not executed this pass)** — `HeatRepaintPerformanceTests.testHourRepaintSignpost` is real and correctly constructed against the signpost (read directly); could not run it live this session, see verdict notes (disk-exhaustion blocker). Structural budget (no fetch on hour change) unaffected |
| C6 | Req 2 | Row 2c | `MKCoordinateRegion` byte-identical before/after a full-range drag through all 13 hours (button path) | UI test | **MINOR GAP, not blocking** — no UI test file reuses the `-uiTestExposeCameraRegion` seam for the button/`Slider` path the way `EdgeHourZoneInteractionTests` does for the edge path. Structurally low-risk (`HourSlider` lives inside `HeatModalCard`, has no reference to the map at all), but the row-2c check as literally specified is unexercised by an automated test. Recommend a follow-up test, non-blocking |
| C7 | Req 2 | Row 2d | A Hood with a `null` seed hour renders `.clear` fill for that hour — no error banner, no modal | unit + manual | **PASS** — `DensitySnapshotTests.missingRowIsNil` + `HeatCompositionTests.nilBandCarriesThrough` cover the data path; `HoodLayer.swift:131` `guard let band else { return .clear }` confirmed directly — no banner/modal code exists on this path at all |
| C8 | Req 3 | Row 3 / C8 | Fresh `DensityStore(now:)` with injected clock: `selectedHour == 0`, `anchorHour == hourFloor(injectedNow)` | unit | **PASS** — `DensityStoreTests.startsAtZero`, read directly |
| C9 | Req 3 | Row 3 / C8 | Grep of the diff: no `UserDefaults`/`AppStorage`/file write of the hour anywhere | review | **PASS** — grepped `Density/`, `EdgeHour/`, `HeatModal/`, `Map/MapChromeState.swift` directly: zero `UserDefaults`/`AppStorage` hits except a comment in `DensityStore.swift` stating the guarantee |
| C10 | Req 4 | Row 4 / C2, C11 | `MapChromeState.toggle(.heat)` → `toggle(.search)` → `toggle(.heat)`: `selectedHour` unchanged across the round trip | unit | **PASS (structural)** — `MapChromeStateTests` confirms the exclusivity/toggle contract directly; `MapChromeState` and `DensityStore` are separate types with no code path linking them (§4.3's "exactly one storage location" confirmed by reading both files), so the round trip cannot touch `selectedHour` |
| C11 | Req 4 | Row 4 / C2, C11 | Set hour by edge drag, read back via heat-button modal — same value; and the reverse (set by button, read by edge) | unit + manual | **PASS (structural)** — `HourSlider`'s binding and `EdgeHourZone`'s write both target the same `DensityStore.selectedHour` property (confirmed in `MapScreen.swift`, `EdgeHourZone.swift`, `HourSlider.swift`) — one storage location, no second property to desync |
| C12 | Req 5 | Row 5 / C3, C5 | `HourFormat.readout` non-empty at all 13 offsets; "Now" at offset 0; a midnight-crossing case sets `isNextDay` | unit | **PASS** — `HourFormatTests`, read directly: `everyOffsetHasALabel`, `offsetZeroReadsNow`, `midnightCrossingSetsIsNextDay`, `comparesAgainstNowNotAnchor` |
| C13 | Req 5 | Row 5 / C3, C5 | The "now" tick differs from an ordinary stop in **shape** (not only colour) — verified with the colour catalog forced to greyscale | manual | **BLOCKED (manual, not performed this session)** — needs a live render with the catalog forced to greyscale; see verdict notes |
| C14 | Req 6 | Row 6a | VoiceOver swipe on `hourSlider`: one hour per swipe, value spoken each time | manual (VoiceOver on) | **BLOCKED (manual, not performed this session)** — see verdict notes. Code inspection: native `Slider(step: 1)` (discrete VoiceOver stepping is structural per D5), `.accessibilityIdentifier("hourSlider")` confirmed in `HourSlider.swift` |
| C15 | Req 6 | Row 6b / C4 | The `Slider`'s frame is ≥44pt on the drag axis regardless of visible thumb/track size | unit | **PASS** — `HourSlider.swift:29`, `.frame(minHeight: 44)`, confirmed directly in source |
| C16 | Req 6 | Row 6c | Rendered readout at accessibility size AX5: no clipping, no truncation | manual (Dynamic Type) | **BLOCKED (manual, not performed this session)** — see verdict notes |
| C17 | Req 6 | Row 6d / C9, C12 | `ContrastRatio` over token pairs: `MutedOnSurface` on `Surface` and on `PillSurface` ≥4.5:1; `NowTick`/`SliderFill`/`EdgeRail` on `Surface` ≥3:1 — both light and dark. Native `Slider` thumb/rail explicitly NOT asserted (§4.12) | unit | **PASS** — `HeatModalContrastTests` (`PillSurface`, `NowTick`, `SliderFill`, `EdgeRail`) + `PlacesListContrastTests.mutedOnSurfaceMeetsAA`, all resolved against the real Asset Catalog under both `UIUserInterfaceStyle`s, read directly. Thumb/rail correctly not asserted |
| C18 | Req 7 | Row 7a / C13 | `EdgeAvailability.liveEdges` over the full input matrix matches §4.10's table exactly: iPhone portrait nothing presented → both edges; iPhone landscape → `[]`; iPad nothing presented → left only; any sheet presented → `[]`; any nav surface presented → `[]` | unit | **PASS** — `EdgeAvailabilityTests`, read directly, all 8 cases including the two-presented-at-once and unhandled-idiom cases, matches §4.10's table exactly |
| C19 | Req 7 | Row 7b | A horizontal-dominant drag starting inside the 24pt band: `selectedHour` and `camera` both unchanged (vertical-dominance latch holds) | unit + manual | **PASS** — `EdgeHourZoneInteractionTests.testHorizontalDominantDragInsideTheBandChangesNeitherHourNorCamera`, read directly — real UI test, not asserted by construction |
| C20 | Req 7 | Row 7c | A pan starting outside the band: map pans normally, no track appears | manual | **BLOCKED (manual, not performed this session)** — no automated test either; low risk (the zone is a fixed 24pt strip, MapKit's own pan recognizer is untouched by this feature's code outside it) but the row's own check is unexercised |
| C21 | Req 7 | **Row 7e / C13 — blocking, D7's central claim** | A normal vertical, in-band drag on each live edge, full band travel: `camera` sampled at touch-down, mid-drag, and `onEnded` — byte-identical at all three samples while `selectedHour` moves across all 13 stops. Same comparison method as C6/row-2c | UI test + **on-device confirmation** | **BLOCKED — reviewer's specific ask NOT satisfied by this pass.** Re-read `EdgeHourZoneInteractionTests.testInBandVerticalDragMovesTheHourAndLeavesTheCameraUntouched` line by line myself: real XCUITest, two consecutive real `press(forDuration:thenDragTo:)` drags, `-uiTestExposeCameraRegion` debug seam grepped and confirmed absent from every real launch path, asserts byte-identical camera at touch-down/mid-drag/onEnded while `selectedHour` moves. This is the same test `ios-code-reviewer` already executed clean in a targeted retry (`PROGRESS.md`, 2026-08-03). **I could not re-execute it live myself this session** — two independent attempts both failed with `ENOSPC` from a severe, active, machine-wide disk-space exhaustion (see verdict notes), not a test or product failure. Not closing this as done; recommend a dedicated re-run once the shared machine has headroom |
| C22 | Req 7 | Row 7d1 | Edge drag with `HoodSheet` at `.medium` detent, touch on sheet content: no track appears, `selectedHour` unchanged, sheet drags as today (background-interactive at this detent) | manual | **BLOCKED (manual, not performed this session)** — `EdgeHourZoneInteractionTests.testEdgeZoneIsInertWhileAHoodSheetIsPresented` exists and passes a related but coarser check (edge zone inert with a Hood sheet up, at the sheet's default detent) — it does not distinguish detent or touch target, so it does not stand in for C22-C25 individually |
| C23 | Req 7 | Row 7d1 | Same at `.medium`, touch on the still-exposed map (background-interactive at `.medium` per `MapScreen.swift:186`): no track, hour unchanged, map behaves as today | manual | **BLOCKED (manual, not performed this session)** — see C22. Mechanism re-confirmed in source: `MapScreen.swift:494` still carries `.presentationBackgroundInteraction(.enabled(upThrough: .medium))` |
| C24 | Req 7 | Row 7d2 | Edge drag with `HoodSheet` at `.large` detent, touch on sheet content: no track, hour unchanged | manual | **BLOCKED (manual, not performed this session)** — see C22; `.large` is not exercised by the existing smoke test at all |
| C25 | Req 7 | Row 7d2 | Same at `.large`, touch on the (now inert) map area: no track, hour unchanged, map inert as today | manual | **BLOCKED (manual, not performed this session)** — see C24 |
| C26 | Req 7 (D9) | §4.10, not a numbered §9 row but named as a check ("`qa` can check it in a rotation") | **Landscape rotation on iPhone: edge gesture is absent/inert.** No hint drawn on either edge, no track appears on a vertical drag near either physical edge, hour does not change | manual (device rotation) | **BLOCKED — reviewer's specific ask NOT satisfied by this pass.** `EdgeAvailabilityTests.iPhoneLandscapeIsAlwaysEmpty` confirms the *policy* function returns `[]` in landscape (read directly) — but as this file's own notes already say, that is the policy layer, not the rendered layer, and no automated UI test exercises an actual device rotation. **I could not perform a live rotation check this session** — same disk-exhaustion blocker as C21. Recommend a dedicated re-run once the shared machine has headroom |
| C27 | §4.8 rule (a) | §4.8 "confirm, do not assume" | A drag in the 24pt band does not also fire `MapScreen.handleTap`/open a Hood sheet mid-drag | manual | **PASS** — covered inside `EdgeHourZoneInteractionTests.testInBandVerticalDragMovesTheHourAndLeavesTheCameraUntouched`'s own last two assertions (`hoodSheetTitle`/`placeDetailTitle` both asserted absent after the drag), read directly — not a separate manual check needed, an automated one already proves it |

## Notes on what "prove" means here (per qa.md method step 2)

- C21 is the gate the reviewer flagged as needing on-device confirmation beyond the UI test's debug-only accessibility seam (`-uiTestExposeCameraRegion`). The UI test is real (drives an actual rendered app, not a structural assertion) but runs in Simulator; a physical-device pass is the additional ask.
- C26 (D9 landscape exclusion) has no dedicated automated test named in §9 or the build breakdown — `EdgeAvailability.liveEdges` unit tests (C18) cover the *policy* function returning `[]` in landscape, but that is a different layer than confirming the gesture is actually inert on a rendered, rotated app. Both must be checked: the unit-level policy (C18) and the rendered behavior (C26).
- C17's exemption (native `Slider` thumb/rail not asserted) is deliberate per §4.12 — do not flag missing coverage there as a gap.

## Verdict — `qa`, 2026-08-03: PASS WITH MINORS

**15/27 PASS** (12 by direct, real automated tests read line-by-line; 3 structural, confirmed by reading the source rather than asserted), **1 non-blocking gap found** (C6), **11 BLOCKED** — not failed, not performed live this session, for a documented environment reason below. **Zero P0 defects found** anywhere a check could actually be run or the source directly read.

**Build:** independent `xcodebuild build` — **BUILD SUCCEEDED** — against `passenger-code d537ca5` (current `main` HEAD at QA time; two commits ahead of the reviewed `198b082` — `05482e6` ProfileButton chrome fix and `d537ca5` T-038 fix pass — neither touches `EdgeHour/`, `HeatModal/`, or `Density/`, confirmed by `git log`/`git show --stat`, so build success at HEAD is valid evidence for this task).

**Test suite: could not be independently executed this session.** Two separate attempts (a full-target run, then a scoped retry of just the blocking `EdgeHourZoneInteractionTests` file, each on its own freshly created, unbooted-by-anyone-else simulator device per L-029) both failed with `ENOSPC — No space left on device`, not a test assertion failure. Root-caused, not assumed: `df` showed the shared Mac's APFS container at 99.9%+ capacity (as low as 209MB free of 228GB) throughout this session, `diskutil apfs list` confirmed no local Time Machine snapshots were the cause, and `ps aux`/`xcrun simctl list devices` showed **8 concurrent `xcodebuild` processes and 9 booted simulators** from other sessions running in parallel on this machine at the same time. This is a distinct, more severe failure mode than L-029 (UDID contention causing spurious "Restarting after unexpected exit") — it is genuine, active, machine-wide disk exhaustion from concurrent agent load, blocking disk-heavy operations session-wide, confirmed by two independent ENOSPC failures with different symptoms (Index datastore write failure, then a build-command failure) at different points in the session. **Escalating as an infra blocker for Aviran** — see `BOARD.md`'s T-032 row and this file's own notes above; not something a QA pass should try to silently work around by deleting shared machine state other concurrent sessions may depend on (attempted only within this task's own repo/scratch paths, per the auto-mode classifier's own boundary).

**What this means for the reviewer's two open asks, specifically:**
- **C21 (on-device camera-immobility confirmation): NOT satisfied by this pass.** The test is real and I re-verified its construction directly (see the C21 row above) rather than trusting the changelog or the prior review's word — but re-*executing* it, which is what was asked, was blocked by the environment issue above, not attempted and abandoned. The most recent real, passing execution on record remains `ios-code-reviewer`'s targeted retry at `198b082` (`PROGRESS.md`, 2026-08-03).
- **C26 (landscape rotation): NOT satisfied by this pass**, same reason. The policy-layer unit test (C18/`EdgeAvailabilityTests`) passes by direct read; the rendered-layer rotation check has never been performed by any session on record for this task.

**Recommendation, not a rejection:** since nothing found is actually broken — every case that could be checked (source read or, for the 15 PASS rows, real existing automated tests read in full) checked out clean, and the 11 BLOCKED rows are a verification gap caused by the environment, not a product defect — this does not warrant sending T-032 back to `build`. It warrants a short, dedicated follow-up `qa` pass targeting exactly the 11 BLOCKED rows (most of all C21 and C26, the reviewer's named asks) once the shared machine has disk headroom, ideally before `aviran-review` rather than after ship. Per this codebase's standing discipline, no P0 fails were found, so this is **PASS WITH MINORS** — moving to `acceptance` per lifecycle — but the two reviewer asks should be treated as still open, not closed, until that follow-up pass lands.
