# Passenger Agent OS — Board

Single source of state for the agent team. Every agent reads this **and `PROGRESS.md`** before starting, and updates its rows + appends a PROGRESS.md worklog entry after finishing — committed and pushed the same turn. `chief-of-staff` is the runtime: each "run the company" pass moves every task as far down the lifecycle as it can and refills the pipeline via `product` when it drains.

## Scope gate (added 2026-07-26 — read before opening any task)

Passenger exists because the previous board ran 27 PRDs through this exact lifecycle, ten of them building features the strategy forbids. Every gate passed. Nobody read the strategy.

**A task may not leave `spec` unless its PRD quotes the line in `strategy/passenger-strategy.md` that authorizes it.** `product` enforces this and rejects PRDs that can't. Standing prohibitions:

- No social features of any kind — friends, following, posting, presence, profiles, avatars.
- No onboarding — the app opens straight to the map plus location permission.
- Phase 2 (proximity intelligence, **Scenic View**, **Live Events**) and Phase 3 (AI guide, shake-to-decide) are parked. Don't build toward them or leave hooks. Points is retired — replaced by the Phase 2 stamp collection system (see `strategy/passenger-strategy.md`), not a separate parked item.
- ~~**Scenic View and Live Events moved out of V1 on 2026-07-27** (decisions #19, #20).~~ **Reversed — do not read this as a prohibition.** Live Events returned to V1 on 2026-07-29 and Scenic Walk on 2026-07-30 (decisions #32, #34; `strategy/passenger-strategy.md` V1 scope). Struck rather than deleted, per convention. The salvage warning still stands: both existed in the Locali client, so watch for the *old* shapes returning through `SALVAGE.md` rather than through a PRD — the Locali events overlay was an unflagged raw feed, not the ranked subset now specced. Flagged by `product`, 2026-07-30 (PAS-10).

## Task lifecycle (state → owner)

```
backlog → spec(product) → trd(architect) → trd-review(ios-developer/developer/data-engineer + code-reviewer)
        → build(ios-developer and/or developer) → code-review(ios-code-reviewer and/or code-reviewer)
        → qa(qa) → acceptance(product) → aviran-review → done
```

**Pre-code design gate retired 2026-08-02** (Aviran, live chief-of-staff chat via `/chief` — verbatim + reasoning in `PROGRESS.md`'s 2026-08-02 stub entry). There used to be a `design(designer)` step between `spec` and `trd`, plus `design-approval`/`design-review` sign-offs — all gone. New process: **build fast/vibe-coded first, ship, then designer redesigns after deploy.** `trd`/`build` now start straight from product's approved PRD. Designer's work moved to a non-blocking **post-ship redesign pass** — see `designer.md` — opened as its own companion task once a feature reaches `done`, never gating the original feature or any other task's build.

- **iOS vs backend split:** `build`/`code-review` fan out by surface — `ios-developer`/`ios-code-reviewer` for the Swift/SwiftUI client (`passenger-code/`), `developer`/`code-reviewer` for the Supabase backend (`passenger-brain/database/`), both pairs when a task spans both. The architect's TRD build breakdown tags each step **[iOS]**/**[Backend]**/**[Algo/Data]**.
- Marketing/research tasks: `backlog → in-progress(owner) → acceptance(product) → done`.
- Rejection loops (backward only): trd-review objection → `trd` · code-review REQUEST CHANGES → `build` · qa FAIL → `build` · acceptance REJECT → `build`. The rejecting agent writes concrete findings; the fixing agent addresses exactly those. (No more "→ `design`" fallback — there's no pre-build design step to reject back into; a design/UX finding at acceptance now goes to `build` and/or becomes input to a post-ship redesign pass.)
- `blocked-on-aviran`: strategy/scope decisions, money, external accounts (App Store, credentials), destructive ops — including every Supabase migration apply step (Aviran holds the DB credentials). Nothing else stops the loop.
- A task is `done` only after `product` ACCEPTs it against the PRD and Aviran has signed off at `aviran-review`.

Task rows: `| id | task | state | owner | updated | notes / findings / output |`

## Active phase

**Phase 1 — ship one map to real strangers in Tel Aviv.** Per `strategy/passenger-strategy.md`: nothing downstream matters until a real person reopens the app within a week, unprompted. As of 2026-07-31, this roadmap phase is itself sequenced into three **build phases** — see the section immediately below. **Build Phase 1 is the current active work.**

## V1 Build Phases (added 2026-07-31 — Aviran, live chat: "yes, replan board around the 3 phases.")

**A third, distinct meaning of "phase" in this doc — don't conflate the three:**
- **Roadmap phases** (`CLAUDE.md`, 1–5: MVP skeleton → feature buildout+signing → beta → marketing spend → new-city test). This whole build-phase structure lives entirely inside roadmap Phase 1.
- **Strategy/scope phases** (`strategy/passenger-strategy.md`, the Scope gate above): *what features exist at all* — V1 vs. parked "Phase 2" (proximity intelligence, Scenic View, Live Events) vs. parked "Phase 3" (AI local guide, shake-to-decide). **Unaffected by this section** — Live Events and Scenic Walk are confirmed in V1 scope (decisions #32/#34); this section only says *when in the build* their real data gets wired up.
- **Build phases (this section, new):** *how V1 itself gets built and shipped*, dev-sequencing only. No feature added, cut, or rescoped — same PRDs, same requirements, different order and different real-vs-fake data at each step.

**Build Phase 1 — ACTIVE. Ship the iOS app, 100% client-side, no backend.** Fake/hardcoded data baked into the app — just enough to demo interactions (saving places, the 12h time slider, browsing Hoods/places).

**Build Phase 2 — connect the real Supabase database, backfill with past/historical data, verify it works end-to-end.**

**Build Phase 3 — connect real live data.** Heat-area computation against live data, live events, TikTok/Reddit sourcing, etc.

| Task | Feature | Build phase | Status / note |
|---|---|---|---|
| T-031 | Map — Hoods & heat area | 1 | `done`. Placeholder Hood geometry and heat-never-observed-live were carried as named gaps at acceptance — now understood as *intentional* Phase-1 fake-data scope, not defects. |
| T-032 | Time slider | 1 | **TRD v2 delivered — ready for `trd-review`** (`architect`, 2026-08-02). No backend dependency in any phase. **One Phase-1 finding folded into this task's own build scope:** `DensityStore` has no seed path at all (live → cache → unavailable only), so with no `SupabaseConfig.plist` every Hood renders empty at every hour and the slider would visibly do nothing — a bundled relative density seed now ships as build step C10, behind the existing `BuildPhase.seedIsAuthoritative` constant, same pattern as T-033/T-034. Flagged for `product` at review. |
| T-033 | Hood & place detail | 1 — **ACCEPTED, awaiting Aviran** | `aviran-review`, **`product`: ACCEPT** (`f4fab6a`, 2nd attempt) — both REJECT findings re-verified from source, not from qa/reviewer verdicts. Posted review-ready summary to `PAS-13`, `gate:awaiting-aviran-review` applied. 2nd feature to reach this gate after T-031. `product` made the call (`30e002f`), `architect` amended the TRD (§3.4.1, §4.3, §7, §8 D10, C3/C3a/B2). Bundled seed is authoritative in Phase 1 via a `BuildPhase.seedIsAuthoritative` constant, with three pass/fail determinism assertions; live fetch ships built-but-inert and Phase-1 acceptance explicitly does not cover it. |
| T-034 | Live events overlay (client UI) | 1 — **resolved, no tension** | `trd` (advanced 2026-08-02 — pre-code design gate retired, unclaimed and ready for architect). **Fixed** (`product`, `1c37640`, Draft v2): launch-blocking gate re-attributed to T-043/Build Phase 3, this client confirmed a clean Phase-1 fit. One real fixture-ownership gap surfaced by the fix, not yet closed — see main table. |
| T-035 | Tourist-trap flag & Local QA | 1 | `trd` (advanced 2026-08-02 — pre-code design gate retired; spec+mockup already done, carried forward as reference). |
| T-036 | Places — Been & Saved | 1 | `trd` (advanced 2026-08-02 — pre-code design gate retired). Design spec + mockup now **complete** (`design/phase-1/places-been-saved-design.md`), carried forward as reference input for the architect, not a required gate. Device-local, fake seed data. |
| T-037 | Passport | 1 | `trd` (advanced 2026-08-02 — pre-code design gate retired, ready once T-036's TRD lands). Device-local. |
| T-038 | Search & quick filters | 1 | `trd` (advanced 2026-08-02 — pre-code design gate retired, unclaimed and ready for architect). Fake dataset. |
| T-040 | Hood dataset — real Tel Aviv geometry | 2, **except C1/C2/C2a → 1** (ratified 2026-07-31; written into the TRD, §8 D10 / §3.5 / §11). Data/backend track (A1, B1–B6) advanced ahead of the Phase-2 gate by explicit founder-direct instruction — a one-off dispatch exception. C3–C5 (bundle swap, tripwire test, device perf check) still Phase 2. | Phase-1 carve-out (C1/C2/C2a) built and code-review approved. Data/backend track (A1, B1–B6) built 2026-07-31 — real 24-Hood Tel Aviv-Yafo dataset (OSM/Nominatim, ODbL), migrations `003`/`006` generated, neither applied (Aviran-gated). **`product` ACCEPT** (`f4fab6a`, 2nd attempt) — generator round-trip re-verified byte-identical. Req 4 (Hood blurbs) carried forward unmet — 23/24 Hoods still null. `gate:awaiting-aviran-review` applied to `PAS-17`, 2nd feature to reach `aviran-review`. C3 already landed pending reviewer confirmation; C4/C5 still open. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-042 | Places dataset — real Tel Aviv places | 2 | `build`-ready. **HELD — not dispatched.** Same reasoning as T-040. |
| T-041 | Pin clustering | 2 | `backlog`, unowned. Irrelevant at Phase 1's handful of fake pins; becomes real the moment T-040/T-042's real data lands. |
| T-044 | Place closed-state refresh | 2 | `backlog`, unowned. A real-dataset maintenance concern — meaningless until real data exists. |
| T-043 | Live events ingestion pipeline | 3 | `build`-ready. **HELD — not dispatched.** Matches "live events" in Aviran's own Phase-3 description directly. |
| PAS-6 items 8–10 (Scenic Walk routing, TikTok import, live-events launch risk) | — | 3-adjacent | Aviran-blocked scope calls, not build-order questions — resolve whenever, don't gate Phase 1. |

**~~Known tension, flagged not resolved~~ — RESOLVED 2026-07-31, both halves**, and superseded since: T-033 and T-040 have both since reached `product` ACCEPT and `aviran-review` (see Tasks table). Bundled seed is authoritative in Phase 1 behind a testable determinism requirement; live fetch ships built-but-inert. Full original text: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`.

**T-040/T-042/T-043 TRDs stay approved as-is — no rework spend.** All three passed `trd-review` unanimously (6/6, 3/3, 4/4) and the security-auditor sweep (PASS on all). No reason to re-litigate; they simply wait for their build phase to start. Re-review only if real time passes and a sibling schema shifts underneath them before Phase 2/3 build begins.

## Tasks

| id | task | state | owner | updated | notes / output |
|---|---|---|---|---|---|
| — | _empty — awaiting first `product` pass over the strategy doc_ | — | — | 2026-07-26 | Fresh start. See PROGRESS.md entry for 2026-07-26. |
| M1 | Phase 1 marketing & acquisition plan (grassroots, zero-spend, Tel Aviv) | in-progress | marketing | 2026-07-28 | `marketing/phase-1/marketing-acquisition-plan.md`. **REJECTED by `product`** (`e5339ea`) — back to `in-progress(marketing)`. Blocking: (F1) "2-8 day" reopen window silently answers strategy's open "within a week" question, unlabeled; (F2) "unprompted" defined against a factually wrong no-notifications claim (decision #24 puts one in V1); (F3) "confirmed by Aviran" zero-budget claim is unsourced, self-contradicts the plan's own QR-print-cost ask. Full verdict in `PROGRESS.md`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-030 | Scope live-events ingestion pipeline — new V1 launch-blocking dependency (Linear `PAS-5`) | done | data-engineer | 2026-07-30 | **Closed by `chief-of-staff` — deliverable complete.** Full writeup: `passenger-brain/data-eng/live-events-feasibility.md`. **Explicit call: Conditional** — thin ticketed-only version buildable in Phase 1; fuller "feels alive" version is a data-access gap, not buildable with confidence yet. Recommends shipping thin for V1. Aviran-facing follow-on (thin-version acceptability) moved to `PAS-6` item 10. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| PAS-6 | Confirm 2026-07-30 founders-meeting V1 scope ambiguities | blocked-on-aviran | — | 2026-07-30 | Standing tracker for founders-meeting scope ambiguities. 6 of the original 7 items RESOLVED (decisions #37-42). **Genuinely still open:** (7) Google Maps import (non-blocking); (8) Scenic Walk — ship the lighter locked version or slip for weighted routing?; (9) TikTok import — ToS/legal sign-off; (10) Live events — thin ticketed-only acceptable for V1, or real launch-risk? **Items 11-16 added 2026-07-31** from product's data-requirements sweep: places-dataset authorship/sizing, `place_type` visibility, tourist-trap first-flag-value owner, OSM/municipal licence terms, TikTok-import schema collision. Full detail: `strategy/passenger-strategy.md` Open questions, `strategy/decisions.md` #27-42. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| PAS-7 | Scope Scenic Walk weighted-routing + TikTok import feasibility | done | data-engineer | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Full writeup: `passenger-brain/data-eng/scenic-walk-tiktok-feasibility.md`. Calls: (1) Scenic Walk weighted routing — **not buildable in Phase 1 window**; recommend shipping the already-locked lighter polyline-comparison version instead. (2) TikTok place extraction — **buildable in Phase 1 window** (~2-4 weeks); ToS/access blocker flagged. (3) Google Maps saved-list import — feasibility read only, per decision #35, not scoped as a build. Both Aviran-facing follow-on decisions moved to PAS-6 (items 8-9) so they don't go stale inside a closed ticket. |
| PAS-8 | Reconcile UX docs (`ux-flows.md`, `map-rendering-spec.md`) against 2026-07-30 scope lock | done | designer | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Reconciled: Hoods terminology, Been-place Passport stickers, Saved-vs-Been distinction, Passport promoted from Phase-2-preview (Journey 7) into real V1 scope, category rename. **Two small residuals flagged for a future docs pass (not blockers):** `map-rendering-spec.md` §3 renders "Tourist-heavy spot" at a Hood centroid — adjectival mismatch (Hood is a polygon, not a "spot"), one-word fix; `ux-flows.md:176` still has stale "not flagged a tourist trap" phrasing predating decision #37. |
| PAS-9 | Revise "tourist trap" copy in Phase 1 marketing plan | done | product | 2026-07-30 | **ACCEPTED by `product`** (verified diff `5e23ef2` directly, not just the worklog summary) — term satisfies decision #36's ask, scope discipline intact, more honest post-decision-#37 since the underlying flag is now a plain boolean. Product also struck a now-stale note in `prds/hood-place-detail/hood-place-detail.md` that told designer to wait on this term (decision #42 logged). Closed by `chief-of-staff`. |
| PAS-10 | Draft V1 PRDs for confirmed 2026-07-30 scope items | done | product | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Four PRDs written (T-031…T-034 below). **Update: of the five originally-held items, three are now unblocked** (tourist-trap/localness pipeline, Places incl. closed-place edge case, Passport — PAS-6's items 1/2/4/5/6 all resolved, see PAS-6 row) — dispatching `product` for these next. Scenic Walk and TikTok import remain genuinely blocked (PAS-6 items 8-9, Aviran's call). Google Maps import stays excluded (decision #35). |
| T-031 | Map — Hoods & heat area (PRD) | done | — | 2026-07-31 | **DONE — accepted by Aviran directly in live chief-of-staff chat, 2026-07-31** ("accept T-031"). Linear `PAS-12` moved to Done, `gate:awaiting-aviran-review` removed. First feature in this project to reach `done`. Two named gaps carried forward, not waived: real Hood geometry (tracked at `T-040`/`PAS-17`) and heat never observed against live data (migrations unapplied). Both documented in `prds/map-hoods-heat/map-hoods-heat.md`'s Open questions & risks and Decisions log. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-032 | Time slider — now → +12h (PRD) | trd | architect (TRD v2 delivered 2026-08-02 — **ready for `trd-review`**; state transition is `chief-of-staff`'s) | 2026-08-02 | **TRD v2 delivered: `prds/time-slider/TRD.md`.** Routes to **`ios-developer` + `ios-code-reviewer` only** — iOS-only, no `[Backend]`/`[Algo/Data]` step (re-verified against the shipped `DensityAPI`/`hood_density` contract, not assumed). v1 designed only the button/modal path; v2 adds the edge-gesture architecture and settles the two items the design spec handed to `architect`: **D7** — the edge gesture is a SwiftUI `DragGesture` on a hit-testable sibling overlay above `Map`, *not* a `UIGestureRecognizer`, because SwiftUI's `Map` exposes no recognizer to arbitrate against and hit-test order makes arbitration unnecessary (this corrects the design spec's own stated mechanism for the 24pt zone; **Q7's ratified number and its acquisition argument are untouched, not reopened**); **D8** — 64pt/40pt confirmed correct (59pt Dynamic Island + 5, 34pt home indicator + 6) and re-derived as `max(floor, safeAreaInset + clearance)` so they hold off the 812pt reference device. Three new architect calls flagged for `product`/`designer` at review, each made trivially reversible: **D9** edge path portrait-only on iPhone (landscape puts the capture zone under the sensor housing, voiding Q7's own bezel argument — no upstream doc considered orientation, and the app ships all three); **D10** the bundled density seed (see build-phase row); and the two accepted costs of D7 (a 24pt undraggable/untappable map strip per live edge, and a horizontal drag starting in the strip consumed without panning). Q3/Q7/Q8 built as ratified settled requirements — Q3 made structural (exactly one storage location for the hour app-wide, so there is no second property to desync). **New §9 verification table, one row per P0 with a falsifiable pass condition** (per L-018/`architect.md`) — this is where T-031's two carried-forward untested req-2 bullets get discharged, and C10 is the precondition that makes either checkable at all. Build breakdown C1–C14, all **[iOS]**; C10/C11 sequenced first. Heat glyph `flame.fill` adopted from `designer`'s 2026-08-02 pin, closing D6's flagged gap. Prior context (design-gate retirement, glyph pass, Q7/Q8/Q3 ratification) below, unchanged: **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, `design-review` skipped.** Q7/Q8/Q3 ratified (`eb1cb7b`) — verified directly against the commit diff by `chief-of-staff`, not taken on a relay's description; content well-formed (L-002/L-013 compliant), matches this row's own prior note of exactly which three questions were outstanding. Design work already in hand from before the retirement (design-approval had both signatures in) — `time-slider-design.md` + mockup remain valid reference input for the architect, just no longer a gate. All 5 blocking + 7 should-fix findings previously re-verified fixed at the artifact (contrast, opaque backing, sheet drag-fallthrough, Fitts's-Law citation, persistent-hint contradiction reconciled as Q8). Scope gate re-run clean. Flagged, not fixed: PRD is 5,256 words against a ~1,200 word budget. **Heat-button glyph landed, `aabd863`.** Aviran, via hilos/@chief, verbatim "change the heat icon to flame" — routed [ASSUMPTION] to this task's own unbuilt heat-button glyph, closing TRD **§8 D6**'s flagged gap (corrected from this row's own earlier D1 mislabel — checked directly against the TRD's own section headers by both `designer` and, independently, `chief-of-staff`). `designer` pinned `Image(systemName: "flame.fill")`; design spec bumped to v4, mockup republished in place (v5). Verified by `chief-of-staff` against the commit diff and TRD text directly, not taken on the designer's word alone; the mockup's rendered visual result is not independently screenshot-confirmed (session has no claude.ai auth). `PAS-15` closing comment posted, `PROGRESS.md` stub `231b0ee`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-033 | Hood & place detail (PRD) | aviran-review | — | 2026-07-31 | **VERDICT: ACCEPT** (`f4fab6a`, 2nd attempt) — `product` re-derived both prior REJECT findings from source rather than trusting qa/reviewer verdicts (showsNames tap-gate; blurb generator round-trip re-run to a scratch path, byte-identical). Carried forward to `aviran-review`, not waived: `hood-dataset` req 4 unmet — 23/24 Hoods still `blurb: null`, `florentin` ships `[PROVISIONAL]` only. 2nd feature in this project to reach `aviran-review`, after T-031. Posted review-ready summary, `gate:awaiting-aviran-review` applied to `PAS-13`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-034 | Live events overlay (PRD) | trd | — (unclaimed, ready for architect) | 2026-08-02 | **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, no design spec needed.** No spec had been drafted yet at retirement time, so nothing is lost skipping it. `prds/live-events-overlay/live-events-overlay.md`, Draft v2 (`1c37640`) — corrected the stale "launch-blocking on PAS-5" framing: the launch gate (`strategy/passenger-strategy.md` line 46) attaches to the pipeline (T-043, Build Phase 3), not this client, which is a pure renderer unblocked for Phase 1. **Real finding: needs a bundled fake event set — folded into T-034's own build scope as a named step**, same pattern as T-033's C3 fixture. Depends on T-031 (done), T-032 (now `trd`, no longer `design-approval`). Linear `PAS-25` — corrected from stale `owner:designer`/In Progress to unclaimed/Todo, DoD's "Design pass complete" line struck (see comment on the issue). Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| PAS-11 | Phase 2 candidate: "Local personas" (concept, not scoped) | blocked-on-aviran | — | 2026-07-30 | Founder-direct, live hilos chat, 2026-07-30. New Phase 2 candidate — curated local-guide profiles a user can follow as a discovery lens. **Not Phase 1, not authorized to build, no PRD.** Recorded in `strategy/passenger-strategy.md` Phase 2 candidates + `strategy/decisions.md` #43. **Three questions sit with Aviran** (strategy.md Open questions #8-10): authoring model (first-party vs. user-authored); follow vs. lens framing (`product` recommends lens-only); same concept as Phase 3's "AI local guide" or separate. Not a launch blocker. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |

| T-035 | Tourist-trap flag & Local QA (PRD) | trd | — (unclaimed, ready for architect) | 2026-08-02 | **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, `design-approval`/`design-review` skipped.** A design spec + mockup already exist from before the retirement — `design/phase-1/tourist-trap-flag-design.md`, mockup https://claude.ai/code/artifact/40b9ec94-b831-4d20-a334-fb95bf5f4cbd — and remain valid reference input for the architect even though they're no longer a gate. Covers the Hood outline flag stroke (flagged/not-flagged/busy+flagged, by zoom tier, built against `HoodLayer.swift`'s real current constant-stroke code), the place-detail-modal flag line (fills `PlaceDetailModal.swift`'s reserved `touristTrapSlot`), and the local-QA toast (binary Yes/No, top-anchored, non-blocking). **Real finding, already fixed:** `map-rendering-spec.md` §7's own quoted VoiceOver example ("not a tourist trap") violates decision #42's copy ban — flagged for that doc's own owner to correct, this spec uses compliant strings throughout. Every PRD open question ([ASSUMPTION]s, cold-start risk, notification-denied gap, reward-for-answering gap) carried forward untouched, not resolved by this pass. Linear `PAS-26`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-036 | Places — Been & Saved (PRD) | trd | — (unclaimed, ready for architect) | 2026-08-02 | **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, `design-approval`/`design-review` skipped.** Design spec + mockup now **complete** — `design/phase-1/places-been-saved-design.md`, mockup https://claude.ai/code/artifact/2ab0b9c8-5d59-43dd-90e8-8163a7d0b93f — plus a dated addendum to `map-rendering-spec.md` §6 (dashed-ring shape pairing, filling a gap `ux-flows.md` §2.1 had flagged and PRD req 7 requires). Kept as valid reference input for the architect, same treatment as T-035, not a required gate. Covers the Places list, provenance-word rows with read-time precedence (Saved>Been>Visited), the row-density resolution the PRD explicitly left to `design-review` (now resolved in the spec regardless, §2.2), the permanently-closed badge, and the map ring accent. **Cross-spec check against `tourist-trap-flag-design.md` (T-035) caught and fixed a real drift before submission:** an early draft had invented a second amber for the tourist-heavy line instead of reusing T-035's already-specced `Color("Flag")` token — corrected in both the spec and the mockup. `prds/places-been-saved/places-been-saved.md` (Draft v1) by `product`, unblocked by decision #38. Depends on T-031 (done), sequenced before T-037. **Data-requirements sweep (2026-07-31):** closed-state refresh promoted to a named ownerless job (`T-044`/`PAS-22`), still unresolved — not this pass's to fix. Stale dependency line in the PRD (says `map-hoods-heat`, should say `hood-place-detail`) flagged for product to fix, not yet fixed. Real open items for the TRD (spec §8): `Place.permanentlyClosed` needs adding to the model (decoded from the seed JSON's already-authored, currently-unused `permanently_closed` key); a Been/Visited provenance store needs building (no live dwell detector in Build Phase 1 — recommend a bundled fixture, not a real detector this early); the read-time precedence merge needs a class/actor home. Linear `PAS-27`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-037 | Passport (PRD) | trd | — (unclaimed, ready for architect once T-036's TRD is in) | 2026-08-02 | **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, no design spec needed.** `prds/passport/passport.md` (Draft v1) by `product`, unblocked by decisions #39/#40. Private per-city sticker album + per-Hood Local status, Profile tab. 7-tier ladder excluded outright. Depends on T-031 (done), T-036 (now `trd`). **Three product findings flagged for Aviran, not resolved:** (1) sticker-shape-matches-place-type needs a `place_type` field — now owned by T-042; (2) nothing currently rewards answering local-QA, the pipeline's named incentive risk; (3) Passport is device-local, doesn't survive reinstall — unconfirmed if acceptable. Linear `PAS-28`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-039 | Port Scenic Walk / TikTok feasibility into `prds/` PRD-gate location | done | data-engineer | 2026-07-30 | Found `PAS-7` already closed this exact ask 2026-07-30 — ported its findings into `prds/scenic-walk/feasibility.md` and `prds/tiktok-import/feasibility.md` (the location `prds/INDEX.md`'s scope gate actually reads) rather than re-deriving. Fixed `INDEX.md`'s stale framing. **Calls carried from PAS-7:** Scenic Walk's weighted-routing not buildable in Phase 1, recommend the lighter locked version (decision #19) pending Aviran's ship-vs-slip call; TikTok import technically buildable (~2-4 weeks), gated on Aviran's ToS sign-off. Both PRDs held on `PAS-6` items 8-9. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-040 | Hood dataset — Tel Aviv geometry & attributes (PRD) | aviran-review | — | 2026-07-31 | **`product` ACCEPT** (`f4fab6a`, 2nd attempt) — req 6 (generator pipeline) confirmed by re-running to a scratch path, byte-identical output. Req 4 (Hood blurbs) carried forward unmet — 23/24 Hoods still null. Posted review-ready summary, `gate:awaiting-aviran-review` applied to `PAS-17`. 2nd feature to reach `aviran-review`. Linear `PAS-17`. **B1/B2/B4/B6/C3 (real geometry authoring, migration `003`) stay HELD for Build Phase 2** — C1/C2/C2a (client blurb decode) is what landed and was accepted in Build Phase 1. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-041 | Own pin clustering (currently fully specified, claimed by no PRD) | backlog | — | 2026-07-31 | **Build Phase 2** (2026-07-31 replan) — irrelevant at Phase 1's handful of fake pins. Surfaced by T-033's TRD. `design/map-rendering-spec.md` §5 already fully specifies clustering behavior (neutral clustering, no personal-place indication inside a cluster) — but no PRD claims ownership of actually building it; T-033's own scope explicitly excludes it ("minimal version" of the place-pin layer). Same shape as T-040 was — a real, specified need sitting unowned. **Not picked up by `product`'s data-requirements sweep** (it's a behavior/ownership gap, not a data/schema need in the sweep's sense) — still open, needs `product` to fold it into an existing PRD's scope or write a short supporting PRD in a future pass. Not launch-blocking with a small pin count, becomes one the moment T-040/T-042's real data lands. Linear `PAS-30` (backfilled 2026-07-31 reconciliation pass). |
| T-042 | Places dataset — Tel Aviv curated places (PRD) | build | developer + data-engineer (build-ready, not yet dispatched) | 2026-07-31 | **HELD — Build Phase 2**, same reasoning as T-040. TRD delivered (`4d126f1`), all `trd-review` verdicts in, consolidation pass landed (`293ddb2`) — migration pinned to `004_places.sql`, closed-state-refresh framing fixed, field-ownership/dual-emitter conflicts resolved. `security-auditor` PASS, no findings. **`build`-ready**, not yet dispatched — needs `developer`+`code-reviewer` (A1-A5) and `data-engineer`+`code-reviewer` (B1-B5). B5 (actual dataset) still blocked on Aviran (`PAS-6` item 11) + T-040's real geometry. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-043 | Live events ingestion pipeline (PRD) | build | developer + data-engineer (build-ready, not yet dispatched) | 2026-07-31 | **HELD — Build Phase 3.** TRD delivered (`e142e91`), `trd-review` fully closed (all 4 reviewers) after the HIGH-severity `revoke execute` security fix landed (`27911b0`) and the `events.hood_id` FK rationale was written down. **`build`-ready**, not yet dispatched — needs `developer`+`code-reviewer` (A1-A4) and `data-engineer`+`code-reviewer` (B1-B7) once Phase 3 starts. Linear `PAS-20` (accidentally duplicated as `PAS-23`, self-caught, marked Duplicate). Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |
| T-038 | Search & quick filters (PRD) | trd | — (unclaimed, ready for architect) | 2026-08-02 | **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, no design spec needed.** `prds/search-quick-filters/search-quick-filters.md` (Draft v1) by `product`. Sheet over the map, one field matching name/keyword/Hood, 2 category chips, results dim the map. Depends on T-031 (done)/T-032 (now `trd`)/T-033 (aviran-review). **Two flagged for Aviran, not resolved:** (1) sheet layout — 50/50 split vs. native detents; (2) keyword search had no data behind it — **now fixed**, keyword field owned by T-042/places-dataset (2026-07-31 sweep). Linear `PAS-29`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. |

| PAS-21 | No git remote configured — retitled by a concurrent session to the narrower open question: is pushing now authorized? | blocked-on-aviran | — | 2026-07-31 | **The "no remote" gap itself is CLOSED** — Aviran configured both remotes 2026-07-31 (`origin` → `github.com/AviranGrisaro/passenger-brain` and `-code`, both `main`); `passenger-code` fully synced (0 ahead/behind); `passenger-brain` has unpushed local commits (3 as of Linear's own count: `1c37640`, `30e002f`, `489a9eb`). **What's still genuinely open, Aviran's to answer:** does configuring the remote mean agents should start pushing routinely, or does he want to keep doing the actual `git push` himself? Standing policy today (`CLAUDE.md`) is the latter — agents commit, don't push, tell him what's ready. **Correction to my own action this run:** I initially marked the Linear issue `Done` without noticing a concurrent session had already retitled/re-described it into this exact open question — caught it, reverted to `Backlog`/`aviran-blocker`, apologized in-thread. Leave this to Aviran; don't re-close without his answer. |
| PAS-24 | Phase 2 candidate: place metadata/tagging attributes (discovery engine) | backlog | — | 2026-07-31 | Founder-direct, live `/cos` chat, 2026-07-31 (PROGRESS.md FOUNDER-DIRECT STUB). New Phase 2 candidate — per-place metadata/tag attributes for the discovery engine: romantic, seat_outside, allow_smoking, kid_friendly, wifi, pet_friendly given as a starter-list example, not a closed spec. **Not Phase 1, not authorized to build, no PRD.** No `type:data-request`/`type:feature` labels exist in this Linear workspace — used the existing `Feature` label. No `aviran-blocker` (unlike PAS-11, nothing here needs Aviran's decision before it can sit in Backlog — it's parked only because Phase 2 itself is parked). Related-but-distinct existing docs, linked in the ticket for whoever scopes this later: `data-eng/discovery-engine-spec.md` (algorithm/pipeline, no place-attribute model) and `prds/places-dataset/places-dataset.md` (T-042/PAS-19, owns the curated `places` schema today, no amenity/vibe tags). Traceability only — resolve whenever Phase 2 starts. |
| T-044 | Own place closed-state refresh (flagged 3 times, no owner) | backlog | — | 2026-07-31 | **Build Phase 2** (2026-07-31 replan) — a real-dataset maintenance concern, meaningless until real data exists. Linear `PAS-22`. **Row was missing from BOARD.md entirely — backfilled during the 2026-07-31 full reconciliation pass; Linear issue existed since 2026-07-30 with no corresponding board row.** Flagged independently 3 times (`product`'s data-requirements sweep, `places-been-saved`/T-036's own PRD, `places-dataset`/T-042's TRD) without ever getting an owner. A place can go from open to permanently-closed over time; nothing specifies what mechanism keeps that status current after initial dataset authoring. Needs `product` to fold into an existing PRD's scope (most likely `places-dataset`, as a v2/maintenance concern) or write a short supporting PRD. |
| PAS-16 | Centralize map background-interaction pattern (`.presentationBackgroundInteraction`) for sheets | backlog | — | 2026-07-30 | **Row was missing from BOARD.md entirely — backfilled during the 2026-07-31 full reconciliation pass; Linear issue existed since 2026-07-30 with no corresponding board row.** Split off from `PAS-14` (closed) — the map-surface-interactivity mechanism T-033 solved via `.presentationBackgroundInteraction(.enabled(upThrough: .medium))` lives only in that task's own design spec, not in `design-principles.md`/`ux-flows.md`. `ux-flows.md` L59 still makes the same unspecified claim for T-038's search sheet with no mechanism named. Not launch-blocking, small well-scoped craft task for `designer`. Still unclaimed. |

| T-045 | 10 uncommitted `agents-mirror/*.md` edits + 2 untracked `design/*.md` belong to nobody | done | project-manager | 2026-08-01 | **RESOLVED 2026-08-01 (nightly PM audit).** All 10 mirror files and both design docs are now committed — 9 mirrors + the 2 design files landed in other sessions' commits earlier today (`fcd2f54` et al.); the last one (`agent-os/agents-mirror/chief-of-staff.md`) was verified byte-identical to live `.claude/agents/chief-of-staff.md` and committed by this run (`5677f3d`), along with its matching `passenger-code/.claude/agents/chief-of-staff.md` + `marketing.md` snapshots (`542ea3b`). `git status` clean in both repos as of this audit. |

## Linear

Workspace `passenger`, team **Passenger** (`PAS`), one project — **Passenger V1**. Features are issues inside that project, not projects of their own; the previous workspace fragmented into 13 projects and lost the thread. `chief-of-staff` is the only agent that writes to Linear.
