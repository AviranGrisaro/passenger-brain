# Passenger Agent OS — Board

Single source of state for the agent team. Every agent reads this **and `PROGRESS.md`** before starting, and updates its rows + appends a PROGRESS.md worklog entry after finishing — committed and pushed the same turn. `chief-of-staff` is the runtime: each "run the company" pass moves every task as far down the lifecycle as it can and refills the pipeline via `product` when it drains.

## Scope gate (added 2026-07-26 — read before opening any task)

Passenger exists because the previous board ran 27 PRDs through this exact lifecycle, ten of them building features the strategy forbids. Every gate passed. Nobody read the strategy.

**A task may not leave `spec` unless its PRD quotes the line in `strategy/passenger-strategy.md` that authorizes it.** `product` enforces this and rejects PRDs that can't. Standing prohibitions:

- No social features of any kind — friends, following, posting, presence, profiles, avatars.
- No onboarding — the app opens straight to the map plus location permission.
- Phase 2 (proximity intelligence, **Scenic View**, **Live Events**) and Phase 3 (AI guide, shake-to-decide) are parked. Don't build toward them or leave hooks. Points is retired — replaced by the Phase 2 stamp collection system (see `strategy/passenger-strategy.md`), not a separate parked item.
- ~~**Scenic View and Live Events moved out of V1 on 2026-07-27** (decisions #19, #20).~~ **Reversed — do not read this as a prohibition.** Live Events returned to V1 on 2026-07-29 and Scenic Walk on 2026-07-30 (decisions #32, #34; `strategy/passenger-strategy.md` V1 scope). Struck rather than deleted, per convention. The salvage warning still stands: both existed in the Locali client, so watch for the *old* shapes returning through `SALVAGE.md` rather than through a PRD — the Locali events overlay was an unflagged raw feed, not the ranked subset now specced. Flagged by `product`, 2026-07-30 (PAS-10).

## Task lifecycle (state → owner)

```
backlog → spec(product) → design(designer) → trd(architect) → build(ios-developer and/or developer)
        → code-review(ios-code-reviewer and/or code-reviewer) → qa(qa) → acceptance(product) → done
```

- **iOS vs backend split:** `build`/`code-review` fan out by surface — `ios-developer`/`ios-code-reviewer` for the Swift/SwiftUI client (`passenger-code/`), `developer`/`code-reviewer` for the Supabase backend (`passenger-brain/database/`), both pairs when a task spans both. The architect's TRD build breakdown tags each step **[iOS]**/**[Backend]**.
- Pure-build tasks (no UX surface) skip `design`.
- Marketing/research tasks: `backlog → in-progress(owner) → acceptance(product) → done`.
- Rejection loops (backward only): code-review REQUEST CHANGES → `build` · qa FAIL → `build` · acceptance REJECT → `build` or `design`. The rejecting agent writes concrete findings; the fixing agent addresses exactly those.
- `blocked-on-aviran`: strategy/scope decisions, money, external accounts (App Store, credentials), destructive ops — including every Supabase migration apply step (Aviran holds the DB credentials). Nothing else stops the loop.
- A task is `done` only after `product` ACCEPTs it against the PRD.

Task rows: `| id | task | state | owner | updated | notes / findings / output |`

## Active phase

**Phase 1 — ship one map to real strangers in Tel Aviv.** Per `strategy/passenger-strategy.md`: nothing downstream matters until a real person reopens the app within a week, unprompted. As of 2026-07-31, this roadmap phase is itself sequenced into three **build phases** — see the section immediately below. **Build Phase 1 is the current active work.**

## V1 Build Phases (added 2026-07-31 — Aviran, live chat: "yes, replan board around the 3 phases.")

**A third, distinct meaning of "phase" in this doc — don't conflate the three:**
- **Roadmap phases** (`CLAUDE.md`, 1–5: MVP skeleton → feature buildout+signing → beta → marketing spend → new-city test). This whole build-phase structure lives entirely inside roadmap Phase 1.
- **Strategy/scope phases** (`strategy/passenger-strategy.md`, the Scope gate above): *what features exist at all* — V1 vs. parked "Phase 2" (proximity intelligence, Scenic View, Live Events) vs. parked "Phase 3" (AI local guide, shake-to-decide). **Unaffected by this section** — Live Events and Scenic Walk are confirmed in V1 scope (decisions #32/#34); this section only says *when in the build* their real data gets wired up.
- **Build phases (this section, new):** *how V1 itself gets built and shipped*, dev-sequencing only. No feature added, cut, or rescoped — same PRDs, same requirements, different order and different real-vs-fake data at each step.

**Build Phase 1 — ACTIVE. Ship the iOS app, 100% client-side, no backend.** Fake/hardcoded data baked into the app — just enough to demo interactions (saving places, the 12h time slider, browsing Hoods/places).

**Build Phase 2 — connect the real Supabase database, backfill with past/historical data, verify it works end-to-end.**

**Build Phase 3 — connect real live data.** Heat-area computation against live data, live events, TikTok/Reddit sourcing, etc.

| Task | Feature | Build phase | Status / note |
|---|---|---|---|
| T-031 | Map — Hoods & heat area | 1 | `done`. Placeholder Hood geometry and heat-never-observed-live were carried as named gaps at acceptance — now understood as *intentional* Phase-1 fake-data scope, not defects. |
| T-032 | Time slider | 1 | `design`. Fake data throughout, no backend dependency — clean Phase-1 fit. |
| T-033 | Hood & place detail | 1 — **fix landed, blurb decision made, fast re-review in flight** | `build`, **Fix pass landed** (`307e5af`) — ✕ button, D4 test, all 9 fixture coords repositioned inside real Hoods, all verified independently. **Blurb-branch scoping call made (chief-of-staff):** one `[PROVISIONAL]` blurb hand-added to `florentin`. `ios-code-reviewer` fast re-review dispatched on the combined diff — last gate before `qa`. `product` made the call (`30e002f`), `architect` amended the TRD (§3.4.1, §4.3, §7, §8 D10, C3/C3a/B2). Bundled seed is authoritative in Phase 1 via a `BuildPhase.seedIsAuthoritative` constant, with three pass/fail determinism assertions; live fetch ships built-but-inert and Phase-1 acceptance explicitly does not cover it. |
| T-034 | Live events overlay (client UI) | 1 — **resolved, no tension** | `design`. **Fixed** (`product`, `1c37640`, Draft v2): launch-blocking gate re-attributed to T-043/Build Phase 3, this client confirmed a clean Phase-1 fit. One real fixture-ownership gap surfaced by the fix, not yet closed — see main table. |
| T-035 | Tourist-trap flag & Local QA | 1 | `design` (queued). Fake flag values. |
| T-036 | Places — Been & Saved | 1 | `design` (queued). Device-local, fake seed data. |
| T-037 | Passport | 1 | `design` (queued). Device-local. |
| T-038 | Search & quick filters | 1 | `design` (queued). Fake dataset. |
| T-040 | Hood dataset — real Tel Aviv geometry | 2, **except C1/C2/C2a → 1** (ratified 2026-07-31; written into the TRD, §8 D10 / §3.5 / §11). **Data/backend track (A1, B1–B6) advanced ahead of the Phase-2 gate 2026-07-31 by explicit founder-direct instruction** (Aviran, chat: "create the Hood dataset based on the actual Tel Aviv hoods maps") — a one-off dispatch exception, not a phase-gate change; C3–C5 (iOS bundle swap, tripwire test, device perf check) are still Phase 2 and not yet dispatched. | Phase-1 carve-out (C1/C2/C2a): **BUILT + code-review APPROVED WITH MINOR NOTES** (`3d94ba6`/`8c7a838`, 2026-07-31), holding for `qa` jointly with T-033. **Data/backend track (A1, B1–B6): BUILT 2026-07-31 by `data-engineer`** — real 24-Hood Tel Aviv-Yafo dataset (OSM via Nominatim, ODbL), `validate_dataset.py`, `build_hoods.py`, migrations `003`/`006` (generated, neither applied — Aviran-gated), regenerated iOS bundle, shared containment fixture, drift-check repurpose of `export_hoods_geojson.py`. 0 errors/0 warnings on validation, cross-artifact ids/polygons verified identical, 9/9 fixture cases pass. **Not yet through `developer`/`code-reviewer` sign-off** — TRD §11 requires it for A1/B6's SQL and this agent doesn't own migration mechanics; flagged in the worklog entry, not self-certified. C3 (swap in the real bundle — **actually already landed**, `passenger-code` `72f4fc7`, its own message states "retires the C2a exception," just not yet `ios-code-reviewer`-confirmed), C4 (non-overlap tripwire over the real bundle), C5 (device perf check against real vertex counts, `map-hoods-heat/TRD.md` §7 budget) still open. See `PROGRESS.md` 2026-07-31 data-engineer entry for full detail. **Downstream fallout found 2026-07-31 (chief-of-staff, independent point-in-polygon check):** the real geometry landing broke T-033's Phase-1 fixture two ways — 6/9 `places-tel-aviv.json` coordinates now fall outside their real Hood polygon (were valid against the old placeholder rectangles), and all 24 real Hoods ship `blurb: null` (correctly not invented), so T-033's req-2 blurb-branch demo can't be satisfied by anything currently on disk. Both routed via `PAS-13`'s fix dispatch, not this issue's fault. |
| T-042 | Places dataset — real Tel Aviv places | 2 | `build`-ready. **HELD — not dispatched.** Same reasoning as T-040. |
| T-041 | Pin clustering | 2 | `backlog`, unowned. Irrelevant at Phase 1's handful of fake pins; becomes real the moment T-040/T-042's real data lands. |
| T-044 | Place closed-state refresh | 2 | `backlog`, unowned. A real-dataset maintenance concern — meaningless until real data exists. |
| T-043 | Live events ingestion pipeline | 3 | `build`-ready. **HELD — not dispatched.** Matches "live events" in Aviran's own Phase-3 description directly. |
| PAS-6 items 8–10 (Scenic Walk routing, TikTok import, live-events launch risk) | — | 3-adjacent | Aviran-blocked scope calls, not build-order questions — resolve whenever, don't gate Phase 1. |

**~~Known tension, flagged not resolved~~ — RESOLVED 2026-07-31, both halves.** T-034's half was fixed by `product` (`1c37640`, Draft v2 — gate re-attributed to T-043/Phase 3). T-033's half was decided by `product` (`30e002f`) and written into both TRDs by `architect` (`hood-place-detail/TRD.md` §8 D10; `hood-dataset/TRD.md` §8 D10): bundled seed pinned authoritative in Phase 1 behind a testable determinism requirement, live fetch built-but-inert, two-file fixture spec'd with pass/fail floors, T-040 C1/C2/C2a carved into Phase 1. **Both tasks may now be dispatched to `build` against their current text.** Original text preserved below for the record. **Known tension, flagged not resolved — needs an architect/product pass, not fixed in this replan:** T-033's landed TRD (`prds/hood-place-detail/TRD.md`) builds its client fetch directly against T-042's and T-040's real Supabase schema (`places`, `hoods.blurb`). T-034's PRD (`prds/live-events-overlay/live-events-overlay.md`) still states it's launch-blocking on `PAS-5`, the real events pipeline. Both contradict Phase 1's "100% client-side, no backend" mandate as currently written. Recommendation: an architect pass swaps the live Supabase fetch for a bundled fixture / hardcoded fake dataset for Phase 1 `build`, with the real wiring becoming a scoped Phase 2 (T-033) / Phase 3 (T-034) TRD amendment once T-040/T-042/T-043 actually land. Do not dispatch either task's `build` step against the current TRD/PRD text without resolving this first.

**T-040/T-042/T-043 TRDs stay approved as-is — no rework spend.** All three passed `trd-review` unanimously (6/6, 3/3, 4/4) and the security-auditor sweep (PASS on all). No reason to re-litigate; they simply wait for their build phase to start. Re-review only if real time passes and a sibling schema shifts underneath them before Phase 2/3 build begins.

## Tasks

| id | task | state | owner | updated | notes / output |
|---|---|---|---|---|---|
| — | _empty — awaiting first `product` pass over the strategy doc_ | — | — | 2026-07-26 | Fresh start. See PROGRESS.md entry for 2026-07-26. |
| M1 | Phase 1 marketing & acquisition plan (grassroots, zero-spend, Tel Aviv) | in-progress | marketing | 2026-07-28 | `marketing/phase-1/marketing-acquisition-plan.md`. **REJECTED by `product`** (PROGRESS.md, commit `e5339ea`) — back to `in-progress(marketing)` per rejection-loop rule. Blocking findings F1–F3: (F1) the "2–8 day" reopen window silently answers strategy's open "within a week" question with no [ASSUMPTION] label; (F2) "unprompted" is defined against a factually wrong claim that V1 ships no notifications — decision #24 puts a local notification in V1, so a notification-tap reopen would wrongly count as unprompted, and tracking needs a launch-source field (cold launch vs. notification tap); (F3) "confirmed by Aviran" zero-budget claim is unsourced (no such decision exists anywhere) and self-contradicts the plan's own ask for Aviran to approve QR-card print costs. Should-fix, non-blocking: restate the 20%/30-install gate as existence/repeatability (20% of 30 is only 6 people — too noisy for a rate reading), remove or escalate the unauthorized native share-sheet "invite a friend" idea (no strategy line permits in-app share/invite, adjacent to no-social-features), fix the plan's stale "Phase 4"/"friends & family beta" Open-questions residual (role-file itself already fixed in `8b19a5e`). Full verdict in PROGRESS.md. |
| T-030 | Scope live-events ingestion pipeline — new V1 launch-blocking dependency (Linear `PAS-5`) | done | data-engineer | 2026-07-30 | **Closed by `chief-of-staff` — deliverable complete.** Full writeup: `passenger-brain/data-eng/live-events-feasibility.md`. Sourcing options assessed: Eventbrite/Ticketmaster (real APIs, good freshness, skews commercial/ticketed), PredictHQ-style aggregator (Israel coverage/pricing **[ASSUMPTION]**, unverified), Facebook Events (very likely dead as a feed), local Israeli ticketing/nightlife platforms (most likely home for the real informal-event layer, API access **unconfirmed — single biggest open gap**), municipal calendars (scraping, official-only), manual/crowd curation (tied to decisions #22/#24's known tradeoffs). Cost: ~1-2 weeks first feed integration (gated on places/Hoods schema), ~1-2 weeks/additional source, ~1-2 weeks ranking layer; ~3-5 weeks total for a thin single-feed v1. **Explicit call: Conditional** — thin ticketed-only version buildable in Phase 1; the fuller "feels alive" version is a data-access gap (not engineering-hours), not buildable in Phase 1 with confidence. Recommends shipping thin for V1, flagging the commercial skew, treating the informal-events gap as fast-follow. **Aviran-facing follow-on (thin-version acceptability, real launch-date risk if not) moved to PAS-6 item 10** so it doesn't go stale inside a closed ticket — same pattern as PAS-7's Scenic Walk/TikTok follow-ons. Dispatch note: this task raced against a second, unidentified "chief-of-staff"-labeled relay that asked for the same dispatch around the same moment; `main` had already spawned one before the second was actioned, that one was stood down, only one agent ever ran — see PROGRESS.md for the full near-collision record. |
| PAS-6 | Confirm 2026-07-30 founders-meeting V1 scope ambiguities | blocked-on-aviran | — | 2026-07-30 | **Corrected — was wrongly reported as "nine items, none resolved" earlier this run; caught by `product` during the PAS-9 pass, verified directly against strategy.md/decisions.md.** Six of the original seven are RESOLVED, decided live by Aviran the same day (decisions #37-42): (1) tourist-trap boolean fully replaces the 3-tag system, (2) closed-place save allowed with distinct badge, (3) softer copy landed on "tourist-heavy spot", (4) "Profile tab" naming confirmed as gate exception, (5) per-Hood Local status replaces the 7-tier ladder outright, (6) quick filters stay sheet-internal. **Genuinely still open:** (7) Google Maps import — already known explore-only, not a build blocker either way; **(8) Scenic Walk — ship the lighter locked version now, or slip V1 for weighted routing?**; **(9) TikTok import — ToS/legal sign-off needed before a TRD.**; **(10) Live events — thin ticketed-only version acceptable for V1, or does its commercial skew mean the launch-blocking risk is real (PAS-5's data-engineer scoping, 2026-07-30)?** Net effect: the three PRDs held pending this ticket (tourist-trap/localness pipeline, Places incl. closed-place, Passport) are now unblocked — dispatched `product` to write them. Full detail: `strategy/passenger-strategy.md` Open questions, `strategy/decisions.md` #27-42, Linear PAS-6 comments. **Items 11-16 added (2026-07-31)** from `product`'s data-requirements PRD sweep: who authors/how many rows for the places dataset; the Local-status threshold's dataset-sizing consequence; whether `place_type` is internal-only or user-facing; whether anything's scheduled to produce the first tourist-trap flag value; OSM/municipal licence terms; a TikTok-import schema collision (extracted places have no `id` to save against in the curated store, one fix path reopens the no-user-write-into-curated-data rule strategy already set). |
| PAS-7 | Scope Scenic Walk weighted-routing + TikTok import feasibility | done | data-engineer | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Full writeup: `passenger-brain/data-eng/scenic-walk-tiktok-feasibility.md`. Calls: (1) Scenic Walk weighted routing — **not buildable in Phase 1 window**; recommend shipping the already-locked lighter polyline-comparison version instead. (2) TikTok place extraction — **buildable in Phase 1 window** (~2-4 weeks); ToS/access blocker flagged. (3) Google Maps saved-list import — feasibility read only, per decision #35, not scoped as a build. Both Aviran-facing follow-on decisions moved to PAS-6 (items 8-9) so they don't go stale inside a closed ticket. |
| PAS-8 | Reconcile UX docs (`ux-flows.md`, `map-rendering-spec.md`) against 2026-07-30 scope lock | done | designer | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Reconciled: Hoods terminology, Been-place Passport stickers, Saved-vs-Been distinction, Passport promoted from Phase-2-preview (Journey 7) into real V1 scope, category rename. **Two small residuals flagged for a future docs pass (not blockers):** `map-rendering-spec.md` §3 renders "Tourist-heavy spot" at a Hood centroid — adjectival mismatch (Hood is a polygon, not a "spot"), one-word fix; `ux-flows.md:176` still has stale "not flagged a tourist trap" phrasing predating decision #37. |
| PAS-9 | Revise "tourist trap" copy in Phase 1 marketing plan | done | product | 2026-07-30 | **ACCEPTED by `product`** (verified diff `5e23ef2` directly, not just the worklog summary) — term satisfies decision #36's ask, scope discipline intact, more honest post-decision-#37 since the underlying flag is now a plain boolean. Product also struck a now-stale note in `prds/hood-place-detail/hood-place-detail.md` that told designer to wait on this term (decision #42 logged). Closed by `chief-of-staff`. |
| PAS-10 | Draft V1 PRDs for confirmed 2026-07-30 scope items | done | product | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Four PRDs written (T-031…T-034 below). **Update: of the five originally-held items, three are now unblocked** (tourist-trap/localness pipeline, Places incl. closed-place edge case, Passport — PAS-6's items 1/2/4/5/6 all resolved, see PAS-6 row) — dispatching `product` for these next. Scenic Walk and TikTok import remain genuinely blocked (PAS-6 items 8-9, Aviran's call). Google Maps import stays excluded (decision #35). |
| T-031 | Map — Hoods & heat area (PRD) | done | — | 2026-07-31 | **DONE — accepted by Aviran directly in live chief-of-staff chat, 2026-07-31 ("accept T-031"; PROGRESS.md FOUNDER-DIRECT STUB).** Linear `PAS-12` moved `In Progress` → `Done`, `gate:awaiting-aviran-review` removed, acceptance comment posted. **First feature in this project to reach `done`.** Two named gaps carried forward, not waived — still documented, not resolved: real Hood geometry (tracked at `T-040`/`PAS-17`, `build`-ready) and heat never observed rendering against live data (migrations unapplied; becomes a real verification event once Aviran applies them + enables `pg_cron`). Both remain in `prds/map-hoods-heat/map-hoods-heat.md`'s Open questions & risks and Decisions log — PRD text unchanged, still accurately says "Accepted... two named data gaps carried past acceptance." **ACCEPTED by `product`, commit `545bca4` — first feature in this project to reach `aviran-review`.** `product` didn't take the test plan's word on 3 "PASS by construction" bullets — read the actual source (`HeatPalette.swift`, `HoodHitTester.swift`, `HoodCatalog.swift`, `DensityStoreTests.swift`, the bundled fixture) directly. Req 5's zero traced cases confirmed correct, not sloppy (T-031 changes nothing about the selected hour, so those bullets are genuinely T-032's to verify — carry this into T-032's dispatch/acceptance or they fall through the floor). Req 3's non-overlap assumption found genuinely unenforced (`HoodHitTester` relies on it, nothing validates it) — harmless with today's 4 disjoint placeholders, a live risk the moment real polygons land; PRD amended with a pass condition and a fail-loudly validation requirement. Req 4 clean. **One P0 genuinely unmet, recorded not waived:** req 3's "dozens of Hoods, not thousands" — shipped data on both sides is placeholder (4 client rectangles, 5 DB rows); B3 delivered an export script, never a real dataset. Deliberately not rejected back to `build` — authoring real polygons is its own sourcing deliverable with its own quality stakes. **New task created: `T-040`/Linear `PAS-17`** (backlog) — scope + author real Tel Aviv Hood geometry, `data-engineer`'s to pick up. **Reframed 3 of the 5 disclosed gaps as one root cause, not five separate caveats:** no backend has ever been reachable in this whole pipeline, so nobody has actually *seen* heat render — every heat bullet passed by construction (code/test read), never by observation. Presented to Aviran that way in the `aviran-review` summary (Linear `PAS-12` comment) rather than as scattered minors. **Posted the review-ready summary** — what it is, how to try it, the two things worth his attention (apply-migrations-is-the-real-first-observation event; real Hood geometry doesn't exist yet), the low-risk physical-device note, and the fine-to-move-past VoiceOver note. Applied `gate:awaiting-aviran-review`. **New finding worth knowing before this closes (T-040's TRD, 2026-07-31):** the two Hood data artifacts this task actually shipped — the iOS bundled fixture and migration 001's DB seed — already disagree with each other (4 Hoods vs. 5, 3 shared ids with different polygons, incompatible ring-closure formats) because the tool meant to keep them in sync has literally never been run (needs Aviran-gated DB credentials nobody's used). Harmless today (neither artifact is live against real users), and `T-040`'s TRD already fixes the pipeline itself — flagging so Aviran knows this exists in already-accepted code, not a new ask. Waiting on Aviran to move this to `done`. |
| T-032 | Time slider — now → +12h (PRD) | design-approval | product (2nd signature given — both in) | 2026-07-31 | **`product`'s second design-approval signature over the fix pass (`eaa589b`): APPROVE WITH MINORS — both signatures now in, this is ready for `design-review`.** Reviewed fresh against the current artifact and spec, not carried forward from either prior REJECT. **All 5 blocking findings verified fixed at the source, not taken from the fix-pass report.** (1) Re-fetched the live artifact (73.6KB) and recomputed every contrast pair in Python from its own `:root` hex values: `.edge-track-line` is now `var(--fg-faint)` = **3.22:1 light / 3.61:1 dark**, clears the 3:1 bar; the `--surface-3` value it used to ship recomputes to **1.29:1 / 1.28:1**, confirming the defect was real. `fg-muted`/`heat`/`fg` on `surface` recompute to 6.12/7.45, 3.93/6.62, 17.99/14.57 — every figure in §10's table is exact. (2) Opaque backing: `.edge-track` and `.edge-readout` both carry `background: var(--surface)` with `opacity` 0→1 and no partial value, so the `--surface` citations are genuinely valid for what's drawn; `.edge-hint` is now `opacity: 1`, was 0.92. (3) Sheet-open behaviour is the load-bearing one and it holds in code, not just prose: `startEdgeDrag` returns on `state.view !== 'map'` with **no `preventDefault`/`stopPropagation`**, sheet z-index 34 sits above the edge zone's 33, and all **six** `.sheet-edge-margin` regions (2 each on `heatSheet`/`searchStub`/`profileStub`) are wired to `wireDragToClose` against their own sheet — the fall-through is demonstrated, not asserted. §8 item 6(c) correctly translates this to iOS as a *non-engagement* requirement and leaves the recognizer construction to `architect` rather than handing over the DOM z-order as if it were the answer. (4) Fitts's-Law miscite genuinely retired: `design-principles.md:28` states the law as "≥44pt touch targets" flat with no edge exception, and §2/§7 now argue the 24pt zone on its own terms (edge-anchored targets can't be overshot past the bezel; ~708pt correction axis) with the citation column reading "no direct citation — stated departure, pending Aviran's ratification." The ratification question itself is untouched and still Q7's. (5) The persistent-hint contradiction is reconciled internally (§3's "Modal closed" row scoped to the button/modal path, new "Edge idle (at rest)" row carrying what actually renders) **without** resolving the PRD tension either direction — no requirement text widened, no hint deleted. All 7 should-fix items also verified in the text, including the founder quote restored to the verbatim "finder" `[sic]` (L-013) and the new **§5c** giving Q3 its own home. **Q3 confirmed still open, still [ASSUMPTION]** — §5c explicitly declines to answer it and instead states what "two sliders" would actually cost (a second store, a rule for what the button path reads, a new answer to req 4), which is exactly the framing the prior pass asked for. **Scope discipline clean:** the fix pass touched 4 files and nothing beyond the 12 dispatched items — PRD diff is the Status line, the scope note, a Q7 addendum, Q8 and one Decisions row (reqs 1–7 byte-identical); `ux-flows.md` gained one *pending-item* sentence rather than adding the hint to the settled enumeration. **Scope gate (Job 0) re-run on inherited work: clean** — `strategy/passenger-strategy.md:35` quoted verbatim in Motivation, no social surface, no onboarding surface (§9 rejects coach-marks outright, and the hint exists precisely because no onboarding surface is permitted), no Phase 2/3 hook. Data/schema rule re-checked, fourth confirmation: no new table, column, endpoint or query parameter. **One product-side edit made here rather than sent back (same posture as v5's req-6 split):** Q8 named the tension and routed it correctly but stated **neither consequence**, so Aviran could have ruled "no permanent chrome" without seeing that dropping the hint ships a founder-requested gesture with nothing permitted to announce it. Added both costs plus an explicit `product` recommendation (**keep it, and widen the Description line**) — the question stays open and stays his. **Three minors, none blocking, none worth a third round:** (a) the mockup's `.edge-zone` CSS comment still frames 24px as "half of the 44pt Fitts's-Law minimum" without the departure flag the spec now carries — every other blocking fix got its artifact comment updated in this same pass, so this one is inconsistent rather than wrong; (b) `.now-tick-label` and `.track-min-max` comments still cite the superseded 6.24:1 while `.edge-stop`'s comment correctly says 6.12:1 — the artifact now disagrees with itself and with §10, no accessibility consequence either way; (c) the PRD is **5,256 words** against a ~1,200 ceiling, up another ~1,050 at a pass that changed no requirements — `product`'s own debt, compressible by the Q1/Q2/Q4/Q5 branches once Q3/Q7/Q8 land, deferred once already at v5 and named here so it doesn't get deferred silently a second time. **Next gate: `design-review` — Aviran's sign-off alone clears it (2026-07-30 change; Serge deferred to post-code), with Q3, Q7 and Q8 all riding in as his calls.** **Prior entry preserved below.** **`designer`'s consolidated fix pass landed** (commit `eaa589b`, verified directly against the diff, not the summary) — all 5 blocking + 7 should-fix items from the reconciled dispatch genuinely fixed: `.edge-track-line` moved off the exempt-only `--surface-3` (1.29:1/1.28:1, failing) onto `--fg-faint` (3.22:1/3.61:1, passing, existing token); the opaque-backing statement for the active track/chip added to match what the mockup already built, and the idle hint's measured 0.92 opacity corrected to 1.0 to match its own "never translucent" claim; the sheet-open state now states both halves (zone doesn't claim the touch at all, so it falls through to the sheet's own drag-to-dismiss) and demonstrates it with a real `.sheet-edge-margin` hit-region wired to each sheet; the Fitts's-Law miscite replaced with the actual edge-anchored-target argument, citation column now honestly reads "no direct citation — stated departure, pending ratification"; the persistent-hint contradiction reconciled internally and escalated as new **Q8** (PRD now Draft v6) rather than silently resolved either direction — routed to Aviran at `design-review` alongside Q7, `ux-flows.md`'s chrome enumeration updated to name it as pending. Should-fixes: traceability table now covers both paths, clamp rule stated, hint given a visibility figure (5pt×56pt), the missing discoverability sentence added (Q1's supplement is the real de-risking), stale version header fixed, founder quote corrected to verbatim "finder" + `[sic]` (was silently normalized), and Q3 given its own **§5c** with the stray "§5b" cross-reference fixed. **One small correction, not worth a re-dispatch:** designer's own report said the next gate is "design-review (Serge + Aviran)" — per the 2026-07-30 gate change, Serge's sign-off is deferred to post-code now; Aviran's approval alone clears `design-review`. Noting the correction here rather than sending this back over a summary wording slip that touches no artifact. **`chief-of-staff`'s design-approval half: APPROVE.** Checked the diff directly against all 12 items in the consolidated dispatch — every one addressed, none silently dropped, and the two Aviran-owned items (Q7, Q8) correctly left open rather than resolved by designer/product. Could not independently reload the live mockup in this environment (same auth-wall gap as the prior pass) — relying on designer's specific, re-verified numbers and its stated re-fetch-after-publish check, same posture as last time. **Dispatching `product` now for the second required signature** (relay via `main`, one dispatch only). Once both land, this goes to `design-review` — Aviran's sign-off alone, with the mockup link, a Components list, and Q3/Q7/Q8 named as what needs his ruling. **Carried, unclaimed:** `T-045` (uncommitted agent-mirror files) still `project-manager`'s. **Prior entry preserved below — the reconciliation record.** `chief-of-staff`: reconciled both independent REJECT passes (`7cdedb8`, `0dd3d21`) into one consolidated dispatch to `designer` — confirmed by direct diff that `0dd3d21` only touched req 6's per-path split + new Q7, so exactly 1 of the 4 blocking findings below overlaps it; 3 blocking, all 6 should-fix, and 1 more should-fix unique to the concurrent pass (Q3's dead cross-reference to "§5b") are still fully open. Full consolidated list in `PROGRESS.md`'s reconciliation entry. Only one relay sent — not two — per L-007/the T-030 precedent.** **`product`'s design-approval half (2nd signature): REQUEST CHANGES — 4 blocking, 6 should-fix. Back to `design` per the rejection-loop rule.** `chief-of-staff`'s APPROVE is not contradicted — none of these findings were in what it checked (it verified the claims the spec makes; these are comparisons of the spec against the PRD, against `design-principles.md`, and against its own preserved text), which is what a two-signature gate is for. **CONCURRENCY (L-007): a second `product`-labelled session ran this same review in parallel and reached the same verdict independently.** Its PRD **v5** amendment was sitting uncommitted in the shared tree when I read it — I did **not** stage or edit `prds/time-slider/time-slider.md` (safety rule 2, its in-flight file), and its v5 work stands: req 6's touch-target bullet split per path, new **Q7** routing the sub-44pt departure to Aviran at `design-review`, and a blocking contrast defect on the edge track. **Two sessions, one dispatch — `chief-of-staff` should confirm only one relay goes back to `designer`.** Scope gate re-run on inherited work (Job 0 item 4): **clean** — strategy's authorizing line verified verbatim at `strategy/passenger-strategy.md:35`, no social surface, no onboarding surface (§9 explicitly rejects coach-marks), no Phase 2/3 hooks. Q3 verified still open, still **[ASSUMPTION]**, untouched — read against the text, not the claim. **No requirement text changed at v4 — verified by `git diff b4baa33 38c2fd6`, not by reading the header's claim** (Status line, Scope note, Technical design's open-question (b), Open questions and Decisions log are the whole diff; reqs 1–7 bullets are byte-identical). **B1 — contrast on the two new edge elements is computed against a background they do not sit on.** §4/§10 price the edge active track's stop-dot/fill and the readout chip against `--surface` (6.12:1 / 7.45:1, 3.93:1 / 6.62:1) — valid inside the modal card, but both edge elements are drawn **over live map**, and §2 gives an opaque backing to the **idle hint only** (line 98), never to the active track or the chip. The spec's own T-031 D1 reasoning says contrast against unknown map content isn't a checkable number, so these figures don't support the bar they're cited for. Compounding it: `designer`'s own verification records the idle hint at **opacity 0.92**, which is translucent-over-live-map — the thing §2 says it must never be. Independently reached here and by the concurrent pass. **B2 — req 7's sheet bullet is half-covered.** The PRD requires two things while a sheet is up: the edge slide is unavailable **and** "an edge drag over a presented sheet moves the sheet, as it does today." §1.2 and §3's "Edge unavailable — sheet presented" state only the first — "does nothing" — and never say the touch reaches the sheet; the mockup check only proved `state.hour` doesn't move. A build reading "does nothing" as "the zone captures and no-ops" regresses `HoodStubSheet`'s already-shipped drag-to-dismiss inside two 24pt bands. This is the exact collision Q6 exists to resolve, so leaving the winner unstated defeats the call. **B3 — req 6's "touch target ≥44pt" vs. the 24pt capture zone.** `design-principles.md` §2 states ≥44pt flat, no edge exception; §7 cites that same principle as authority for half its number, and both justifications given (beat MapKit's pan recognizer, minimize the unpannable band) are architectural, never about the finger landing on it. The spec scoped req 1's reachability bullet and req 6's rail exemption per path with real care and didn't do it here. **PRD side already fixed by the concurrent pass (v5 split + Q7); the design-side action stands** — state the departure and the actual argument for it (an edge-anchored target can't be overshot past the bezel; the gesture axis is ~708pt) so Aviran has something to ratify at `design-review`. **B4 — the persistent idle hint contradicts the spec's own preserved text and the PRD's Description.** §2/§1.2 introduce "a minimal, persistent affordance at rest on each live edge"; §3's preserved v1 row still reads "no persistent chrome for it," and the PRD Description says "The edge control is not permanent chrome; it comes with the slide and goes again." `ux-flows.md:314` enumerates always-on map chrome and `:83` defines the two chrome classes T-032's own v1 REJECT pass centralized — the hint belongs in both and is in neither. `product` deliberately did **not** pre-emptively widen the PRD Description to make this pass: amending "not permanent chrome" to permit permanent chrome so a design clears the gate is adjacent to cutting a requirement to pass, which is Aviran's call. **Should-fix (6):** §5's traceability table claims full coverage but only P0-6/P0-7 were revisited per path — P0-1…P0-5 rows still map to button-path-only v1 sections (substantively covered in §1.2/§2/§3's edge rows, so the table is stale rather than the coverage missing); P0-1's "no elastic overshoot" has no stated clamp rule where absolute-position mapping meets the 64pt/40pt insets, exactly where a finger will travel; the idle hint is the whole discoverability answer and the one new element with no visibility figure; the discoverability argument needs its missing sentence — the hint makes the control noticeable, not legible, and that's acceptable only *because* Q1 landed on supplement, which is the real de-risking, not B=MAT alone; the spec header still reads "PRD: (Draft v3)" though this pass drove the PRD to v4/v5; §5a silently normalizes the founder's verbatim quote ("finder" → "finger") in a project that has L-013 because quotes drift. **Named, not blocking:** Q3's cost is understated — the design is *structurally* one shared value (one position-to-hour function, one write target), so "two independent sliders" is not a tweak to it, and Aviran should see that framing at `design-review`; the PRD grew 3,435 → 4,199 words at a pass that closed two questions and changed no requirements, against `product`'s own v3 prediction that it would shrink. **Prior entry preserved below:** **`designer`'s additive design pass delivered — PRD now Draft v4, design spec now v2, same mockup URL extended in place.** Resolved: **Q5** — button/modal stays horizontal, unchanged (reasoning: founder's correction was about the edge control only; form-factor mismatch; different physical interaction moments; changing axis would lose req 6's platform-drawn rail exemption for no PRD-driven reason). **Q2** — concrete per-idiom answer given: both edges live on iPhone, left-only on iPad (right is Slide Over); usable vertical band 64pt top / 40pt bottom inset (708pt usable on an 812pt reference device, ~59pt/stop, clears 44pt Fitts with margin); MapKit-pan-arbitration capture band priced at 24pt wide per live edge. **Q6** — `product`'s existing call (edge gesture dead while any sheet is presented) formally adopted with reasoning re-confirmed, one alternative (partial edge availability scoped to sheet footprint) flagged and rejected. **Q3 stays open and [ASSUMPTION]** — not re-derived, not resolved, Aviran's call, demonstrated (not asserted) as one shared value in the mockup pending his answer. Mockup extended in the same artifact (not a fresh one): edge-drag pointer handlers write through the exact same `setHour()` the horizontal track uses (verified live in a browser session, not just reasoned about — dragging the edge then opening the heat modal reads back the identical hour); sheet-open suppression and iPad-idiom right-edge disablement both verified functionally, not just visually; a toggleable reviewer-only overlay renders the real 24pt capture band over the map. **One real bug found and fixed during verification** (not shipped, caught before publish): the edge-readout's position was computed against the phone frame's outer rect instead of `#mapArea`'s own rect, which would have rendered it ~10px off from the finger. **One contrast discrepancy found and corrected, non-blocking:** the v1 spec's `fg-muted`-on-`surface` citation (6.24:1 light) recomputes to 6.12:1 — both clear 4.5:1, fixed in the markdown per the Artifact Conformance rule rather than left. Full detail: `design/phase-1/time-slider-design.md` §5a/§5b/§9/§10. **`product`'s design-approval half: REJECT → back to `design`** (2026-07-31, PRD now Draft v5). **I could reach the mockup** — the artifact URL resolves fine from this session (`WebFetch`, 67KB of published source read directly), so the gap `chief-of-staff` flagged below is an auth-context difference, not a dead link; every claim below is checked against that source, not against the worklog. **Blocking finding 1 — the new edge track's inactive rail misses the bar the spec asserts for it.** Design spec §4/§7 state the custom-drawn edge rail is "held to the full 3:1 bar, not exempted" (correctly — PRD req 6 makes the exemption conditional on a platform-drawn control). The published mockup draws `.edge-track-line` as `background: var(--surface-3)` — byte-identical to the horizontal `.track-line` that only clears review *because* it's exempt. Recomputed from the artifact's own `:root` hex values: **1.29:1 light / 1.28:1 dark**, against a 3:1 bar. Those are the exact two numbers already in this PRD's own Decisions log for the horizontal rail. §4 enumerated the stop-dot (6.12:1/7.45:1) and the fill (3.93:1/6.62:1), both of which pass, and called that the rail — the same enumeration failure the v1 pass was rejected for. Suggested fix, computed not guessed: `--fg-faint` gives 3.22:1 light / 3.61:1 dark, clearing 3:1 in both themes without a new token. **Blocking finding 2 — the 24pt edge capture zone vs. req 6's ≥44pt floor is never reconciled.** §7 cites Fitts's Law to justify 24pt, but `design-principles.md` §2 states that law *as* "≥44pt touch targets," no edge exception; the 24pt is argued from MapKit pan arbitration and claimed map surface — architectural reasons, not accessibility ones. §7 also frames it as "the same drawn-smaller-than-target pattern" as the thumb (28pt visible / 44pt frame), but the thumb's *hit* frame meets 44pt and the zone's does not — the one number req 6 measures is the one that differs. Not fixed by me and not waved: lowering an accessibility floor is an escalation. **My own half of that, done this pass:** req 6's touch-target bullet had never been re-scoped after v2/v3 added a second control, so no gate had an objective pass condition for the zone (L-009) — split per path in the PRD, and the departure routed to Aviran as new **Q7** for `design-review`. Designer states the real argument (an edge-anchored target can't be overshot past the bezel; the ~688–708pt gesture axis is far above the floor); Aviran ratifies or the zone widens. **Should-fix, non-blocking:** §1's Q3 note points the reader to "§5b," which is Q6 — there is no Q3 section; Q3 is nowhere in §8's open items either, so the one question that is genuinely Aviran's could reach his gate only via the PRD. **Verified good, not taken on faith:** all 7 reqs have a §5 traceability row; `setHour()` really is the single writer both paths call (so Q3's one-shared-value is structural, not two synced copies); `hourFromClientY` is one shared function with `1 - (clientY-top)/height`, so up=later is identical on both edges by construction; `startEdgeDrag` gates on `state.view !== 'map'` (Q6) and `edgeLive(side)` (Q2) in code, not just CSS; absolute-position mapping (not delta) really does make every stop reachable from any touch-down point; the readout is positioned against `#mapArea`'s rect, i.e. the ~10px bug the designer reported finding is genuinely fixed; edge elements are `aria-hidden`, matching §4's inert-to-AT claim; `fg-muted`/`heat` on `surface` recompute to 6.12:1 and 3.93:1, matching the spec — the designer's numbers are trustworthy across the board, which makes the rail an enumeration gap rather than sloppiness. **Scope gate re-run, clean:** strategy line 35 quoted verbatim and matches the PRD's Motivation word-for-word; no requirement text changed at v4; no social/onboarding/Phase-2 surface — the edge idle hint is explicitly the *answer* to discoverability without an onboarding surface, which honors the prohibition rather than working around it. L-013 check: re-read both FOUNDER-DIRECT STUBs, the verbatim quotes are carried faithfully and the "one continuous motion" reading is still correctly labelled inference. No new data/schema need (third confirmation). **Not both signatures — this does not advance to `design-review`.** Once `designer` fixes findings 1–2 it needs a fresh `design-approval` pass. **`chief-of-staff`'s own design-approval half: APPROVE.** Reviewed the PRD v4 traceability table (§5, all 7 reqs mapped) and the design spec directly, not just the worklog summary. Checked the Q2 arithmetic by hand: 812pt reference height − 64pt top − 40pt bottom = 708pt usable ÷ 12 intervals ≈ 59pt/stop, clears the 44pt Fitts minimum with real margin — correct. Confirmed the rail-exemption logic is applied in both directions (kept on the still-native button, correctly dropped on the new custom-drawn edge track) rather than defaulted one way for convenience. Confirmed scope discipline held: no PRD requirement *text* changed at v4 (only Open questions/Decisions log, as the PRD's own header states) and Q3 was correctly left untouched, still [ASSUMPTION], still Aviran's. **Could not independently re-verify the live mockup in this environment** — the artifact URL 404s/redirects to a sign-in wall from this session's browser tool (different auth context than whichever account published it) — so I'm relying on `designer`'s own specific, falsifiable verification claims (exact pixel/contrast numbers, named test steps: drag-then-reread, sheet-suppression no-op, iPad right-edge disablement) rather than treating it as independently confirmed by me. Flagging that gap plainly rather than silently signing off as if I'd clicked through it myself. **Dispatching `product` now for the second required signature** (relay via `main`) — design-approval needs both. Once both are in, this goes to `design-review` (Aviran's sign-off alone, per the 2026-07-30 gate change) with the mockup link + a Components list. **Nav-row D6 (separate icons, side-by-side) stands from the prior entry below, unaffected by this pass** — different control, not re-litigated. **Carried forward, not re-derived:** `product` confirmed no new data/schema needed (v2, v3); `T-045` (uncommitted agent-mirror files) still `project-manager`'s, still refused to stage. Linear `PAS-15` not yet updated this pass (designer has no Linear write access — `chief-of-staff`'s to post). **Prior entry, preserved below:** **PRD at Draft v3** (`b4baa33`, verified against diff). Req 7 rewritten around the corrected single-continuous-motion gesture: touch down on either edge, one up/down slide moves the hour from first contact — no reveal step, no second gesture. **Q1 (supplement) and Q4 (one continuous motion) closed in text**, each carrying Aviran's verbatim quote with the "continuous motion" reading labelled as inference, not his words. **Q3 (one value vs. two) stays open and [ASSUMPTION]** — never re-confirmed. **Scope materially shrank:** v2's "design spec and TRD wholly superseded" claim is **falsified** by supplement — the delivered `ZStack` card, nav-row anchoring and 3 exit paths are the *primary* path and stand as-is. This is now an **additive** design pass (add the edge gesture, state its axis) and a TRD **patch** (§2.3/D4 re-check only), not a re-issue. **Q2 narrowed by `architect`'s feasibility read** (`f2920fe`, verified directly against `passenger-code` — no `NavigationStack` anywhere, `grep`-confirmed; iPad is a real target, `TARGETED_DEVICE_FAMILY` confirmed): the left-edge/OS-back-gesture worry **doesn't apply** (no nav stack exists, and the gesture is vertical/off-axis from that recognizer anyway) — keep both edges, no right-edge-only fallback needed. Real constraints for `designer` instead: iPad's right edge is the OS Slide Over gesture (per-idiom answer needed); top/bottom inset squeeze on the vertical track; a live conflict with the already-shipped `HoodStubSheet`'s own drag-to-dismiss; MapKit's documented gesture-swallowing bug (FB19394663) claiming a band near both edges. None launch-blocking given supplement guarantees a fallback everywhere the gesture doesn't work. **Two new items surfaced at v3, both within `designer`'s own remit to resolve, not Aviran's:** **Q5** — does the heat-button/modal control stay horizontal (as already shipped/approved) or go vertical to match the edge gesture? Undecided, `designer`'s call, must be stated explicitly in the new spec. **Q6** — `product` made a working call (edge gesture unavailable while any sheet is presented; heat button covers that state) in response to `architect`'s request, explicitly flagged as product's own call and cheap to overturn — carrying it into design as the working assumption rather than re-asking Aviran, since it's within product's normal decision space and low-risk. **Dispatched `designer`** to produce the additive design spec: state the edge-gesture's visual/interaction spec against Q2's 4 real constraints, decide Q5, adopt Q6 as a stated (revisable) assumption, and note Q3's one-shared-value assumption in the spec too. Then `design-approval` → `design-review` (still required — even an additive change is code Aviran hasn't seen) → TRD patch → `trd-review`. **Nav-row question — ANSWERED 2026-07-31, applied.** Aviran reconfirmed live (relayed via `chief-of-staff`): "yes, separate icons, same switching behavior." Applied by `architect` as a visual-only TRD revision — `prds/time-slider/TRD.md` §2.3 + new §8 **D6** (icons are separate independent side-by-side buttons; `MapNavRow` draws no shared bar/capsule/segmented chrome), C2 build step updated. **No behaviour touched** — modal exclusivity, `NavSurface`, the z-order table, the z4 never-covered guarantee and D2 all stand; `design/ux-flows.md` §2.1 was **not** edited. One **[ASSUMPTION]** left for `designer`: per-icon treatment (shape/material/spacing) is pinned nowhere in text, so D6 defaults it to the `NearMeButton` chrome idiom — cheap to overturn, one file. **This does not unblock T-032** — the in-flight additive design pass (edge gesture, Q2/Q3/Q5) and the §2.3/D4 TRD patch behind it are unaffected and still own this task's state. **Carried, not re-litigated:** `product` confirmed no new data/schema needed (twice, v2 and v3); `T-045` (uncommitted agent-mirror files) filed for `project-manager`. Linear `PAS-15` updated throughout this whole thread; full verbatim trail in `PROGRESS.md`'s FOUNDER-DIRECT STUBs and the `product`/`architect` worklog entries dated 2026-07-31. |
| T-033 | Hood & place detail (PRD) | trd | **`build`-ready — hold cleared, all amendments landed** (architect, 2026-07-31) | 2026-07-31 | **BUILD-PHASE-1 HOLD CLEARED 2026-07-31 — `architect` amendment landed, TRD now matches product's PRD-level call with no gap.** §3.4.1 (new): the pin is a runtime constant `BuildPhase.seedIsAuthoritative = true`, not `#if` (both branches stay compiled and reviewable) and not the absence of a plist (machine-dependent — no `SupabaseConfig.plist` exists in `passenger-code/` today, and relying on that makes the demo's data source a property of the build machine). Determinism written as **three pass/fail `PlaceCatalogTests` assertions**: `source == .seed` after `load()`; **zero** fetch attempts, proven by a `PlacesFetching` spy injected through the shipped `DensityStore.init(api:cache:now:)` idiom; and `.seed` still holds **with a populated plist present** — that third one is what distinguishes a pin from an accidental fall-through. §4.3 lead-in + §7: the live path (the `GET`, the `hoodID`-stamping decoder, the two live-only boundary rules, `PlacesCache`'s disk round trip, and the `.live`/`.cache`/`.unavailable` half of §4.8's error-vs-empty distinction) ships **built and unexercised**; Phase-1 acceptance covers reqs 1–7 against the fixture and covers none of that — named up front so it isn't retroactively re-read as intentional the way T-031's "heat never observed live" had to be. §7: Phase 1 → Phase 2 is one boolean, and the pin is explicitly *not* the feature flag §7 rules out. §11: **C3 extended** (constant + injection seam + the three assertions), **new C3a** hand-authors `places-tel-aviv.json` to the pass/fail floor (real Tel Aviv names, both categories, coordinates inside the placeholder rectangle each `hood_id` names, ≥8 places / ≥2 Hoods, one Hood with zero places, one with no blurb, provisional in-file — floors are product's **[ASSUMPTION]**), **B2 reconciled across phases** (nothing to drift against until T-042's B3 exists at Phase 2 — dormant, explicitly do not stub it green). New §8 **D10** records the pass with commit refs. **Depends on T-040's C1/C2/C2a carve-out** for the blurb branch — not a build blocker (both branches already specced in §4.8/C7), but a Phase-1 acceptance condition for req 2. **A1/A2 phase call made (chief-of-staff, 2026-07-31, per architect's explicit ask — architect declined to invent it):** A1/A2 are `developer`+`code-reviewer` steps that only confirm T-042's `004`/T-040's `003` migrations landed — both Phase 2, both unapplied. **Explicitly deferred to Phase 2**, not dispatched now and not silently skipped — they'll be picked up alongside T-040/T-042's own Phase-2 dispatch. **Phase-1 build dispatched: `ios-developer`+`ios-code-reviewer` on C1–C12+C3a only** (C1 first, `.presentationBackgroundInteraction` proof gate), sequenced after T-040's own C1(whole)+C2a (same pair, dependency: T-033 needs T-040's `schemaVersion` 2 blurb decode to exist first). **Prior record below.** **TRD delivered** (`2455ff4`), then amended (`7e33f21`) once T-042/T-040 claimed the schema it originally self-defined. **Schema ownership resolved cleanly:** §3.1's own migration 003 (places table + hoods.blurb) is struck entirely — T-033 now depends on T-042's and T-040's migrations landing, builds neither. Confirmed no gap: every field T-033 reads (id/name/category/hood_id/lat/lng/updated_at) is present in T-042's superset schema, checked column by column. `cascade`→`restrict` ratified after walking every place-reading path (client is read-only, no delete path exists). **T-033's own review then found 2 more cross-TRD conflicts in T-042's text** (relayed to T-042's reviewers, now being fixed in a consolidated architect pass): the client-field-ownership split (fixture carries T-042's 4 new fields, Swift model gains each only where first read — T-035/036/037/038, not here) and the `places-tel-aviv.json` dual-emitter conflict (T-042's own B3 should own the emitter, T-033 keeps only a drift check). ~~**Still holding at `trd` for the separate nav-row question only** (T-032's row)~~ — **hold CLEARED 2026-07-31.** Aviran's answer ("yes, separate icons, same switching behavior") landed and was applied; **nothing in T-033's own text changed**, because this TRD never described the nav row's visual grouping — its only two nav-row mentions (§4.2 rule 5, §4.2's cross-task contract) are about coverage and exclusivity and read identically either way. Recorded as new §8 **D8** plus a pointer in §4.2 to where the visual call actually lives (`time-slider/TRD.md` §2.3 / §8 D6). Schema-ownership question was already resolved. ~~**T-033 is now ready for `trd-review` with no outstanding holds**~~ — **`trd-review` COMPLETE, 6/6, unanimous APPROVE / APPROVE WITH MINOR NOTES, zero blocking findings** (`72bf8d0`, `dbb6045`, `7694bf4`, `db1a7cb`, `48400bc`). **Consolidation pass landed — all 5 non-blocking findings applied in one amendment**, recorded as new §8 **D9** with per-finding reviewer attribution: (1) §8 D7 + §11 B2 rewritten to point at their resolutions (`places-dataset/TRD.md` §2.1 D5/D4) instead of still posing them as open — plus the two downstream restatements that would have gone stale with them (§3.1's consequences row, §11's closing ratification paragraph); (2) §4.1 now declares `isDepth1Presented`/`isDepth2Presented` as two-way `Binding<Bool>` with setters routed to `closeHood()`/`closePlace()`, so swipe-to-dismiss can't desync the router from the screen, and C5's tests extended to that path; (3) §4.3's boundary table split by data source (the stale-`hood_id` rule is bundled-seed-only) and the previously-unstated decode rule added — the live path's decoder stamps each place's `hoodID` from its enclosing Hood, since the `select` deliberately omits the column (guessing wrong = silent Hood misattribution, not a crash); (4) §4.7 classifies `HoodButton` as T-032 bucket-2 chrome, which resolves that §4.2's cross-task rule has no reachable trigger while a nav surface is up — kept as a stated invariant, **stated from T-033's side only, `time-slider/TRD.md` deliberately untouched** (mid-design-pass); (5) §4.8's reserved T-035 flag line clarified as a genuinely empty slot so C8 doesn't fabricate a placeholder `isTouristTrap` or hardcode always-absent as a decision. **One consequent retag, and it's the reviewers' own:** §11 B2 drops `[Algo/Data]` → `[iOS]` only (export owned by T-042's B3; what survives is a `HoodCatalogTests`-shaped drift check — both B2 reviewers said so and `data-engineer` explicitly asked to have the ownership settled before build). **Build-phase tension above is NOT resolved by this pass and was deliberately not acted on** — it's a scoping call, not a `trd-review` finding; D9's closing note records it with two evidence points (§3.4's live→cache→**seed**→empty precedence and §7's ship-without-backend statement mean a no-backend path is already designed in; a Phase-1 build needs a which-source-is-authoritative decision plus a real fixture, touching §3.4/§4.3/§7 and C3 only). Dispatch pairs when it does go to `build`: `ios-developer` + `ios-code-reviewer` (C1–C12, C1 first as the `.presentationBackgroundInteraction` proof gate), `developer` + `code-reviewer` (A1–A2, verification steps that can't close until T-040's `003` and T-042's `004` are actually built). **Other board writes from this TRD, actioned:** `T-041` (pin clustering), `T-042` (place dataset — now has its real PRD/TRD), `T-036`'s stale dependency (fixed), `D3`'s Waze question (non-blocking, product's). **Build-Phase-1 scoping call now RESOLVED by `product` (2026-07-31, `30e002f`):** bundled seed is authoritative for Phase 1, the live Supabase fetch stays **built but inert** (not deleted — deleting it would force a Phase-2 re-architecture instead of a source-precedence pin). Phase-1 build must land on `PlaceCatalog.source == .seed` deterministically, asserted by a test. Fixture is **two files**, not one: `places-tel-aviv.json` (new — ≥8 places / ≥2 Hoods / one Hood with zero places / one Hood with no blurb / real Tel Aviv names / both categories / coordinates inside the placeholder rectangles) and `hoods-tel-aviv.json` gaining a `blurb` field it doesn't have today. **Chief-of-staff's carve-out call (routed by `product`, decided here 2026-07-31): T-040's C1+C2 only (Hood model gains `blurb`, decode with the existing normalisation) move into Build Phase 1** — pure client decode, zero backend surface, needed so T-033 can demo the blurb branch at all. **T-040's B1/B2/B4/B6 and C3 stay Phase 2** (validator, generator, real geometry authoring, migration `003`, and the swap-to-real-bundle step) — carving those forward would drag the real-geometry long pole into Phase 1, which is exactly what the phase split exists to prevent. This is a build-sequencing decision only, no scope change — labelled **[ASSUMPTION]** on the exact fixture floors (product's own judgment call, not a founder threshold). **Still needed before `build`:** `architect` amends TRD §3.4/§4.3/§7/C3 to state the Phase-1 source pin + determinism test + the two-file fixture spec + the T-040 C1/C2 carve-out dependency — dispatched this run. **BUILD LANDED 2026-07-31, commit `473f325`** (24 files) — verified directly against the diff and the TRD: `BuildPhase.seedIsAuthoritative` pin + all 3 §3.4.1 determinism assertions, the two-way dismiss `Binding<Bool>`, full C1-C12 client, `places-tel-aviv.json` C3a fixture (9 places/3 Hoods, coordinates confirmed inside their bundled rectangles). One real test-only bug found+fixed (`MKMapPoint.distance(to:)` unit confusion, `PlaceHitTesterTests`) — confirmed production code unaffected. 79/79 tests green, 14 suites. One disclosed, accepted deviation: the "zero places" and "no blurb" empty-state demo cases both landed on the same Hood (`lev-hair`, inherited from T-040's own approved choice) rather than two separate Hoods — satisfies the letter, not fixed, reasonable call. `ios-code-reviewer` dispatched next. **VERDICT 2026-07-31: REQUEST CHANGES** (`6d0f157`) — blocking: `HoodSheet.swift` has no ✕ dismiss control (design spec requires 44×44pt on both sheets, retiring a design-approval-REJECTED Fitts's-Law defect; `PlaceDetailModal` has the correct pattern, `HoodSheet` doesn't). 2 should-fix: 3rd §3.4.1 determinism assertion doesn't actually distinguish pin-vs-fallthrough (needs `AppConfig` bundle-injectable, out of diff scope, flagged to `architect`); D4's displayName test missing. **Separately, found independently while processing this review (chief-of-staff):** T-040's real 24-Hood geometry landing (`72f4fc7`, Aviran-authorized, legitimate) retired the placeholder rectangles this task's fixture was built against — ran the actual point-in-polygon math, **6/9 `places-tel-aviv.json` coordinates now fall outside their real Hood's polygon**, and all 24 real Hoods ship `blurb: null` so the req-2 blurb-branch demo is currently unsatisfiable. Fix pass dispatched to `ios-developer`: the ✕ button, the displayName test, repositioning all 9 fixture coordinates into the real polygons (list of the 6 failing ones supplied directly), and a full-suite re-run against current `HEAD` (never tested in this exact combination). Blurb-emptiness is a scoping call, explicitly not `ios-developer`'s to resolve — routed back to chief-of-staff/product once the fix lands. T-033/T-040 do NOT move to `qa` yet. **FIX PASS LANDED 2026-07-31, `307e5af`** — verified independently against diff + re-run point-in-polygon math: `HoodSheet` header/`closeButton` matches `PlaceDetailModal`'s pattern exactly (44×44pt, `xmark.circle.fill`, `router.closeHood()`); `PlaceCategoryTests.swift` asserts both display strings; all 9 `places-tel-aviv.json` coordinates re-confirmed inside their real Hood polygon (was 3/9, now 9/9), diff minimal (~18 lines). **Blurb-branch scoping decision made (chief-of-staff):** extending T-040's own earlier disclosed-placeholder precedent — one `[PROVISIONAL]`-marked blurb hand-added to the real `florentin` Hood (reusing its old placeholder copy), nothing else touched, dispatched to `ios-developer`. `ios-code-reviewer` fast re-review dispatched next on the combined diff (✕ button + D4 test + coordinate reposition + florentin blurb) — this is the explicit last gate before `qa`. |
| T-034 | Live events overlay (PRD) | design | designer | 2026-07-31 | `prds/live-events-overlay/live-events-overlay.md`, now **Draft v2** (`product`, `1c37640`) — supersedes the stale "launch-blocking on PAS-5" framing below. **Corrected, not removed:** the gate is real (`strategy/passenger-strategy.md` line 46, near-verbatim: V1 doesn't ship until the live-events pipeline works) but attaches to the **pipeline** (T-043, Build Phase 3), not to this client. Client has no hard dependency on real data — verified against reqs 3/4/5 directly (pure renderer of whatever set it's handed, including empty; unreachable/empty feed renders normally, no error banner). **Real finding from the correction: the client is unblocked for Phase 1 only if a bundled fake event set exists — it doesn't, and nobody owns it yet** (one fixture file against an already-specced schema, not a new sourcing PRD). **Owner assigned (2026-07-31, chief-of-staff):** folded into T-034's own build scope as a named step for whoever builds this — same pattern as T-033's own fixture (its own C3 step, not a separate task) — rather than spinning up a new task ID for one fixture file. Whoever picks up T-034's build authors a bundled fake event set (real-shaped Tel Aviv event names/times/locations against the same placeholder Hood geometry, quality floor mirroring T-033's: enough events to exercise reqs 1-4/6, not just req 5's empty case) as part of that build, or QA only has req 5 to test against. Components (unchanged): **Event marker layer** — hour-bound, shape-distinct from place pins, clusters neutrally · **Event detail sheet** — name, time, location, route hand-off · **Ranked-subset client** — renders backend rank order, bounded marker count · **Events layer toggle** — sits with the other toggles in the heat modal · **Empty/degraded state** — app fully usable with the layer permanently empty. Depends on T-031 (done), T-032 (design-approval). **Data-requirements sweep (2026-07-31):** the ingestion/ranking pipeline this PRD had fenced out as "not in scope" now has its own real spec, `T-043`/`prds/live-events-pipeline/live-events-pipeline.md`. Linear `PAS-25` — still shows `Backlog`/no owner label in Linear as of this run; correcting this pass. |
| PAS-11 | Phase 2 candidate: "Local personas" (concept, not scoped) | blocked-on-aviran | — | 2026-07-30 | Founder-direct, live hilos chat, 2026-07-30 (PROGRESS.md). New Phase 2 candidate — curated local-guide profiles a user can follow as a discovery lens (favorite places, Hoods, routes). **Not Phase 1, not authorized to build, no PRD.** Recorded in `strategy/passenger-strategy.md` Phase 2 candidates + `strategy/decisions.md` #43. `product`'s concept-level read (not a PRD) found the recording had added an unstated premise ("curated" — flagged [ASSUMPTION], strategy.md Open question #10) and gave recommendations (not resolutions) on the other two tensions — see Linear PAS-11 comments and PROGRESS.md. **Three questions now sit with Aviran, in dependency order** (strategy.md Open questions #8–10, posted in hilos `#general` same date): (1) authoring model, first-party vs. ever user-authored — gates the rest; (2) follow vs. lens framing — `product` recommends lens-only; (3) same concept as Phase 3's "AI local guide" persona, or separate. `aviran-blocker` label applied, no owner while blocked, per the standing PAS-6 pattern. Not a Phase 1 launch blocker — resolve whenever convenient. |

| T-035 | Tourist-trap flag & Local QA (PRD) | design | designer (queued behind T-031 rework) | 2026-07-30 | `prds/tourist-trap-flag/tourist-trap-flag.md` (Draft v1) by `product`, unblocked by decisions #37/#42. One boolean flag per Hood/place, Hood-outline-stroke rendering (already specified, not re-derived — `map-rendering-spec.md` §§2-3,4,7), one text line in the place modal, binary local-QA toast correcting it. Kept as one PRD deliberately — splitting the flag from its correction mechanism would let it ship uncorrected, against the "algorithm plus local QA, together" authorizing line. Depends on T-031 (consumes the Hood-centroid rendering channel T-031's rejection just fixed — see T-031 finding 4). **Not dispatched yet** — queued behind `designer`'s in-flight T-031 rework so the tourist-trap design doesn't get built against a not-yet-fixed map layer. **Data-requirements sweep (2026-07-31):** fixed in place — the flag columns (`is_tourist_trap` etc.) are now given owning PRDs (T-040/T-042), and "nothing is scheduled to produce the first flag value" is promoted to a named risk rather than left implicit (also an open escalation for Aviran — see `PAS-6`). Linear `PAS-26` (backfilled 2026-07-31 reconciliation pass). |
| T-036 | Places — Been & Saved (PRD) | design | designer (queued behind T-031 rework) | 2026-07-31 | `prds/places-been-saved/places-been-saved.md` (Draft v1) by `product`, unblocked by decision #38. One Places list, three provenance states (Saved/Been/Visited) with precedence, permanently-closed badge, binary map ring accent (close zoom only). Depends on T-031. Sequenced before T-037 (Passport consumes its Been signal). Not dispatched yet, same reason as T-035. **Stale dependency line found (T-033's TRD, 2026-07-31), not yet fixed:** `prds/places-been-saved/places-been-saved.md` L86 says "Dependencies: `map-hoods-heat` (pins, ring channel)..." — wrong, T-031 (`map-hoods-heat`) renders Hood polygons only; the place-pin map layer is actually built by T-033 (`hood-place-detail`). PRD's own Dependencies line needs `product` to correct it to `hood-place-detail` for the pin/ring channel — flagging for `product`'s current data-requirements sweep (`places-been-saved.md` is already in its review list) rather than editing PRD content myself. **Data-requirements sweep (2026-07-31):** fixed in place — closed-state refresh (how a place's permanently-closed badge gets updated over time) was implicit; promoted to a named, ownerless job flagged for whoever picks up the underlying data pipeline. Linear `PAS-27` (backfilled 2026-07-31 reconciliation pass). |
| T-037 | Passport (PRD) | design | designer (queued behind T-031 rework) | 2026-07-30 | `prds/passport/passport.md` (Draft v1) by `product`, unblocked by decisions #39/#40. Private per-city sticker album + per-Hood Local status under the Profile tab. Seven-tier ladder excluded outright, not even as a derived/display status. Depends on T-031 and T-036 (consumes Been signal). Not dispatched yet, same reason as T-035/036. **Three real product findings surfaced by `product`, not resolved, flagged for Aviran (see PROGRESS.md and chief-of-staff's direct report):** (1) sticker-shape-matches-place-type (decision #29) needs a `place_type` field the data model doesn't have — either add it (as an internal field, doesn't need to touch the 2-category user-facing split) or V1 ships 2 sticker shapes only; (2) nothing in the current mechanics rewards *answering* local-QA — Passport was pulled forward specifically as that incentive layer, but decision #29 ties stickers to Been-places (visiting) and #40 retired the ladder that would have carried a QA-answer reward — the pipeline's actual named risk goes unaddressed; (3) Passport/Places are device-local (follows from the no-accounts lock) and don't survive a reinstall — nobody has confirmed that losing a sticker album on a new phone is acceptable; the only fix is an anonymous server identity, which is parked to Phase 3. All three are product-shape questions, not blocking PRD/design work, but worth Aviran's read before Passport goes further than a design mockup. **Data-requirements sweep (2026-07-31):** fixed in place — `place_type` and `designated_for_progression` now given owning PRDs (T-042, T-040 respectively). The Local-status threshold's consequence for dataset sizing surfaced explicitly (a threshold tuned for "dozens of Hoods" behaves differently at real scale) — another open escalation for Aviran, see `PAS-6`. Linear `PAS-28` (backfilled 2026-07-31 reconciliation pass). |
| T-039 | Port Scenic Walk / TikTok feasibility into `prds/` PRD-gate location | done | data-engineer | 2026-07-30 | Dispatched to (re-)scope Scenic Walk and TikTok import feasibility; found `PAS-7` already closed this exact ask 2026-07-30 with a full writeup at `data-eng/scenic-walk-tiktok-feasibility.md`. **Did not re-derive the research** — per the doc rule against near-duplicates, ported PAS-7's findings into the location `prds/INDEX.md`'s scope gate actually reads before unblocking `product`: `prds/scenic-walk/feasibility.md` and `prds/tiktok-import/feasibility.md`, each with the feature-scoped recommendation and an explicit go/no-go/slip call. `data-eng/README.md` cross-referenced both ways so the two locations don't drift. **Updated `prds/INDEX.md`'s stale framing** — it said both items were "launch-blocking on `data-engineer`'s feasibility scoping," which PAS-7 had already answered; corrected to say scoping is done and the real remaining blocker is Aviran's own scope/ToS calls, already tracked as `PAS-6` items 8–9. **Calls carried over from PAS-7, not re-derived:** Scenic Walk's weighted-routing version (decision #32's literal text) is not buildable in Phase 1 — recommend shipping the already-locked lighter comparison-polyline version (decision #19) instead, pending Aviran's ship-vs-slip call. TikTok import is technically buildable in Phase 1 (~2–4 weeks, off-the-shelf OCR/ASR/LLM/geocoding, mandatory confirm-before-save), gated on Aviran's ToS/access sign-off before a TRD. Neither PRD should be written until those two Aviran calls land — this task doesn't change that, it only fixes the stale reason given for the hold. |
| T-040 | Hood dataset — Tel Aviv geometry & attributes (PRD) | build | data-engineer + developer + ios-developer (build-ready, not yet dispatched) | 2026-07-31 | **HELD — Build Phase 2 (2026-07-31 replan, see `## V1 Build Phases` above). Not being dispatched to `build` until Phase 2 starts** — this is backfill-shaped real-dataset work, exactly what Phase 2 is for. TRD stays approved, no rework needed while it waits. Linear `PAS-17`. **TRD delivered** (`prds/hood-dataset/TRD.md`, commit `2eac160`). **The existing export pipeline was never runnable by anyone** — `export_hoods_geojson.py` reads DB→bundle, needing an Aviran-gated `DATABASE_URL` no one has ever used. Inverted: a git-tracked source JSON is the only authored file, one generator emits both the DB seed and the iOS bundle with **no database access needed**; old script repurposed as a post-apply drift check. **Direct consequence of it never running, found not assumed:** the two artifacts T-031 already shipped **already disagree** — 4 Hoods vs. 5, 3 shared ids with different polygons, open vs. closed ring format (the export script actually `RAISE`s on the open form). A real defect sitting in already-accepted code, worth Aviran knowing before `aviran-review` closes T-031 — flagged on that row too. Fixed `on conflict do nothing` (would have silently left stale placeholder rows in place forever) → upsert + city-scoped prune. Non-overlap validator redesigned as strict-interior-containment + proper crossing (naive segment-intersection would flag every legitimate shared Hood edge as an overlap). Adopted T-042's `validate_dataset.py` over an earlier Shapely plan — found and returned one correction (clause 4's cost estimate: ~150 lines, not ~30, since non-overlap needs more than the shared ray-cast). **Final schema, stable for T-042/T-043:** `blurb` nullable, `is_tourist_trap` nullable-boolean (3 states), `designated_for_progression` not-null-default-false. **`hood_for_point()` cross-TRD question now resolved** (data-engineer's T-042 review): keep both implementations (Python offline validator here/T-042, SQL runtime function in T-043 — genuinely different runtime/language/data-source, no practical bridge) — require one shared checked-in fixture (inside/outside/on-boundary/concave/swapped-lat-lng/shared-edge cases) both must pass, not unified code. Wording fix dispatched to architect. **Migration `003`** (numbering now settled: 003=this, 004=T-042, 005=T-043). **B1 must be reviewed jointly with T-042's B2 — one file, two authors.** Escalations unchanged, still Aviran's (`PAS-6`): OSM/municipal licence terms, the designated-Hood set + Local threshold, no scheduled tourist-trap proposer. **`trd-review` fully landed, all 3 pairs — APPROVE/APPROVE WITH MINORS, nothing blocks the schema/design itself.** Artifact-disagreement finding independently re-confirmed by `code-reviewer` (read all 3 source files directly: 4 vs 5 Hoods, genuinely different coordinates on shared ids, ring-closure mismatch, script confirmed never run). **One real numbering collision found (`code-reviewer` + `developer`, independently):** T-040 needs 2 migration files — `003` (schema) + a generated data-seed file its own §3.3 names `004_hoods_tel_aviv_data.sql`, colliding with T-042's `004_places.sql`. Resolved: data-seed file renumbers to `006`; folded into the consolidation architect pass already in flight. **All 3 pairs now landed — 6/6 verdicts, unanimous approve, nothing blocks build.** Numbering collision independently confirmed a 3rd time (`developer`); non-overlap validator and schema contract independently re-derived from `tourist-trap-flag.md`/`passport.md` rather than taken on faith; the ~150-line shared-validator estimate independently re-counted and confirmed exactly. `data-engineer` found one new fixture gap (a T-junction/tangent-vertex case, worked by hand but not yet an explicit fixture entry) and a second, independent reason `hood_for_point()` must stay separate from the Python validator: T-043's own architecture deliberately keeps ranking/business logic in SQL specifically so it's retunable without an Edge Function redeploy — moving attribution logic there to "share" it would violate that TRD's own design, on top of the language/runtime problem already known. Concrete build note: shared fixture lives at `database/data/hood_containment_fixture.json`, SQL side transcribes the same cases with a comment pointing back (manual-sync discipline, no CI to enforce it automatically). **Full data-chain `trd-review` now complete: T-040 6/6 approve, T-042 3/3 approve-with-minors, T-043 4/4 (request-changes pending the in-flight security fix). **Consolidation pass landed (`293ddb2`) — all 6 text fixes applied**, including the data-seed renumber (`006_hoods_tel_aviv_data.sql`, avoiding the `004`/T-042 collision) and the C-HOOD-1 clause-4 correction (own §2.3 now states the accurate ~150-line non-overlap estimate, verbatim quote in §4.4 left intact with the correction alongside it — quote and correction coexist rather than one overwriting the other). **`build`-ready.** Not yet dispatched — 3 pairs needed (`data-engineer`+`code-reviewer` B1-B6, `developer`+`code-reviewer` A1/B6-SQL, `ios-developer`+`ios-code-reviewer` C1-C5). **`security-auditor` sweep (2026-07-31): PASS, no findings** (commit `1cb571b`) — this migration defines zero functions, so the revoke-execute bug class (migration 002, T-043) has nowhere to land; new `hoods` columns correctly get no RLS statement (row-scoped, inherit existing policy). Recommends re-running once `003`/`006` exist as generated files, not just TRD-spec'd SQL — before Aviran applies to prod. Closes the security question for T-040/T-042 (T-043 already had its own dedicated pass + fix). **Build-Phase-1 carve-out ratified 2026-07-31 (chief-of-staff, per product's recommendation on T-033's scoping pass):** C1+C2 (Hood.blurb field, schemaVersion 2 decode with `""`→`nil` normalization, hand-authored provisional blurb values for the 4 placeholder Hoods) move to Build Phase 1 — pure client-side decode, zero backend surface, needed because T-033 can't otherwise demo the Hood-blurb half of its own authorizing strategy line (`strategy/passenger-strategy.md:37`) in Phase 1. B1/B2/B4/B6 (real geometry sourcing/authoring, the actual data pipeline) stay Build Phase 2 exactly as already held — pulling those forward would drag B4's real-municipal-geometry work into Phase 1. ~~Architect dispatched to write this into the TRD alongside T-033's own §3.4/§4.3/§7/C3 amendments.~~ **DONE 2026-07-31 — carve-out now written into `prds/hood-dataset/TRD.md`:** new §8 **D10** (the decision, its provenance, and why exactly three steps move and not more), §3.5 (the hand-edited-bundle exception, bounded: `schemaVersion` 1→2 and `blurb` only, geometry untouched, no invented values for `centroid`/`isTouristTrap`/`designatedForProgression` — §4.3's decode rules already fall back for all three), §11 (per-step build-phase column; **C1 carved whole**, not blurb-only, since the other fields are optional-at-decode and splitting the struct across phases costs a second edit of the same file; **new C2a** for the hand-edit, one blurb deliberately `null` so T-033 req 2's no-blurb branch stays observable; C3 named as what retires the hand-edit). **One bounded architect call flagged rather than assumed:** C4's three decode assertions (`schemaVersion` 2 accepted, `blurb` `""`→`nil`, absent `centroid`→averaged fallback) ride forward with C2 in Phase 1 — a decode rule shipped without the test that proves it is a rule nobody checked; C4's non-overlap tripwire stays Phase 2 with the real geometry it exists to check. No requirement, contract, or schema decision changed; D1–D9 and the 6/6 `trd-review` approvals stand — this is a build-order split of already-approved steps. **Phase-1 slice BUILT 2026-07-31, commit `3d94ba6`** — verified directly against diff: `Hood.swift` gains the 3 fields with fallback-documenting comments, `hoods-tel-aviv.json` bumped to `schemaVersion: 2` with geometry byte-identical and 3/4 Hoods `[PROVISIONAL]`-blurbed (`lev-hair` explicit `null`), `_note` field documents the C2a exception and warns of the future C3 overwrite. `xcodebuild build`/`test` both green (41 tests/8 suites + 1 UI test against the real `HoodCatalog.load()`). **`ios-code-reviewer`: APPROVE WITH MINOR NOTES** (`8c7a838`) — schemaVersion gate, blurb trim, and centroid-fallback ordering all independently re-traced against actual code lines (not the report), fixture diff re-read hunk-by-hunk (geometry byte-identical), scope confirmed clean. 2 non-blocking notes: no programmatic guard against a `[PROVISIONAL]` blurb surviving into a real build if Phase 2 slips (only the human-readable `_note` documents it); the malformed-centroid-array-length throw path has no test. **Holding for `qa` jointly with T-033** (reviewer's own call — this slice alone has no UI surface, the real behavioral check is T-033's Hood sheet rendering this bundle's blurbs). |
| T-041 | Own pin clustering (currently fully specified, claimed by no PRD) | backlog | — | 2026-07-31 | **Build Phase 2** (2026-07-31 replan) — irrelevant at Phase 1's handful of fake pins. Surfaced by T-033's TRD. `design/map-rendering-spec.md` §5 already fully specifies clustering behavior (neutral clustering, no personal-place indication inside a cluster) — but no PRD claims ownership of actually building it; T-033's own scope explicitly excludes it ("minimal version" of the place-pin layer). Same shape as T-040 was — a real, specified need sitting unowned. **Not picked up by `product`'s data-requirements sweep** (it's a behavior/ownership gap, not a data/schema need in the sweep's sense) — still open, needs `product` to fold it into an existing PRD's scope or write a short supporting PRD in a future pass. Not launch-blocking with a small pin count, becomes one the moment T-040/T-042's real data lands. Linear `PAS-30` (backfilled 2026-07-31 reconciliation pass). |
| T-042 | Places dataset — Tel Aviv curated places (PRD) | build | developer + data-engineer (build-ready, not yet dispatched) | 2026-07-31 | **HELD — Build Phase 2 (2026-07-31 replan, see `## V1 Build Phases` above). Not being dispatched to `build` until Phase 2 starts** — same reasoning as T-040, real backfill dataset work. TRD stays approved, no rework needed while it waits. **TRD delivered** (`4d126f1`). **All 3 `trd-review` verdicts in: `developer` APPROVE+1 required fix, `data-engineer` ACCEPT+refinement, `code-reviewer` APPROVE with minors — none block this task's own build.** D1 (places owned once, here — T-033 defers) and C-HOOD-1 (4 falsifiable schema/validation clauses) both independently ratified by every reviewer who read T-033's actual struck text, not just the architect's claim. Schema: `place_type` lookup table (not enum, makes Passport req 3 structurally true), `keywords` client-matched `text[]` (forced by 3 search reqs), `hood_id on delete restrict` (deliberate, matches `hood-dataset` req 7), RLS matches migration 001 exactly, migration order-safe. **`hood_for_point()` cross-TRD question resolved** (data-engineer): keep both implementations (offline Python here/T-040, live SQL in T-043 — genuinely different runtime, no bridge possible), require one shared fixture instead of shared code. **2 more self-contradictions found in this TRD's own text, both now resolved per T-033's recommendation:** the Place-client-field-ownership split (fixture carries all 4 new fields, Swift model per-task) and the `places-tel-aviv.json` dual-emitter conflict (§2.2 was simply a stale line against §5/§9's already-correct answer). **Remaining fixes, all folded into one consolidated architect pass** (in flight): pin `004_places.sql` outright, correct the closed-state-refresh framing (unmet Open Question, not "fenced out" — `T-044`/`PAS-22` already owns the gap itself), write the fixture-based `hood_for_point()` resolution into all 3 TRDs' text. B5 (actual dataset) still blocked on Aviran (`PAS-6` item 11) + T-040's real geometry. **Consolidation pass landed (`293ddb2`) — migration pinned to `004_places.sql`, closed-state-refresh framing fixed in both places it appeared (§8 and §10), Place-field split (§2.1 new D5, confirmed the 4th field is `permanently_closed`) and dual-emitter conflict (§2.2 new D4, `export_places.py` sole emitter) both resolved. `data-engineer`'s revised verdict (clause-4 correction) also landed — clause 4 itself unedited (it's T-042's own source text, correctly describing T-042's containment check), the correction lives in T-040's document since that's where the misapplication occurred. **`build`-ready.** Not yet dispatched — 2 pairs needed (`developer`+`code-reviewer` A1-A5, `data-engineer`+`code-reviewer` B1-B5). **`security-auditor` sweep (2026-07-31): PASS, no findings** (commit `1cb571b`) — migration is 2 `create table`+RLS+index, no functions defined (containment/geometry logic deliberately stays in the offline Python validator, T-042 §4.1 clause 3, no PostGIS); RLS clean, matches migration 001's pattern, places↔hoods embedded-fetch join checked for policy-mismatch, none found. Recommends re-running once `004` exists as a generated file before Aviran applies to prod. |
| T-043 | Live events ingestion pipeline (PRD) | build | developer + data-engineer (build-ready, not yet dispatched) | 2026-07-31 | **HELD — Build Phase 3 (2026-07-31 replan, see `## V1 Build Phases` above). Not being dispatched to `build` until Phase 3 starts** — matches "live events" in Aviran's own Phase-3 description directly. TRD stays approved, no rework needed while it waits. **TRD delivered** (`e142e91`). Surface: **[Backend]** A1-A4 + **[Algo/Data]** B1-B7 (not backend-only). View-based access (`events_public`) inverts migration 001's RLS pattern deliberately — base tables get no policy + full revoke, the view is the sole boundary. `rank` absolute/time-invariant. Source-agnosticism mechanical (`primary_source` FK into `event_sources`, no vendor name possible elsewhere). 3 things surfaced not designed around: `withdrawn_at` for source-cancelled events, no-end-time feeds' strict-drop escalated to Aviran (not silently defaulted), "proximity" resolved as static centrality (no personalization exists to build real proximity from). New Aviran gate: 3 extensions (`pg_net`/`supabase_vault`/`pg_cron`) + an Edge Function deploy. **2 real findings, both being fixed:** (1) **HIGH security** — `security-auditor` confirmed §4.1's `hood_for_point()`/`reattribute_events()` have no `revoke execute`, same bug class as migration 002's incident (§4.3's functions in the same TRD got the correct treatment, §4.1's two were simply missed) — `reattribute_events()` would be an unauthenticated bulk-`UPDATE` RPC endpoint, blunted only *implicitly* by an unrelated RLS revoke elsewhere. Dispatched `developer` to add the 2-line fix. (2) `data-engineer` confirmed `hood_for_point()` needs the same shared-fixture resolution as T-040/T-042 (folded into the consolidation pass) **plus a specific gap**: the on-boundary test case's expected value (`false`, per T-040/T-042's fixture) isn't stated on this TRD's side — flagged as an add-on to the consolidation pass. **`developer`'s formal verdict: REQUEST CHANGES** — 3rd independent confirmation of the same HIGH security finding (already being fixed), plus **one new finding**: `events.hood_id` uses `on delete set null` (§3.2), contradicting `hood-dataset`'s TRD which expects `restrict` to match `places.hood_id`. Might be legitimate (events tolerating null attribution as normal state, repaired by `reattribute_events()`, unlike places) but currently unstated — silently diverges from a sibling TRD it otherwise cross-references carefully. Folded into the same in-flight fix dispatch (narrow additive fix, not a redesign — either flip to `restrict` or add one sentence of stated rationale). **`trd-review` fully landed, all 4 reviewers — HIGH security finding now confirmed by 5 independent passes** (security-auditor, code-reviewer×2, developer×2) — about as corroborated as a finding gets in this project. `code-reviewer` also offered a real opinion on the FK question: `set null` is likely the *correct* call, not a defect (events are ephemeral/re-attachable unlike places, and `restrict` would break T-040's own prune step) — needs the reasoning written down, not necessarily flipped. 2 new minors folded into the same fix pass: missing `if not exists` guards, and a Postgres-15+ version check for `security_invoker = false`. **Security fix landed, commit `27911b0`, verified genuine** — `revoke execute` on both `hood_for_point()` and `reattribute_events()` (§4.1), rationale paragraph mirroring §4.3's pattern naming `reattribute_events()`'s bulk `UPDATE` as the sharp exploit and noting it was previously blunted only implicitly by `events`' own RLS. Both verification notes promoted to explicit TRD requirements (all 4 base tables named for individual RLS confirmation; `pg_net`'s internal log tables checked for anon/authenticated grants, since they'd otherwise leak the outbound Bearer token). All 3 relayed add-ons folded in on their merits, not blindly: `events.hood_id`'s `set null` now has its rationale stated explicitly (matches `code-reviewer`'s own lean — `restrict` here would break T-040's Hood-prune step) rather than flipped or left silent; `if not exists` guards added throughout; the Postgres-15+ requirement now flagged with a named fallback. **T-043's `trd-review` is fully closed — all findings resolved.** Consolidation pass landed (fixes present in `27911b0`/`293ddb2` combined) — hood_for_point() resolution written into §1/§4.1/§8/§9, on-boundary pinned to "no Hood" including shared edges. **2 final small items resolved:** the "same ray-cast" clause-4 nuance was already correctly scoped as covering only T-040/T-042's Python consumers (§8 F7), not this task's separate SQL need — no further edit needed here; the stale "003 claimed by T-033" migration-number comment (§3) was fixed directly (mechanical correction, no design judgment) to state the settled `005`. `developer`'s REQUEST CHANGES verdict independently re-verified as fully resolved — its 2 findings (revoke, FK rationale) are exactly what the security-fix commit addressed. **`build`-ready.** Not yet dispatched — 2 pairs needed (`developer`+`code-reviewer` A1-A4, `data-engineer`+`code-reviewer` B1-B7) — `security-auditor`'s sign-off already given at `trd-review`, not needed again unless the actual SQL diverges from what it reviewed. Linear `PAS-20` (accidentally duplicated as `PAS-23`, self-caught, marked Duplicate). |
| T-038 | Search & quick filters (PRD) | design | designer (queued) | 2026-07-30 | `prds/search-quick-filters/search-quick-filters.md` (Draft v1) by `product` — dispatched directly by the coordinating session on Aviran's ask, not through the usual relay; passed along to stay in sync. Sheet over the map, one field matching place name/keyword/Hood name, 2 category chips (decision #41's placement), results dim the map to matches and land on the same destination sheets a map tap reaches, at the currently-selected slider hour. 8 of the ~9-10 PRDs in `prds/INDEX.md` now written — only Scenic Walk and TikTok import (PAS-6 items 8-9) still held, both on Aviran. Depends on T-031 (place/Hood data, dim behavior), T-032 (selected hour), T-033 (destination sheets) — none of which are dispatched yet either (see below). Not launch-blocking on any feasibility question. **Two things flagged for Aviran, not resolved:** (1) sheet layout genuinely open — original ask was a literal 50/50 map/list split, a design pass recommended native `.medium`/`.large` detents instead, pending his sign-off; all 8 P0s written to hold under either, but resolving this alongside the design brief would save a rework loop (same shape as T-031's lesson). (2) keyword search ("hummus", "rooftop bar") has no data behind it — places table has no keyword field, only name + 1 of 2 categories; either the dataset gains a keyword field or keyword search degrades to name-matching only. **One `product`-labeled [ASSUMPTION], not a finding:** strategy says results "carry same heat and tag signals as map," but `tourist-trap-flag` req 6 gives that flag exactly one home (the place modal) and `map-rendering-spec.md` §4 puts no flag on any pin — product read this as satisfied by the dim-map + destination-sheet pattern (matches `ux-flows.md` Journey 5) rather than a per-row flag line, which would reopen `tourist-trap-flag` req 6. Components for the design-review packet: Search sheet container (layout open) · Query field (3 match types, <400ms) · Category chips (sheet-internal only) · Result row (no flag line) · Map dim state · Session state store. Same word-budget overage self-flag as the prior 3 PRDs (1555 vs ~1200). **Watch item from T-033's design-approval REJECT (2026-07-30):** `ux-flows.md` L59 makes the same unspecified "map stays 100% interactive" claim this sheet will need too — T-033 hit exactly this as a blocking finding (its `.sheet-backdrop` swallowed every tap). Whoever designs this should check whether T-033's fix documented a real shared mechanism (`.presentationBackgroundInteraction` or equivalent) somewhere reusable before re-deriving it from scratch. **Data-requirements sweep (2026-07-31):** fixed in place — the keyword-search field is now given an owning PRD (`T-042`/`places-dataset`), resolving the "keyword search has no data behind it" open question this PRD flagged at its own writing. Linear `PAS-29` (backfilled 2026-07-31 reconciliation pass). |

| PAS-21 | No git remote configured — retitled by a concurrent session to the narrower open question: is pushing now authorized? | blocked-on-aviran | — | 2026-07-31 | **The "no remote" gap itself is CLOSED** — Aviran configured both remotes 2026-07-31 (`origin` → `github.com/AviranGrisaro/passenger-brain` and `-code`, both `main`); `passenger-code` fully synced (0 ahead/behind); `passenger-brain` has unpushed local commits (3 as of Linear's own count: `1c37640`, `30e002f`, `489a9eb`). **What's still genuinely open, Aviran's to answer:** does configuring the remote mean agents should start pushing routinely, or does he want to keep doing the actual `git push` himself? Standing policy today (`CLAUDE.md`) is the latter — agents commit, don't push, tell him what's ready. **Correction to my own action this run:** I initially marked the Linear issue `Done` without noticing a concurrent session had already retitled/re-described it into this exact open question — caught it, reverted to `Backlog`/`aviran-blocker`, apologized in-thread. Leave this to Aviran; don't re-close without his answer. |
| PAS-24 | Phase 2 candidate: place metadata/tagging attributes (discovery engine) | backlog | — | 2026-07-31 | Founder-direct, live `/cos` chat, 2026-07-31 (PROGRESS.md FOUNDER-DIRECT STUB). New Phase 2 candidate — per-place metadata/tag attributes for the discovery engine: romantic, seat_outside, allow_smoking, kid_friendly, wifi, pet_friendly given as a starter-list example, not a closed spec. **Not Phase 1, not authorized to build, no PRD.** No `type:data-request`/`type:feature` labels exist in this Linear workspace — used the existing `Feature` label. No `aviran-blocker` (unlike PAS-11, nothing here needs Aviran's decision before it can sit in Backlog — it's parked only because Phase 2 itself is parked). Related-but-distinct existing docs, linked in the ticket for whoever scopes this later: `data-eng/discovery-engine-spec.md` (algorithm/pipeline, no place-attribute model) and `prds/places-dataset/places-dataset.md` (T-042/PAS-19, owns the curated `places` schema today, no amenity/vibe tags). Traceability only — resolve whenever Phase 2 starts. |
| T-044 | Own place closed-state refresh (flagged 3 times, no owner) | backlog | — | 2026-07-31 | **Build Phase 2** (2026-07-31 replan) — a real-dataset maintenance concern, meaningless until real data exists. Linear `PAS-22`. **Row was missing from BOARD.md entirely — backfilled during the 2026-07-31 full reconciliation pass; Linear issue existed since 2026-07-30 with no corresponding board row.** Flagged independently 3 times (`product`'s data-requirements sweep, `places-been-saved`/T-036's own PRD, `places-dataset`/T-042's TRD) without ever getting an owner. A place can go from open to permanently-closed over time; nothing specifies what mechanism keeps that status current after initial dataset authoring. Needs `product` to fold into an existing PRD's scope (most likely `places-dataset`, as a v2/maintenance concern) or write a short supporting PRD. |
| PAS-16 | Centralize map background-interaction pattern (`.presentationBackgroundInteraction`) for sheets | backlog | — | 2026-07-30 | **Row was missing from BOARD.md entirely — backfilled during the 2026-07-31 full reconciliation pass; Linear issue existed since 2026-07-30 with no corresponding board row.** Split off from `PAS-14` (closed) — the map-surface-interactivity mechanism T-033 solved via `.presentationBackgroundInteraction(.enabled(upThrough: .medium))` lives only in that task's own design spec, not in `design-principles.md`/`ux-flows.md`. `ux-flows.md` L59 still makes the same unspecified claim for T-038's search sheet with no mechanism named. Not launch-blocking, small well-scoped craft task for `designer`. Still unclaimed. |

| T-045 | 10 uncommitted `agents-mirror/*.md` edits + 2 untracked `design/*.md` belong to nobody | backlog | project-manager | 2026-07-31 | **Named here per L-017 / `CLAUDE.md` safety rule 5 — at least three agents have now refused to stage these in their worklogs without anyone claiming them.** Uncommitted in the shared working tree: `agent-os/agents-mirror/{architect,code-reviewer,competitor-research,data-engineer,developer,ios-code-reviewer,ios-developer,marketing,project-manager,qa}.md`. Untracked: `design/interactive-wireframe-prompt.md`, `design/mockup-prompts.md`. Rule 2 forbids any agent staging another session's in-flight files, so each pass refuses them and moves on — which is exactly how days-old edits end up owned by no one. An *uncommitted* mirror is not a mirror (rule 5), so if any of the 10 carries a real live-agent-file change, that change currently has no backup. Needs `project-manager` to identify the authoring session, `diff` each against the live `.claude/agents/` copy, and either commit or archive. Flagged by `product` during the T-032 v2 PRD amendment; also refused by `architect` and `chief-of-staff` in earlier 2026-07-31 entries. |

## Linear

Workspace `passenger`, team **Passenger** (`PAS`), one project — **Passenger V1**. Features are issues inside that project, not projects of their own; the previous workspace fragmented into 13 projects and lost the thread. `chief-of-staff` is the only agent that writes to Linear.
