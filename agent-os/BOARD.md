# Passenger Agent OS — Board

Single source of state for the agent team. Every agent reads this **and `PROGRESS.md`** before starting, and updates its rows + appends a PROGRESS.md worklog entry after finishing — committed and pushed the same turn. `chief-of-staff` is the runtime: each "run the company" pass moves every task as far down the lifecycle as it can and refills the pipeline via `product` when it drains.

## Scope gate (added 2026-07-26 — read before opening any task)

Passenger exists because the previous board ran 27 PRDs through this exact lifecycle, ten of them building features the strategy forbids. Every gate passed. Nobody read the strategy.

**A task may not leave `spec` unless its PRD quotes the line in `strategy/passenger-strategy.md` that authorizes it.** `product` enforces this and rejects PRDs that can't. Standing prohibitions:

- No social features of any kind — friends, following, posting, presence, profiles, avatars.
- No onboarding — the app opens straight to the map plus location permission.
- Phase 2 (proximity intelligence, **Scenic View**, **Live Events**) and Phase 3 (AI guide, shake-to-decide) are parked. Don't build toward them or leave hooks. Points is retired — replaced by the Phase 2 stamp collection system (see `strategy/passenger-strategy.md`), not a separate parked item.
- ~~**Scenic View and Live Events moved out of V1 on 2026-07-27** (decisions #19, #20).~~ **Reversed — do not read this as a prohibition.** Live Events returned to V1 on 2026-07-29 and Scenic Walk on 2026-07-30 (decisions #32, #34; `strategy/passenger-strategy.md` V1 scope). Struck rather than deleted, per convention. The salvage warning still stands: both existed in the Locali client, so watch for the *old* shapes returning through `SALVAGE.md` rather than through a PRD — the Locali events overlay was an unflagged raw feed, not the ranked subset now specced. Flagged by `product`, 2026-07-30 (PAS-10).

## Task lifecycle (state → owner)

```
backlog → spec(product) → design(designer) → trd(architect) → build(ios-developer and/or developer)
        → code-review(ios-code-reviewer and/or code-reviewer) → qa(qa) → acceptance(product) → done
```

- **iOS vs backend split:** `build`/`code-review` fan out by surface — `ios-developer`/`ios-code-reviewer` for the Swift/SwiftUI client (`passenger-code/`), `developer`/`code-reviewer` for the Supabase backend (`passenger-brain/database/`), both pairs when a task spans both. The architect's TRD build breakdown tags each step **[iOS]**/**[Backend]**.
- Pure-build tasks (no UX surface) skip `design`.
- Marketing/research tasks: `backlog → in-progress(owner) → acceptance(product) → done`.
- Rejection loops (backward only): code-review REQUEST CHANGES → `build` · qa FAIL → `build` · acceptance REJECT → `build` or `design`. The rejecting agent writes concrete findings; the fixing agent addresses exactly those.
- `blocked-on-aviran`: strategy/scope decisions, money, external accounts (App Store, credentials), destructive ops — including every Supabase migration apply step (Aviran holds the DB credentials). Nothing else stops the loop.
- A task is `done` only after `product` ACCEPTs it against the PRD.

Task rows: `| id | task | state | owner | updated | notes / findings / output |`

## Active phase

**Phase 1 — ship one map to real strangers in Tel Aviv.** Per `strategy/passenger-strategy.md`: nothing downstream matters until a real person reopens the app within a week, unprompted.

## Tasks

| id | task | state | owner | updated | notes / output |
|---|---|---|---|---|---|
| — | _empty — awaiting first `product` pass over the strategy doc_ | — | — | 2026-07-26 | Fresh start. See PROGRESS.md entry for 2026-07-26. |
| M1 | Phase 1 marketing & acquisition plan (grassroots, zero-spend, Tel Aviv) | in-progress | marketing | 2026-07-28 | `marketing/phase-1/marketing-acquisition-plan.md`. **REJECTED by `product`** (PROGRESS.md, commit `e5339ea`) — back to `in-progress(marketing)` per rejection-loop rule. Blocking findings F1–F3: (F1) the "2–8 day" reopen window silently answers strategy's open "within a week" question with no [ASSUMPTION] label; (F2) "unprompted" is defined against a factually wrong claim that V1 ships no notifications — decision #24 puts a local notification in V1, so a notification-tap reopen would wrongly count as unprompted, and tracking needs a launch-source field (cold launch vs. notification tap); (F3) "confirmed by Aviran" zero-budget claim is unsourced (no such decision exists anywhere) and self-contradicts the plan's own ask for Aviran to approve QR-card print costs. Should-fix, non-blocking: restate the 20%/30-install gate as existence/repeatability (20% of 30 is only 6 people — too noisy for a rate reading), remove or escalate the unauthorized native share-sheet "invite a friend" idea (no strategy line permits in-app share/invite, adjacent to no-social-features), fix the plan's stale "Phase 4"/"friends & family beta" Open-questions residual (role-file itself already fixed in `8b19a5e`). Full verdict in PROGRESS.md. |
| T-030 | Scope live-events ingestion pipeline — new V1 launch-blocking dependency (Linear `PAS-5`) | done | data-engineer | 2026-07-30 | **Closed by `chief-of-staff` — deliverable complete.** Full writeup: `passenger-brain/data-eng/live-events-feasibility.md`. Sourcing options assessed: Eventbrite/Ticketmaster (real APIs, good freshness, skews commercial/ticketed), PredictHQ-style aggregator (Israel coverage/pricing **[ASSUMPTION]**, unverified), Facebook Events (very likely dead as a feed), local Israeli ticketing/nightlife platforms (most likely home for the real informal-event layer, API access **unconfirmed — single biggest open gap**), municipal calendars (scraping, official-only), manual/crowd curation (tied to decisions #22/#24's known tradeoffs). Cost: ~1-2 weeks first feed integration (gated on places/Hoods schema), ~1-2 weeks/additional source, ~1-2 weeks ranking layer; ~3-5 weeks total for a thin single-feed v1. **Explicit call: Conditional** — thin ticketed-only version buildable in Phase 1; the fuller "feels alive" version is a data-access gap (not engineering-hours), not buildable in Phase 1 with confidence. Recommends shipping thin for V1, flagging the commercial skew, treating the informal-events gap as fast-follow. **Aviran-facing follow-on (thin-version acceptability, real launch-date risk if not) moved to PAS-6 item 10** so it doesn't go stale inside a closed ticket — same pattern as PAS-7's Scenic Walk/TikTok follow-ons. Dispatch note: this task raced against a second, unidentified "chief-of-staff"-labeled relay that asked for the same dispatch around the same moment; `main` had already spawned one before the second was actioned, that one was stood down, only one agent ever ran — see PROGRESS.md for the full near-collision record. |
| PAS-6 | Confirm 2026-07-30 founders-meeting V1 scope ambiguities | blocked-on-aviran | — | 2026-07-30 | **Corrected — was wrongly reported as "nine items, none resolved" earlier this run; caught by `product` during the PAS-9 pass, verified directly against strategy.md/decisions.md.** Six of the original seven are RESOLVED, decided live by Aviran the same day (decisions #37-42): (1) tourist-trap boolean fully replaces the 3-tag system, (2) closed-place save allowed with distinct badge, (3) softer copy landed on "tourist-heavy spot", (4) "Profile tab" naming confirmed as gate exception, (5) per-Hood Local status replaces the 7-tier ladder outright, (6) quick filters stay sheet-internal. **Genuinely still open:** (7) Google Maps import — already known explore-only, not a build blocker either way; **(8) Scenic Walk — ship the lighter locked version now, or slip V1 for weighted routing?**; **(9) TikTok import — ToS/legal sign-off needed before a TRD.**; **(10) Live events — thin ticketed-only version acceptable for V1, or does its commercial skew mean the launch-blocking risk is real (PAS-5's data-engineer scoping, 2026-07-30)?** Net effect: the three PRDs held pending this ticket (tourist-trap/localness pipeline, Places incl. closed-place, Passport) are now unblocked — dispatched `product` to write them. Full detail: `strategy/passenger-strategy.md` Open questions, `strategy/decisions.md` #27-42, Linear PAS-6 comments. |
| PAS-7 | Scope Scenic Walk weighted-routing + TikTok import feasibility | done | data-engineer | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Full writeup: `passenger-brain/data-eng/scenic-walk-tiktok-feasibility.md`. Calls: (1) Scenic Walk weighted routing — **not buildable in Phase 1 window**; recommend shipping the already-locked lighter polyline-comparison version instead. (2) TikTok place extraction — **buildable in Phase 1 window** (~2-4 weeks); ToS/access blocker flagged. (3) Google Maps saved-list import — feasibility read only, per decision #35, not scoped as a build. Both Aviran-facing follow-on decisions moved to PAS-6 (items 8-9) so they don't go stale inside a closed ticket. |
| PAS-8 | Reconcile UX docs (`ux-flows.md`, `map-rendering-spec.md`) against 2026-07-30 scope lock | done | designer | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Reconciled: Hoods terminology, Been-place Passport stickers, Saved-vs-Been distinction, Passport promoted from Phase-2-preview (Journey 7) into real V1 scope, category rename. **Two small residuals flagged for a future docs pass (not blockers):** `map-rendering-spec.md` §3 renders "Tourist-heavy spot" at a Hood centroid — adjectival mismatch (Hood is a polygon, not a "spot"), one-word fix; `ux-flows.md:176` still has stale "not flagged a tourist trap" phrasing predating decision #37. |
| PAS-9 | Revise "tourist trap" copy in Phase 1 marketing plan | done | product | 2026-07-30 | **ACCEPTED by `product`** (verified diff `5e23ef2` directly, not just the worklog summary) — term satisfies decision #36's ask, scope discipline intact, more honest post-decision-#37 since the underlying flag is now a plain boolean. Product also struck a now-stale note in `prds/hood-place-detail/hood-place-detail.md` that told designer to wait on this term (decision #42 logged). Closed by `chief-of-staff`. |
| PAS-10 | Draft V1 PRDs for confirmed 2026-07-30 scope items | done | product | 2026-07-30 | Closed by `chief-of-staff` — deliverable complete. Four PRDs written (T-031…T-034 below). **Update: of the five originally-held items, three are now unblocked** (tourist-trap/localness pipeline, Places incl. closed-place edge case, Passport — PAS-6's items 1/2/4/5/6 all resolved, see PAS-6 row) — dispatching `product` for these next. Scenic Walk and TikTok import remain genuinely blocked (PAS-6 items 8-9, Aviran's call). Google Maps import stays excluded (decision #35). |
| T-031 | Map — Hoods & heat area (PRD) | code-review | ios-code-reviewer | 2026-07-30 | **`qa`'s Major finding (camera doesn't recenter on system-prompt permission grant) — fixed by `ios-developer`, commit `f31816c`, verified genuine.** `chief-of-staff` confirmed directly against the code: new `.onChange(of: locationStore.authorizationStatus)` (L110) gated by a pure static predicate `isNewGrant(from:to:)` (L130) — true only for not-authorized→authorized transitions, correctly excludes the lateral `authorizedWhenInUse`↔`authorizedAlways` move and no-op re-renders. Mirrors `handleNearMeTap()`'s existing one-shot camera assignment. New `PassengerTests/MapScreenTests.swift` (9 unit tests on `isNewGrant`) confirmed present. `ios-developer` reproduced `qa`'s exact repro scenario live (fresh install, `simctl` location set to Jerusalem, system prompt granted without any near-me tap) — camera recentered immediately, stable on a follow-up screenshot; `TEST-PLAN.md` case 17 now passing. Full suite 35/35 unit (26 prior + 9 new) + 1/1 UI, 0 failures. Did not touch the 4 non-blocking should-fix items — correctly kept the fix pass narrow. **Dispatching `ios-code-reviewer` for a fresh `code-review` pass on this specific diff** before returning to `qa` — the backend tracks are untouched, so no need to re-dispatch `code-reviewer`/`security-auditor`. |
| T-032 | Time slider — now → +12h (PRD) | design-review | designer | 2026-07-30 | **`design-approval` cleared both signatures (`chief-of-staff` + `product`, second submission, no loop-guard) — advanced to `design-review`.** All 5 rejection findings independently re-verified fixed by both reviewers against the live re-fetched artifact (contrast, chrome-collision via `.chrome-hide`/`setFabsVisible()`, heat-stub label, day-flag/repaint-pill homes, real drag-to-dismiss). `product` additionally caught and fixed its own prior contrast-bullet error in the PRD (missed `.sheet-eyebrow`, wrongly held the track rail to 3:1 when a native Slider's inactive rail can't clear it) — not a second rejection, just a self-correction. **Linear `PAS-15` created**, Components list + follow-ups posted (incl. flagging Aviran's ratification on the `ux-flows.md` Locked-file edit), `gate:awaiting-design-review` applied. **Design-review gate is Aviran-only now** (see T-031's row / PROGRESS.md's "FOUNDER-DIRECT STUB") — no Serge wait. Waiting on Aviran's sign-off via Linear `PAS-15` (hilos unreachable this session). **Cross-task flag correction:** the `ux-flows.md` §2.1 note only resolves T-032's own chrome-collision question, **not** T-033's separate map-background-interactivity question (`.presentationBackgroundInteraction`) — `PAS-14` was closed too broadly last pass; corrected with a follow-up comment, and split the still-open half into a new ticket, `PAS-16` (design-principles.md centralization, for T-038's benefit), not launch-blocking. |
| T-033 | Hood & place detail (PRD) | design-review | designer | 2026-07-30 | **`design-approval` cleared both signatures (`chief-of-staff` + `product`, second submission) — advanced to `design-review`.** History: rejected first pass (3 blocking: map-tap-interactivity broken behind an open sheet, save state reset on reopen, two sub-44pt hit targets; 3 should-fix: color-usage claim, wrong glyph, walking-mode not threaded through) — a stalled fix attempt left the spec text fixed but the mockup untouched; a second `designer` pass fixed all 6 directly in the mockup (commit `d622b05`), independently re-verified against the live artifact by both `chief-of-staff` and `product` (commit `11c4d1b`) before this pass. Full finding-by-finding record in PROGRESS.md 2026-07-30 entries. **Linear `PAS-13` created**, Components list + follow-ups posted, `gate:awaiting-design-review` applied. **Cross-task flag resolved, not left open:** `PAS-14` closed — T-032's fix pass added the shared "modal/sheet vs. persistent-chrome stacking" note to `design/ux-flows.md` §2.1 directly, covering this task's finding 1 mechanism too; T-036/T-037/T-038 now have one place to read instead of re-deriving it a third time. **Design-review gate update (2026-07-30):** per the founder-direct instruction recorded on T-031's row (full record in PROGRESS.md's "FOUNDER-DIRECT STUB" entry, and now in `chief-of-staff.md`'s gate section), Serge's sign-off is no longer required here — Aviran's approval alone clears this gate; no more waiting on hilos/Serge. **But T-033 has zero Aviran signature on record for this gate at all** — the blanket statement named T-031 and T-033 by number but gave no per-task verdict for T-033. Asked `main` to get an explicit Aviran approve/reject for T-033 specifically before applying `gate:design-approved`, rather than inferring it from the blanket statement alone (a clean paper trail, per `main`'s own suggestion). Waiting on that reply — once it lands (Linear `PAS-13` comment or `main`'s relay), apply `gate:design-approved` and advance to `trd`. |
| T-034 | Live events overlay (PRD) | design | designer | 2026-07-30 | `prds/live-events-overlay/live-events-overlay.md` (Draft v1). Components: **Event marker layer** — hour-bound, shape-distinct from place pins, clusters neutrally · **Event detail sheet** — name, time, location, route hand-off · **Ranked-subset client** — renders backend rank order, bounded marker count · **Events layer toggle** — sits with the other toggles in the heat modal · **Empty/degraded state** — app fully usable with the layer permanently empty. **Launch-blocking on PAS-5**; the client is shippable with no data, which does not clear the launch gate. Depends on T-031, T-032. |
| PAS-11 | Phase 2 candidate: "Local personas" (concept, not scoped) | blocked-on-aviran | — | 2026-07-30 | Founder-direct, live hilos chat, 2026-07-30 (PROGRESS.md). New Phase 2 candidate — curated local-guide profiles a user can follow as a discovery lens (favorite places, Hoods, routes). **Not Phase 1, not authorized to build, no PRD.** Recorded in `strategy/passenger-strategy.md` Phase 2 candidates + `strategy/decisions.md` #43. `product`'s concept-level read (not a PRD) found the recording had added an unstated premise ("curated" — flagged [ASSUMPTION], strategy.md Open question #10) and gave recommendations (not resolutions) on the other two tensions — see Linear PAS-11 comments and PROGRESS.md. **Three questions now sit with Aviran, in dependency order** (strategy.md Open questions #8–10, posted in hilos `#general` same date): (1) authoring model, first-party vs. ever user-authored — gates the rest; (2) follow vs. lens framing — `product` recommends lens-only; (3) same concept as Phase 3's "AI local guide" persona, or separate. `aviran-blocker` label applied, no owner while blocked, per the standing PAS-6 pattern. Not a Phase 1 launch blocker — resolve whenever convenient. |

| T-035 | Tourist-trap flag & Local QA (PRD) | design | designer (queued behind T-031 rework) | 2026-07-30 | `prds/tourist-trap-flag/tourist-trap-flag.md` (Draft v1) by `product`, unblocked by decisions #37/#42. One boolean flag per Hood/place, Hood-outline-stroke rendering (already specified, not re-derived — `map-rendering-spec.md` §§2-3,4,7), one text line in the place modal, binary local-QA toast correcting it. Kept as one PRD deliberately — splitting the flag from its correction mechanism would let it ship uncorrected, against the "algorithm plus local QA, together" authorizing line. Depends on T-031 (consumes the Hood-centroid rendering channel T-031's rejection just fixed — see T-031 finding 4). **Not dispatched yet** — queued behind `designer`'s in-flight T-031 rework so the tourist-trap design doesn't get built against a not-yet-fixed map layer. |
| T-036 | Places — Been & Saved (PRD) | design | designer (queued behind T-031 rework) | 2026-07-30 | `prds/places-been-saved/places-been-saved.md` (Draft v1) by `product`, unblocked by decision #38. One Places list, three provenance states (Saved/Been/Visited) with precedence, permanently-closed badge, binary map ring accent (close zoom only). Depends on T-031. Sequenced before T-037 (Passport consumes its Been signal). Not dispatched yet, same reason as T-035. |
| T-037 | Passport (PRD) | design | designer (queued behind T-031 rework) | 2026-07-30 | `prds/passport/passport.md` (Draft v1) by `product`, unblocked by decisions #39/#40. Private per-city sticker album + per-Hood Local status under the Profile tab. Seven-tier ladder excluded outright, not even as a derived/display status. Depends on T-031 and T-036 (consumes Been signal). Not dispatched yet, same reason as T-035/036. **Three real product findings surfaced by `product`, not resolved, flagged for Aviran (see PROGRESS.md and chief-of-staff's direct report):** (1) sticker-shape-matches-place-type (decision #29) needs a `place_type` field the data model doesn't have — either add it (as an internal field, doesn't need to touch the 2-category user-facing split) or V1 ships 2 sticker shapes only; (2) nothing in the current mechanics rewards *answering* local-QA — Passport was pulled forward specifically as that incentive layer, but decision #29 ties stickers to Been-places (visiting) and #40 retired the ladder that would have carried a QA-answer reward — the pipeline's actual named risk goes unaddressed; (3) Passport/Places are device-local (follows from the no-accounts lock) and don't survive a reinstall — nobody has confirmed that losing a sticker album on a new phone is acceptable; the only fix is an anonymous server identity, which is parked to Phase 3. All three are product-shape questions, not blocking PRD/design work, but worth Aviran's read before Passport goes further than a design mockup. |
| T-039 | Port Scenic Walk / TikTok feasibility into `prds/` PRD-gate location | done | data-engineer | 2026-07-30 | Dispatched to (re-)scope Scenic Walk and TikTok import feasibility; found `PAS-7` already closed this exact ask 2026-07-30 with a full writeup at `data-eng/scenic-walk-tiktok-feasibility.md`. **Did not re-derive the research** — per the doc rule against near-duplicates, ported PAS-7's findings into the location `prds/INDEX.md`'s scope gate actually reads before unblocking `product`: `prds/scenic-walk/feasibility.md` and `prds/tiktok-import/feasibility.md`, each with the feature-scoped recommendation and an explicit go/no-go/slip call. `data-eng/README.md` cross-referenced both ways so the two locations don't drift. **Updated `prds/INDEX.md`'s stale framing** — it said both items were "launch-blocking on `data-engineer`'s feasibility scoping," which PAS-7 had already answered; corrected to say scoping is done and the real remaining blocker is Aviran's own scope/ToS calls, already tracked as `PAS-6` items 8–9. **Calls carried over from PAS-7, not re-derived:** Scenic Walk's weighted-routing version (decision #32's literal text) is not buildable in Phase 1 — recommend shipping the already-locked lighter comparison-polyline version (decision #19) instead, pending Aviran's ship-vs-slip call. TikTok import is technically buildable in Phase 1 (~2–4 weeks, off-the-shelf OCR/ASR/LLM/geocoding, mandatory confirm-before-save), gated on Aviran's ToS/access sign-off before a TRD. Neither PRD should be written until those two Aviran calls land — this task doesn't change that, it only fixes the stale reason given for the hold. |
| T-038 | Search & quick filters (PRD) | design | designer (queued) | 2026-07-30 | `prds/search-quick-filters/search-quick-filters.md` (Draft v1) by `product` — dispatched directly by the coordinating session on Aviran's ask, not through the usual relay; passed along to stay in sync. Sheet over the map, one field matching place name/keyword/Hood name, 2 category chips (decision #41's placement), results dim the map to matches and land on the same destination sheets a map tap reaches, at the currently-selected slider hour. 8 of the ~9-10 PRDs in `prds/INDEX.md` now written — only Scenic Walk and TikTok import (PAS-6 items 8-9) still held, both on Aviran. Depends on T-031 (place/Hood data, dim behavior), T-032 (selected hour), T-033 (destination sheets) — none of which are dispatched yet either (see below). Not launch-blocking on any feasibility question. **Two things flagged for Aviran, not resolved:** (1) sheet layout genuinely open — original ask was a literal 50/50 map/list split, a design pass recommended native `.medium`/`.large` detents instead, pending his sign-off; all 8 P0s written to hold under either, but resolving this alongside the design brief would save a rework loop (same shape as T-031's lesson). (2) keyword search ("hummus", "rooftop bar") has no data behind it — places table has no keyword field, only name + 1 of 2 categories; either the dataset gains a keyword field or keyword search degrades to name-matching only. **One `product`-labeled [ASSUMPTION], not a finding:** strategy says results "carry same heat and tag signals as map," but `tourist-trap-flag` req 6 gives that flag exactly one home (the place modal) and `map-rendering-spec.md` §4 puts no flag on any pin — product read this as satisfied by the dim-map + destination-sheet pattern (matches `ux-flows.md` Journey 5) rather than a per-row flag line, which would reopen `tourist-trap-flag` req 6. Components for the design-review packet: Search sheet container (layout open) · Query field (3 match types, <400ms) · Category chips (sheet-internal only) · Result row (no flag line) · Map dim state · Session state store. Same word-budget overage self-flag as the prior 3 PRDs (1555 vs ~1200). **Watch item from T-033's design-approval REJECT (2026-07-30):** `ux-flows.md` L59 makes the same unspecified "map stays 100% interactive" claim this sheet will need too — T-033 hit exactly this as a blocking finding (its `.sheet-backdrop` swallowed every tap). Whoever designs this should check whether T-033's fix documented a real shared mechanism (`.presentationBackgroundInteraction` or equivalent) somewhere reusable before re-deriving it from scratch. |

## Linear

Workspace `passenger`, team **Passenger** (`PAS`), one project — **Passenger V1**. Features are issues inside that project, not projects of their own; the previous workspace fragmented into 13 projects and lost the thread. `chief-of-staff` is the only agent that writes to Linear.
