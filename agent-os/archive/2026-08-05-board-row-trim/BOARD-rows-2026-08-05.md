# BOARD.md row archive — 2026-08-05 (project-manager nightly J check)

Full accumulated history for oversized `done`-state BOARD.md rows, trimmed per check J (row >1200 chars). Same precedent as `archive/2026-08-01-board-row-trim/`.

## T-032

| T-032 | Time slider — now → +12h (PRD) | done | — | 2026-08-04 | **DONE — Aviran, direct instruction in buzz `#general` 2026-08-04: "mark T-032 done."** Overrides the open lifecycle: `product` acceptance was REJECT on 2026-08-03 (F1/F2 below) and this task was still at `build` when the call came — it did **not** re-pass `code-review`/`qa`/`acceptance`. **Carried forward, not waived:** the F1/F2 fix was still uncommitted working-tree WIP in `passenger-code` at the moment of this move (`HourReadout.swift` + untracked `PassengerUITests/HeatModalCardLayoutTests.swift`, on top of `5e1f72f`), and the round-10 `ios-developer` dispatch to verify/build/test/commit it had not replied. Whether F1 (nav row over the readout) and F2 (AX5 mid-token wrap) are actually fixed on `main` is **unverified** — `c1b8bc3` "Nav row position, modal sizing" may have moved F1 independently. Sibling bottom-anchor pattern still tracked at `T-054`; non-blocking C14 (live VoiceOver) and C20 (`mapCenterX` unused in tests) still open. Prior owner at the time of the call: `ios-developer` — **Round 10 (2026-08-04… — full history below. Prior: **trd-review CLEARED on v3, both signoffs APPROVE — build ready and appears underway.** **TRD v3 delivered — all four `trd-review` findings resolved. Ready for `trd-review` re-run by BOTH `ios-developer` and `ios-code-reviewer`** (both signoffs required, one was a block). Blocking finding closed: **§9 row 7 gains sub-check (e)** — an in-band *vertical* edge drag must leave `camera`/`MKCoordinateRegion` byte-identical across three samples, same comparison method as row 2c, on device or in a UI test — and **C13 is redefined as not-done until it passes**, so D7's hit-test claim fails at build rather than at `qa`. §2.4/§4.8/D7 now state that claim as a prediction requiring confirmation, not settled fact; §10 gains the matching risk row. Three minors closed: `coordinateSpace` → default `.local` (verified: zero `.coordinateSpace` hits in `passenger-code`); §9 row 6 Step → `C4, C9, C12`; row 7d/C13 exercise **both** sheet detents × both touch targets (`MapScreen.swift:186`, `HoodSheet.swift:26` `[.medium,.large]`, `PlaceDetailModal.swift:22` `.medium`-only). **Fifth fix, raised by neither review:** the TRD named a nonexistent type `HoodStubSheet` in three places — real type is `HoodSheet` (`Detail/HoodSheet.swift:7`), renamed. **For `project-manager` (rule 2 / L-017):** `prds/places-been-saved/TRD.md` (T-036) sits modified and uncommitted in the shared tree from another session and was deliberately not swept into `architect`'s commit. `chief-of-staff`'s founder-direct icon-only D6 edit to this TRD was uncommitted mid-pass but that session committed it itself (`5220c1f`), so it is not carried here; `architect`'s `PROGRESS.md` hunk does carry `ios-code-reviewer`'s self-signed T-035 verdict entry, left uncommitted by that session and named rather than silently included. Also for a re-reviewer: parts of v3 (header, changelog table, §2.4/§4.8/D7/§9 edits) were already in the working tree uncommitted when `architect` opened the file — each was re-verified against `27faac4`/`7a1f99c` and source rather than trusted, which is how the changelog was caught promising a C13 fix the body had never received. Prior review context below. **`trd-review` SPLIT: `ios-developer` APPROVE WITH MINORS (`27faac4`), `ios-code-reviewer` REQUEST CHANGES (`7a1f99c`) — both verified by `chief-of-staff` directly against the commit diffs, not taken on a relay's summary.** Net verdict REQUEST CHANGES since both signoffs are required and one is a block. Blocking finding: §9 row 7 / C13 verifies camera immobility only for the horizontal-dominant inert case and a pan starting outside the 24pt band — never for a normal **in-band vertical** edge slide, the actual gesture this task ships. D7's claim that MapKit's pan recognizer never sees that touch is asserted from hit-test reasoning, not confirmed on-device, and the TRD's own `FB19394663` citation is evidence such reasoning doesn't always hold for MapKit. Fix: add an explicit check that `camera`/`MKCoordinateRegion` stays byte-identical through a vertical in-band `EdgeHourZone` drag, same comparison method row 2c already uses. Minor, fold in at the same pass: §4.8's `DragGesture` cites an undeclared `coordinateSpace: .named("map")` (should be `.local` — nothing in the codebase declares that named space); §9 row 6's Step column omits C4 (where the ≥44pt button-path assertion actually lives, not C9/C12); row 7d should exercise both `.medium`/`.large` sheet detents, not one. Re-dispatched `architect` for v3; both reviewers re-run once it lands. **TRD v2 delivered: `prds/time-slider/TRD.md`.** Routes to **`ios-developer` + `ios-code-reviewer` only** — iOS-only, no `[Backend]`/`[Algo/Data]` step (re-verified against the shipped `DensityAPI`/`hood_density` contract, not assumed). v1 designed only the button/modal path; v2 adds the edge-gesture architecture and settles the two items the design spec handed to `architect`: **D7** — the edge gesture is a SwiftUI `DragGesture` on a hit-testable sibling overlay above `Map`, *not* a `UIGestureRecognizer`, because SwiftUI's `Map` exposes no recognizer to arbitrate against and hit-test order makes arbitration unnecessary (this corrects the design spec's own stated mechanism for the 24pt zone; **Q7's ratified number and its acquisition argument are untouched, not reopened**); **D8** — 64pt/40pt confirmed correct (59pt Dynamic Island + 5, 34pt home indicator + 6) and re-derived as `max(floor, safeAreaInset + clearance)` so they hold off the 812pt reference device. Three new architect calls flagged for `product`/`designer` at review, each made trivially reversible: **D9** edge path portrait-only on iPhone (landscape puts the capture zone under the sensor housing, voiding Q7's own bezel argument — no upstream doc considered orientation, and the app ships all three); **D10** the bundled density seed (see build-phase row); and the two accepted costs of D7 (a 24pt undraggable/untappable map strip per live edge, and a horizontal drag starting in the strip consumed without panning). Q3/Q7/Q8 built as ratified settled requirements — Q3 made structural (exactly one storage location for the hour app-wide, so there is no second property to desync). **New §9 verification table, one row per P0 with a falsifiable pass condition** (per L-018/`architect.md`) — this is where T-031's two carried-forward untested req-2 bullets get discharged, and C10 is the precondition that makes either checkable at all. Build breakdown C1–C14, all **[iOS]**; C10/C11 sequenced first. Heat glyph `flame.fill` adopted from `designer`'s 2026-08-02 pin, closing D6's flagged gap. Prior context (design-gate retirement, glyph pass, Q7/Q8/Q3 ratification) below, unchanged: **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, `design-review` skipped.** Q7/Q8/Q3 ratified (`eb1cb7b`) — verified directly against the commit diff by `chief-of-staff`, not taken on a relay's description; content well-formed (L-002/L-013 compliant), matches this row's own prior note of exactly which three questions were outstanding. Design work already in hand from before the retirement (design-approval had both signatures in) — `time-slider-design.md` + mockup remain valid reference input for the architect, just no longer a gate. All 5 blocking + 7 should-fix findings previously re-verified fixed at the artifact (contrast, opaque backing, sheet drag-fallthrough, Fitts's-Law citation, persistent-hint contradiction reconciled as Q8). Scope gate re-run clean. Flagged, not fixed: PRD is 5,256 words against a ~1,200 word budget. **Heat-button glyph landed, `aabd863`.** Aviran, via hilos/@chief, verbatim "change the heat icon to flame" — routed [ASSUMPTION] to this task's own unbuilt heat-button glyph, closing TRD **§8 D6**'s flagged gap (corrected from this row's own earlier D1 mislabel — checked directly against the TRD's own section headers by both `designer` and, independently, `chief-of-staff`). `designer` pinned `Image(systemName: "flame.fill")`; design spec bumped to v4, mockup republished in place (v5). Verified by `chief-of-staff` against the commit diff and TRD text directly, not taken on the designer's word alone; the mockup's rendered visual result is not independently screenshot-confirmed (session has no claude.ai auth). `PAS-15` closing comment posted, `PROGRESS.md` stub `231b0ee`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. **Round 7:** dispatched `ios-code-reviewer` for its first-ever code-review, via relay. Linear `owner:ios-code-reviewer` (fresh, verified was stale `owner:ios-developer` before claiming). Also: T-050/`PAS-38` (events toggle) confirmed unblocked now that C5/`HeatModalCard` shipped, and T-035 req 5 (busy+flagged) confirmed concretely observable against this build's seed (florentin hours 2/3/10/11, kerem-hateimanim hours 4/5/6 both reach `.busy`) — both held for next round pending this review's verdict. **Round 8: review verdict is in (APPROVE WITH MINORS) — T-035's req 5 finding is now final, see T-035's own row. T-050 stays held one more round, not for the review gate anymore but to avoid dispatching a second `ios-developer` against `MapScreen.swift` concurrently with the just-dispatched T-038 acceptance-fix (both would touch that file) — dispatch next round once T-038's fix lands.** |

