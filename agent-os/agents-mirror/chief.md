---
description: Call the Chief of Staff to run the company
argument-hint: [what you want it to do]
---

Launch the `chief-of-staff` agent (via the Agent tool, `subagent_type: chief-of-staff`) to handle the following request. If no request is given, have it give a full status across the team and keep the pipeline moving.

Request: $ARGUMENTS
