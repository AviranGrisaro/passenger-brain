# Passenger V1 — Decision Record

**Status:** Locked (grilled with Aviran, 2026-07-22)
**Owner:** Aviran Grisaro
**Source brief:** Passenger Product Brief (V1 / V1.5 / V2, iOS only)
**Linear:** superseded — this record predates the 2026-07-26 reset. The original issues (LOC-79 … LOC-99) live in the archived `locali-app` workspace; current work is in workspace `passenger`, team `PAS`, project **Passenger V1**.

> **Amended 2026-07-26 — the reset.** Decision #1 is **reversed**: Passenger is a greenfield build, not a repositioning. The Locali codebase was frozen (`github.com/AviranGrisaro/locali`) and `passenger-code` started from an empty Xcode project. Reason: ten of the 27 PRDs built against that codebase specified features the strategy forbids, and the drift was structural rather than cosmetic — a god object plus two god views, wired through a social layer that was never supposed to exist.
>
> Knock-on effects on the rows below: **#3** no longer means "flag off" — the social code isn't in the new repo at all, and `FeatureFlags.swift` doesn't exist. **#5** is superseded by the strategy's current line — localness is "algorithm plus local QA, together," crowdsourced from users, not hand-curated by a curator. **#15** is executed as of this date.
>
> Every other row still stands. The rows are kept verbatim as the record of what was decided when; read this note first.

## What this is

Passenger is a **repositioning of the existing `locali-code` app**, not a greenfield build. The brief's V1 (interactive map, now→+12h time slider, density heatmap, tap-a-zone, native-maps hand-off, two categories) is ~70% already built. This project captures the **delta** work to refocus Locali into the tourist "you just landed, immediately know where the locals are" product — and to rebrand it to **Passenger**.

Locali's own strategy was already tourist-first ("where should I walk right now to find the real local vibe?"). What drifted is the *code* — it accreted friends, live-presence, and fog-of-war exploration that appear in neither the strategy nor the brief. Passenger refocuses back to the core.

## Locked decisions

| # | Decision | Notes |
|---|---|---|
| 1 | Reposition existing `locali-code`, not greenfield | — |
| 2 | Map encodes **two orthogonal layers**: heat = crowd density, tag = localness | Packed ≠ good; the tourist trap is the packed-touristy spot. The whole differentiator. |
| 3 | Social layer (friends, live-presence, fog-of-war) → **flag off, not delete** | Off through V2 — no "preserved for V1.5/V2" framing; any return is an undecided future call, beyond V2. Uses FeatureFlags.swift. Superseded by strategy's "No social. No B2B. Full stop." (2026-07-23, direct from Aviran). |
| 4 | Density = external popular-times provider (**TBD spike**); ship a **synthetic feed** for now, **time-bound to the slider hour** | First-party presence deferred (empty at launch). |
| 5 | Localness = **hand-curated for V1**; scraping/algo = **parallel gated spike** | The curator IS the definition of "local" for V1. Algo must beat the hand-curated rank-order before it ships. |
| 6 | Vibe tags (5): **super local · very local · mixed · touristy · tourist trap** | "Tourist trap" is an addition beyond the brief's 4 named. |
| 7 | Launch **Tel Aviv only**; foreign city = fast-follow | Portability proof → existing Multi-City Expansion project. |
| 8 | Cold-open **straight to Tel Aviv map** — no sign-in, no onboarding, no permission gate; lazy location; fading "Tel Aviv, right now" title | — |
| 9 | **Free V1**, monetization deferred (RevenueCat dormant) | Paywall is a later phase with real city coverage. Reconfirmed directly by Aviran, 2026-07-26: no paywall/unlock/trial gate anywhere in Phase 1; subscription (not per-city) introduced starting Phase 2. |
| 10 | "Local read" on tap = **hand-curated neighborhood blurb** | — |
| 11 | **Exactly two categories**: Food & drinks / Things to do | — |
| 12 | Localness granularity = **neighborhood + spot** | Bounded curation: dozens of neighborhoods + key spots, not every hex cell. |
| 13 | Hero screen = **density heat base + localness accent + tap-to-read** | — |
| 14 | **Keep events as a live layer** — reconciled as a time-sensitive overlay on the slider, NOT a third category | Deviation from brief. Watch-item. |
| 15 | **Rebrand Locali → Passenger** | Wordmark, app name, icon, App Store identity. |
| 16 | **Keep Saved + Visited places**; strip only V1.5 target-proximity geofencing | Deviation from brief ("saved lists out of scope"). Visited populates only after lazy location grant. CityGeofenceMonitor stays. |
| 17 | **No gradients anywhere** — heat = stepped bands / flat cells | Supersedes T-024 gradient-heatmap request. |
| 18 | Vibe tags (3): **Local · Mix · Tourist** | 2026-07-27, direct from Aviran. **Supersedes #6.** No dedicated "tourist trap" tag — packed-and-touristy reads off heat + tag together instead of getting its own label. |
| 19 | **Scenic View moves to Phase 2.** V1 tap-a-spot hands off to native Maps/Waze | 2026-07-27, direct from Aviran. No in-app routing in the launch build. Removes routing-engine scope from V1; the cost moves to Phase 2, it doesn't disappear. |
| 20 | **Events overlay moves to Phase 2** | 2026-07-27, direct from Aviran. **Supersedes #14.** V1 map is heat + tag only — two layers, matching the north star exactly. |
| 21 | **Phase 2 additions are subscription-gated** — proximity intelligence, Scenic View, Live Events | 2026-07-27, direct from Aviran. Free core is permanently the V1 scope: heat + tag, both categories, slider, zone/spot detail, Saved/Visited, native hand-off. Consistent with #9 (Phase 1 fully free). |
| 22 | **Localness = algorithm + crowdsourced local QA**, together, as the permanent pipeline | 2026-07-27, confirmed by Aviran. **Supersedes #5** — the curator is not the definition of "local" for V1. Real users are asked in-app whether a spot is actually local; the algorithm proposes, they verify and correct. Trades staffing risk for cold-start and incentive risk (no reward layer until Phase 3's points system). |
| 23 | **Search ships in V1** — place names, keywords, and neighborhoods | 2026-07-27, direct from Aviran. Reached from an icon in the map chrome, opens as a sheet over the map — Secondary tier, not persistent chrome, so the map stays the default answer. Results carry heat + tag and honor the slider hour. Softens, but does not void, the "no query" positioning line: search is for when you already have somewhere in mind. **Watch-item** — if users reach for search before reading the map, that's a signal the map isn't working. |
| 24 | **Local-QA is asked by a post-visit toast**, fired by a local notification | 2026-07-27, direct from Aviran. Geofence detects the visit; an iOS **local** notification fires while backgrounded; opening it drops a toast from the top of the screen asking whether the place felt local or touristy. Chosen over waiting for the next app open, so the ask lands while the memory is fresh. **Two costs to watch:** it adds a *second* permission prompt to V1 on top of location, which sits awkwardly with #8's "no permission gate" cold open; and it is the first notification of any kind in V1, so the "no push" shorthand in the business-model section no longer reads literally — the habit-loop argument for freemium still stands, a post-visit question is not re-engagement. |
| 25 | **Category chips move into the search sheet** — off the map chrome | 2026-07-27, direct from Aviran. Chips drop from Primary to inside search. One fewer permanent control on the map, consistent with moving spot-level tags off the map surface entirely (see `design/map-rendering-spec.md`). **Cost:** filtering by category while casually browsing now costs an extra tap, since it means opening search first. Supersedes the always-visible-chip assumption in `design/ux-flows.md`. |
| 26 | **Saved and Visited merge into one list**, and auto-saved places move from Phase 3 into V1 | 2026-07-27, direct from Aviran. **Supersedes #16.** One list fed three ways: manual save, dwell 20+ minutes (auto-save, the Bump mechanic), and geofence detection. It also renders on the map, not only as a list. Auto-save fires only on places already in Passenger's places table, so a home or office never gets saved — that guard is load-bearing, not an optimization. Auto-save leaves Phase 3 because it needs neither identity nor preference tracking, which was the stated reason the Phase 3 group waits; it only needs dwell detection on top of location V1 already collects. **Open:** what the merged list is called, and how an entry shows which of the three ways put it there. |
| 27 | **"Hoods" confirmed as the product-facing term** for zone/neighborhood granularity | 2026-07-29 founders meeting, applied 2026-07-30. Decision #12's neighborhood+spot granularity is unchanged; only the name is confirmed. Internal docs still say "zone"/"neighborhood" in places — not swept in this pass, flagged for `designer` to reconcile. |
| 28 | **Tourist-trap flag (boolean 1/0), "no local tags"** — proposed to replace decision #18's three-way **Local · Mix · Tourist** vibe tag | 2026-07-29 founders meeting, applied 2026-07-30. **Not fully confirmed — flagged, not a clean supersession.** Applied to strategy.md as stated scope because it came from the founders meeting and was framed as final, but it reverses a 3-day-old decision and the exact intended scope of "no local tags" (full removal of the graduated tag, or something narrower) isn't certain from the brief alone. Do not build against this until Aviran confirms in writing. See strategy.md's Open questions, item 1. |
| 29 | **"Been" places auto-save gets a Passport sticker matching place type**, sticker-album style, per city | 2026-07-29 founders meeting, applied 2026-07-30. Extends decision #26's dwell-based auto-save (unchanged 20-minute threshold, same guard against untagged places) — the sticker is a new consequence of the existing signal, not a new detection mechanism. |
| 30 | **"Saved places" stay visually/functionally distinct from "Been" places** | 2026-07-29 founders meeting, applied 2026-07-30. Manual save vs. dwell-triggered auto-save must read differently to the user — consistent with decision #26's existing provenance-labeling intent, now with an explicit visual distinction requirement layered on top. **Open, Aviran's own question:** what happens when a user tries to save a place Apple Maps marks permanently closed — tourist-trap classification, or something else? Not answered. |
| 31 | **Passport pulled forward from Phase 2 into V1**, progression system with per-Hood "Local" status | 2026-07-29 founders meeting, applied 2026-07-30. Supersedes the 2026-07-28 Phase-2 placement confirmation for this feature (that placement stood for exactly two days). Surfaces under a **Profile tab** per Aviran's verbatim brief — **[FLAGGED]** collides with the standing no-profiles scope gate and the 2026-07-28 "Passport not profile" naming decision; read as the tab housing Passport, not a reintroduction of accounts, pending Aviran's explicit confirmation. **[FLAGGED]** relationship between the new per-Hood "Local" status mechanic and the existing seven-tier global ladder (Tourist→Legend) is not stated — not resolved here. |
| 32 | **"Scenic Walk"**: walking-only routing that maximizes per-street-segment "Attractiveness" weight A→B, plus a separate fastest-route mode | 2026-07-29 founders meeting, applied 2026-07-30. **Scope increase over the 2026-07-29 routing-preview lock** (two-polyline comparison only) — this describes a real weighted-graph routing algorithm over street-segment data. Feasibility/timeline scoping dispatched to `data-engineer`, same risk class as decision below on live events (PAS-5). Still hands off to native Maps/Waze for the actual walk; no in-app turn-by-turn. |
| 33 | **Quick filters + search**, categories renamed **"Things to do"** / **"Eat & Drink"** (was "Food & drinks" / "Things to do") | 2026-07-29 founders meeting, applied 2026-07-30. Category rename confirmed. **[FLAGGED]** whether "quick filters" returns category chips to permanent map chrome (reversing decision #25) or stays sheet-internal under a new name is not stated — not resolved here. |
| 34 | **TikTok import** — user saves a TikTok video into the app; places mentioned/shown are extracted and added to Saved Places | 2026-07-29 founders meeting, applied 2026-07-30. New V1 feature, new ingestion pipeline (content/entity extraction from video, not a URL bookmark). Feasibility/timeline scoping dispatched to `data-engineer` — treated as a launch-blocking risk alongside live events (PAS-5) and Scenic Walk (decision #32) until scoped. |
| 35 | **Google Maps saved-list import — explored, not committed** | 2026-07-29 founders meeting, applied 2026-07-30. Aviran wants feasibility explored, explicitly not authorized to build into V1. A one-pager assessment is in scope; a pipeline is not. |
| 36 | **"Tourist trap" needs softer public-facing copy** | 2026-07-29 founders meeting, applied 2026-07-30. Aviran's own flag: current framing risks alienating/angering place owners. No replacement term given — copy work for `product`/`marketing`, not decided here. The Phase 1 marketing plan's word-of-mouth pitch line currently uses "tourist trap" verbatim and needs revisiting once a softer term lands. **Resolved 2026-07-30 (see #42):** marketing landed on "tourist-heavy spot." |
| 37 | **Tourist-trap boolean (1/0) fully replaces the three-way Local · Mix · Tourist tag.** **Supersedes #18, resolves #28's flag — confirmed.** | 2026-07-30, direct from Aviran, live session, same day as the founders-meeting brief this closes out. Not a narrower reading, not "boolean alongside the tag" — the graduated tag is gone. Every Hood/place carries a single tourist-trap flag and nothing else on this axis. Local-QA (decision #24) becomes a binary ask ("is this a tourist-heavy spot, yes/no") rather than a 3-way Local/Mix/Tourist classification — the toast copy and the algorithm/local-QA pipeline (#22) both need to be re-read against this, not the old 3-way model. `design/ux-flows.md` §6 and `design/map-rendering-spec.md` §§2–3 are rewritten against this as of the same date — no longer flagged, no longer describing the old three-tag model. |
| 38 | **Permanently-closed places: allow the save, show a distinct "permanently closed" badge** — not folded into the tourist-trap classification. | 2026-07-30, direct from Aviran, resolves #30's open question. A place Apple Maps marks permanently closed can still be saved; the UI shows a clearly distinct closed-state badge on that Places-list row (and, if visited historically, wherever else it surfaces), unrelated to the tourist-trap flag (#37) — closed is a factual state about the place, not a judgment about its character. Exact badge visual/copy is a `designer` call, not decided here. |
| 39 | **"Profile" is confirmed as the literal tab name housing Passport** — does not reopen the standing no-profiles/no-accounts scope gate. | 2026-07-30, direct from Aviran, resolves #31's naming flag. The tab is named Profile; it surfaces only the private, single-user Passport screen (no friend graph, no following, no social surface, no accounts/login being added). The gate in `passenger-brain/CLAUDE.md` and `BOARD.md` still bans social profiles as a *feature* — this is a naming exception for a private stats surface, confirmed explicitly by Aviran, not a reversal of the gate itself. |
| 40 | **Per-Hood "Local" status replaces the seven-tier global ladder (Tourist → Legend) outright.** **Supersedes the seven-tier-ladder framing in decision #31 and the strategy/ux-flows Phase-2-era stamp-collection write-up.** | 2026-07-30, direct from Aviran, resolves #31's ladder-vs-per-Hood flag. The old ladder does not persist as a derived meta-status — progression is per-Hood Local status only; overall "Local" (the top-line status the app cares about) means Local in every designated Hood. `strategy/passenger-strategy.md`, `design/ux-flows.md` (Journey 7, §2 Passport row) rewritten to drop the ladder as a parallel mechanic. |
| 41 | **"Quick filters" stay sheet-internal — decision #25 stands, unreversed.** | 2026-07-30, direct from Aviran, resolves #33's placement flag. Category chips ("Eat & Drink" / "Things to do") remain inside the search sheet, not permanent map chrome. "Quick filters" was Aviran's shorthand for the renamed categories, not an instruction to move them back to the map. |
| 42 | **Tourist-trap public-facing copy: "tourist-heavy spot."** | 2026-07-30, `marketing` (PAS-9), per decision #36's flag. Beat "touristy spot," "mainstream pick," "crowd-magnet for visitors" — descriptive of foot traffic, not a verdict on the business; pairs with the existing "real local spot" pitch line. Applied to `marketing/phase-1/marketing-acquisition-plan.md`. Separate from decision #37's boolean-flag mechanic — this is copy only. |
| 43 | **"Local personas" added as a new Phase 2 feature candidate — concept only, not scoped, not authorized to build.** | 2026-07-30, direct from Aviran, live hilos chat (`@chief`). Curated local-guide profiles a user can follow as a discovery lens — favorite places, Hoods, routes. Recorded in `strategy/passenger-strategy.md` Phase 2 candidates. **Two open tensions flagged, neither resolved by this decision:** (1) "follow" is social-shaped language against the standing no-social-features gate — curated content profile vs. banned friend-graph shape needs an explicit read; (2) likely overlaps the existing Phase 3 "AI local guide" named-persona concept — same idea at two fidelity levels, or two separate features. See strategy.md Open questions #8–9. Linear `PAS-11` tracks it as a backlog concept. |
| 44 | **Scenic Walk V1: ship the lighter locked version — two comparison routes (fast vs. "scenic") from Apple/Google's own walking-directions APIs, biased via 1-2 waypoints toward good Hoods. Not the heavier custom-weighted-routing version.** **Resolves decision #32's scope-increase question and `PAS-6` item #8.** | 2026-08-04, direct from Aviran, live `/chief` session, relayed via `chief-of-staff`. `PAS-7`'s feasibility scoping (`data-eng/scenic-walk-tiktok-feasibility.md`) found genuine weighted street-segment routing not buildable in the Phase 1 window — MapKit/Google walking-directions APIs don't expose custom per-street weighting, so that version would require a self-hosted routing engine (GraphHopper/Valhalla) over a fresh OSM extract, ~3-5 weeks, sequenced after the places/Hoods schema. This decision picks that doc's recommended fallback instead: the route-preview-only shape already locked by decision #19, still handing off to native Maps/Waze for the actual walk. `product` dispatched to write the PRD. |
| 45 | **The Local threshold is 2 Been places per Hood — confirmed, no longer provisional.** One app-wide constant (`LocalStatus.threshold`), never per-Hood. **Resolves T-048 / `PAS-35` and the threshold half of the Passport PRD's "two numbers this cannot ship without."** | 2026-08-04, `product` (T-048/`PAS-35`), re-argued from product meaning rather than from the fixture-observability reasoning that produced the same number provisionally in `prds/passport/TRD.md` D6. **Not a founder call and not treated as one:** decision #40 already locked the mechanic, the strategy states no number, and its own wording — *"a set of hoods Passenger designates"* — puts the parameterisation with the company. Nothing here is a scope change, money, or a cut requirement. **The meaning: Local = you came back, not you completed the Hood.** One 20-minute dwell is a tourist having lunch; two distinct curated places in the same Hood, each earned by a separate 20+ minute dwell, is someone who returned on purpose — which is the weight *"Land like a tourist. Move like a local."* asks the word to carry. **The range was fixed before taste entered, at [2, 3]:** the floor is 2 because at 1 per-Hood Local is true on first contact and overall Local degrades into "has been to each designated Hood once," a checklist rather than a status; the ceiling is the smallest curated place count across designated Hoods — 3 today (`florentin`, `kerem-hateimanim`, `neve-tzedek`, three places each) — because above it Local is unreachable by construction and Passport req 4 can never pass (`places-dataset` req 7). **Within [2, 3], 3 is rejected:** it makes Local identical to 100% completion of a Hood's catalogue, leaves zero margin between reachable and unreachable for a number that gates dataset validation, and lets a single place leaving the catalogue make a whole Hood Local-unreachable. **Review trigger, because the number's meaning is relative to catalogue size:** 2-of-3 reads as "most of the Hood"; 2-of-15 would read as "barely started." This value is correct for roughly 3–6 curated places per designated Hood — **[ASSUMPTION]**, that band is `product`'s judgement and no source states one. Re-derive when any designated Hood leaves the band, or when the designated set changes (T-047), and re-derive it as one constant — per-Hood is forbidden by Passport req 4 by name and was rejected in TRD D6. **Consequences:** unblocks `validate_dataset.py`'s currently-skipped check (`PAS-6` item 12) as "every designated Hood holds ≥2 curated places" — all three pass today; constrains T-047, which may not designate a Hood carrying fewer than 2 curated places; and leaves `LocalStatus.threshold = 2` unchanged in code, with only its now-false `[ASSUMPTION]` doc-comment to correct (`ios-developer`, T-048 row). |
| 46 | **The shared dwell/geofence "been here" detector (T-046 / `PAS-33`) is V1 scope, not parked Phase 2 "proximity intelligence."** It is Build Phase 2 work inside roadmap Phase 1, and it is **launch-blocking**. | 2026-08-04, `product` scope-gate ruling (Job 0), recorded here because three TRDs have now named this gap and the parked-vs-authorized question would otherwise be re-litigated a fourth time. **Authorized by three verbatim lines in `strategy/passenger-strategy.md`'s V1-scope list** (not its Phase 2/3 candidate list): *""Been" places are auto-saved when you dwell 20+ minutes somewhere already in Passenger's places table"*; *"Geofence-detected plain visits (no 20-min dwell) still log too, per decision #26."*; and *"**The ask is triggered by a detected visit** — when the geofence logs that someone was actually somewhere, a local notification fires…"*. The detector is not a new feature — it is the sensor three already-authorized V1 features (T-035, T-036, T-037) were specified to read. **Why it is not Phase 2's parked geofencing:** Phase 2's line is *"Geofence detection — the app notices when you're near a spot you were already heading to"* — prospective, en-route, feeding an arrival card. This detector is retrospective and dwell-based: it fires on dwell completion and emits a record, not a nudge. Phase 2's own next bullet — *"Location access only turns on while you're actually en route to a suggestion — never standing, never background"* — is the opposite of what V1 auto-save requires, so the two cannot be the same mechanic without that bullet contradicting the V1 line above it in the same document. **Standing boundary for whoever builds it:** the detector may emit nothing to the user beyond (a) a `{place_id, kind, first_observed_at}` record (`prds/places-been-saved/TRD.md` §3.4) and (b) the local-QA notification decision #24 authorizes. No arrival card, no "you're near X" nudge, no proximity-ranked suggestion, no en-route state — those are Phase 2 and stay parked. **Corrects the earlier "not launch-blocking for Phase 1 … becomes real work at Phase 2/3" framing** on `BOARD.md` T-046 and `PAS-33`: what is true is that the three consumers *compile and pass their tests* against stand-ins, which is a build-sequencing fact, not a launch-readiness one. Without the detector, V1 ships with Been/Visited permanently empty, Passport permanently at zero stickers, and local-QA — half the localness pipeline — never firing. **Two questions inside it remain Aviran's and are not resolved here:** the permission-priming line and the confirm-modal question, both on the T-046 board row. |

## 2026-07-30 lock — strategy and UX flows

All open items from the 2026-07-29 founders-meeting brief that were tracked on Linear `PAS-6` are resolved as of decisions #37–41 above, confirmed live by Aviran the same day. `strategy/passenger-strategy.md` and `design/ux-flows.md` (plus `design/map-rendering-spec.md`, which the tag-model reversal touches directly) are updated to match and are no longer in Draft status on these points. Google Maps saved-list import (decision #35) remains explore-only, not committed — the one item from the founders-meeting brief still deliberately open.

**Same day, separately:** `data-eng/discovery-engine-spec.md` — Aviran's working spec (v0.1, 2026-07-27) for the discovery/localness algorithm itself, written for Yeari — was recovered from a Claude Artifact that had never been saved into this repo, and is now in `data-eng/`. It's the detailed design thinking underneath decision #22's "algorithm proposes, local QA verifies" pipeline, and it independently arrives at the TikTok share-in mechanism that decision #34/#45's TikTok import feature builds on. Cross-referenced from `data-eng/tourist-trap-algorithm-rescope.md`.

## Knowing deviations from the brief

- ~~**Events kept**~~ — reversed by #20. Events is Phase 2; V1 is back to the brief's two-layer map.
- **Saved + Visited kept** (brief: saved lists out of scope for V1).
- ~~**"Tourist trap"** tag added~~ — reversed by #18, then #18 itself superseded by #37 (2026-07-30): back to a single tourist-trap boolean flag, no graduated tag at all — narrower than either prior version, and than the brief's four.
- **V1 does not live-scrape** — the "from TikTok / local-language" north star is delivered in V1 by the algorithm-plus-crowdsourced-QA pipeline (#22), not by live scraping; the scraping pipeline is the C4 spike.

## Critical path

C4 (localness algo) is the risk and a gate — but a research track, not a V1 blocker. C1 (synthetic density) + B1 (design) start day 1 in parallel. Epic A (shell) can start immediately (our own code). B3–B5 join on C1 + C3. Design unblocks build flow-by-flow.

## Ticket map (LOC team)

- **Epic A — Shell** (LOC-79): A1 cold-open (LOC-83) · A2 flag-off social + V1.5 proximity (LOC-84) · A3 two categories (LOC-85) · A4 rebrand (LOC-86) · A5 supersede T-024 (LOC-87)
- **Epic B — Two-layer map** (LOC-80): B1 tag+non-gradient design (LOC-88) · B2 hero flow (LOC-89) · B3 accent + tap-zoom (LOC-90) · B4 blurb + tagged spots (LOC-91) · B5 spot tag + hand-off (LOC-92)
- **Epic C — Data** (LOC-81): C1 synthetic density + contract (LOC-93) · C2 provider spike (LOC-94) · C3 hand-curated localness (LOC-95) · C4 define-local spike+gate (LOC-96) · C5 events overlay (LOC-97)
- **Epic D — Validate** (LOC-82): D1 QA acceptance (LOC-98) · D2 success metric (LOC-99)

## Related existing Linear projects

- **Flag off / de-scope for V1:** Follow Friends & Locations, Onboarding Flow, Auth & Login, Live Heatmap (cross-user aggregation).
- **Reuse:** Map Screen, Heatmap & Live Events.
- **Defer, keep dormant:** Paywall & Monetization.
- **Foreign fast-follow:** Multi-City Expansion.
