# T-077 row history, trimmed 2026-08-08 (L-057 board-row budget)

**`ios-code-reviewer` REQUEST CHANGES against tip `e4cc1a8` (`passenger-brain e3db2ee`) — `b90ba0a` genuinely clears the 44pt gap (default+AX3 PASS), but AX3 `hourSlider`×`mapNavRow` occlusion is real and worse (17.33pt vs 10.67pt) — root cause is `hourContent`'s un-clipped layout, not `HourSlider.swift`, so **T-081's removal won't fix this on its own**. Also: this failing assertion shares a method with AX3 containment/positive-control/wrap, silently leaving them unrun — **standing rule 5 recurring on a different sub-check within days of ratification.** Needs (1) a real layout fix (clip/reposition `hourContent` against the nav-row band) and (2) the same test-method split sub-check (iii) got. Back to `build`/`ios-developer`.** Prior: **Test-ordering fix done (`ios-developer`, `passenger-code d497ac4`) — restructure worked, default capture now fully runs (occlusion/containment/positive-control PASS). GENUINE NEW FINDING: at AX3, `hourSlider` overlaps `mapNavRow` — real occlusion failure, not the ordering bug, aborted the rest of that capture legitimately. 2 expected FAILs (44pt gap, now confirmed at AX3 too). **COLLISION:** `passenger-code b90ba0a` landed on `main` on top of `d497ac4` mid-run — a concurrent session's real `HourSlider` fix (UIKit-wrapped slider overriding `accessibilityFrame`). Results above pinned to `d497ac4`, stale against tip `e4cc1a8`. **Routed to `ios-code-reviewer` for a fresh (not diff-scoped) review against current tip** — not a re-approval, given both the new occlusion finding and the concurrent fix.** Prior: **`product` REJECT (`passenger-brain a1f72bb`) — both prior APPROVE/PASS verdicts void. `assertHourSegmentCapture` runs the ≥44pt check before the positive control under `continueAfterFailure=false`; the pre-disclosed 31pt gap aborts the method at the default capture, so the AX3 half (occlusion/containment/≥44pt — the actual new v6 coverage) and the F2 wrap check never ran. Log evidence: one launch, 16.58s teardown vs. the real two-capture test's 30.97s. Fix (test-file only, `HourSlider` itself untouched): split the ≥44pt assertion into its own test method so it fails visibly without blocking the other 4 sub-checks at either capture; report per-sub-check, not just a failure count. **Circular-hold question resolved: T-081 stays held** — the test-restructure fix doesn't need `HourSlider` removed first. Back to `build`/`ios-developer`.** Prior: **`qa` PASS WITH MINORS (`passenger-brain 3abde83`) — independent 5/6 (1 = pre-disclosed 31pt, 3rd confirmation), occlusion/AX5-ceiling/containment/wrap/positive-control all PASS. Full suite 520/522, both pre-existing. Moved to `acceptance`/`product` (full-TRD ticket, not `/bug`-shortened).** Prior: **`ios-code-reviewer` APPROVE WITH MINORS (`passenger-brain b634979`) — TRD v6 conformance clean, independent 5/6 (1 = already-disclosed 31pt finding, not new), AX5-ceiling confirms C15 live. Minor: `driveSliderToMaximum` retry loop no self-`XCTFail` (non-blocking). Moved to `qa`.** Prior: **C16 shipped (`ios-developer`, `passenger-code 1021428`) — all 3 parts done, 2 out-of-scope blocking bugs found+fixed (`MapNavRow` a11y modifier order, slider-adjust retry helper), 1 real finding disclosed not patched (`HourSlider` 31pt vs required 44pt — out of C16's touch-set, relevant to T-081's removal instead). 527/529 (2 unrelated: the disclosed 31pt + T-076's pre-existing). Moved to `code-review`.** Prior: **Re-scope done, TRD v6 (`architect`, `passenger-brain 83bea24`).** Row 5b/6(c) couldn't be straight-ported to `SearchOverlay` — 3 v5 conditions were false-by-design against the new card (deliberately edge-flush, `MapNavRow` intentionally above by z-order, fixed-fraction not content-sized height); all observables moved off card frame onto content+containment. **Real new coverage found:** `hourContent` has no `.clipped()`, AX3 growth can push `HourSlider` (P0 ≥44pt) off-screen — row 5b now checks containment + re-asserts ≥44pt at ceiling. 2 more pre-existing-on-old-card defects fixed: `HourReadout` is one combined a11y element (per-element checks were never obtainable), and the positive control's "+12h" doesn't match the real "+12 hours" label. C15 seam (`UITestOverrides.swift`) confirmed applies cleanly, no change needed — one stale doc-comment naming `HeatModalCard` left to fix. **C16 build work, dispatched to `ios-developer`:** (1) `accessibilityIdentifier` on `SearchOverlay`'s card frame, (2) the rendered AX3/AX5 suite per row 5b in `SearchHourSegmentInteractionTests.swift`, (3) the stale comment fix. Then `ios-code-reviewer`, then `qa` re-runs 5b/6(c) — C1–C15 all shipped, this is the only outstanding TRD step.** Prior: **Partially done, then overtaken (`chief`, 2026-08-07):** `ios-developer` fixed findings 2/3/5 (`8fe34d3`) and built C15 (`967b7c2`, `UITestOverrides.swift` — **survives on `main`, still wired into `MapScreen.swift`, actively used**), also caught its own "+12h" vs real "+12 hours" label bug. But before landing the row 5b/6(c) rendered AX3/AX5 check, a concurrent session (T-078/PAS-60+T-079/PAS-73) deleted `HeatModalCard.swift` entirely, merging it into `SearchOverlay` — that session's own `SearchHourSegmentInteractionTests.swift` explicitly discloses the row 5b/6(c) rendered check doesn't carry over (flagged as an open follow-up, not silently dropped). **Needs re-scoping, not a straight re-review** — routed to `architect` to amend TRD `time-slider/TRD.md` (→ v6) with row 5b/6(c) re-targeted at `SearchOverlay`'s actual current structure, since `HeatModalCard` no longer exists to test against. Findings 2/3/5 + C15 are done and don't need redoing.** Prior: **Filed 2026-08-07 (`chief`), allocated per `architect`'s ruling on PAS-51 (`prds/time-slider/TRD.md` v5, `passenger-brain cf3444a`/`passenger-code 60b4c07`).** Row 5b's AX5 capture was unsatisfiable by design (card clamps to `.accessibility3`) — rewritten to capture at ceiling + assert equality at requested-AX5. Composition root hardcodes `Date()` (`MapScreen.swift:177`,`:89`) despite `HourFormat`/`DensityStore` already taking an injected clock — the "next day" pill (row 5b's own positive control) was silently absent from every pre-noon test run. C15: two launch-arg reads at composition root (dynamicTypeSize override, injected clock), both default to current behavior, `[ASSUMPTION]` flagged in TRD §10 (dynamicTypeSize propagation through the clamp modifier not observed live — BLOCKED disclosure owed if it doesn't hold, not a grep fallback). Same dispatch as PAS-51's findings 2/3/5. |