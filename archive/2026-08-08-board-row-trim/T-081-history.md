# T-081 row history, trimmed 2026-08-08 (L-057 board-row budget)

Prior: **UN-HELD & DISPATCHED 2026-08-08 08:10 (`chief`).** Aviran re-raised the identical ask directly this session ("continue with the backlog + remove the hour slider from the search modal — we already have hour sliders at the sides"). Original hold condition ("wait for T-077/PAS-51 to close") superseded: T-077 has hit REQUEST CHANGES on this exact `hourContent`/`HourSlider` occlusion surface 3 times (loop-guard threshold) and T-081 deletes that surface outright, so further T-077 cycles would only polish code about to be removed — T-081's own description already states Aviran wants `HourSlider` gone regardless of its bugs. `PAS-51` left `In Progress`, will be closed as superseded once this lands and is verified, not separately re-fixed. `T-056` (drawn-frame 31pt-vs-44pt touch-target gap, same `HourSlider`, no Linear ticket ever filed) also becomes moot the moment this lands — close alongside, no separate fix owed. Dispatched `ios-developer` (relay via `main`) in an isolated worktree, explicitly warned off the shared tree's two stale uncommitted clusters (`PAS-17` alias-folding files; the pre-`PAS-78` `SearchOverlay.swift`/`SearchHourSegmentInteractionTests.swift` WIP named in the standing top-of-file warning) — build from committed `main` tip only. **Filed 2026-08-07 (`chief`), Linear `PAS-76` created.** Founder-direct via coordinator (verbatim + provenance: `PROGRESS.md`'s 2026-08-07 FOUNDER-DIRECT STUB entry). "On the sides" resolved by source investigation, not asked back to Aviran — `Passenger/EdgeHour/`'s `EdgeHourZone`/`EdgeHourTrack` confirmed as the referent: `MapScreen.swift`'s own doc comments describe `HourSlider` and `EdgeHourZone` as the two writers of one shared `densityStore.selectedHour` value. **Held un-dispatched** — T-077/PAS-51's C16 work is actively building/reviewing a rendered test suite against this exact `HourSlider`/`hourContent` surface right now; removing it first breaks C16, building C16 against a control about to be deleted wastes it. Dispatch once T-077 is `done`; will also need an `architect` TRD amendment (row 5b/6(c) checks become moot) folded in. **Update, 2026-08-07:** `ios-developer`'s C16 pass found `HourSlider.swift`'s rendered accessibility frame is 31pt, not the required ≥44pt (reproduced 3x, disclosed not patched — out of C16's touch-set). Left as a live finding for this ticket rather than a separate fix, since removing `HourSlider` entirely makes it moot; flagged also via `spawn_task` chip `task_8d9fa06a`. **Update, 2026-08-07:** a concurrent, uncoordinated session landed `passenger-code b90ba0a` — a real fix to `HourSlider.swift`'s 44pt gap (swapped SwiftUI `Slider` for a `UIViewRepresentable`-wrapped `UISlider` overriding `accessibilityFrame`), plus T-077/PAS-51's fresh AX3 test run found a **second, new** `HourSlider` defect (AX3 occlusion with `mapNavRow`). Neither changes this ticket's own scope (Aviran wants `HourSlider` removed regardless of whether its bugs are fixed) — noting for whoever picks this up that the underlying control may now be in better shape than when this ticket was filed, and that removing it will make both `b90ba0a`'s fix and the new AX3 occlusion finding moot along with the original 31pt gap. Still held pending T-077/PAS-51's close. |