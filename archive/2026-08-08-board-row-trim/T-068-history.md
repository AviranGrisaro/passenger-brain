# T-068 row history, trimmed 2026-08-08 (L-057 board-row budget)

Prior: **COMPLETE, routed to code-review 2026-08-08 (`chief`).** Fix + regression test (`regressionCountsVisitableNotRawPlaces`) landed `passenger-code 01bdcd0`, fast-forward onto `main`. Full suite 501/502 — 1 pre-existing flake (`SearchHourSegmentInteractionTests.testReadoutNeverIntersectsTheNavRowAtDefaultTextSize`, wall-clock-dependent, unrelated, self-resolves once T-081 deletes that file). Shared-tree WIP (T-081/T-066) confirmed untouched. Worklog `passenger-brain f2ddabf`. Prior: **DISPATCHED 2026-08-08 08:10 (`chief`), independent single-file test fix, isolated worktree — no overlap with T-081/T-066's files.** Prior: **Linear `PAS-64` created 2026-08-05 by `chief-of-staff`** (this row predated the actual Linear ticket — `product` doesn't write Linear). **Filed 2026-08-05 (`product`, via T-047/PAS-34 acceptance ACCEPT, `passenger-brain 4bfad28`).** `PassportBundleInvariantTests.everyDesignatedHoodMeetsThreshold` currently counts every place in a designated Hood, including `permanently_closed: true` ones. `kerem-hateimanim`/`neve-tzedek` each have exactly 2 *visitable* places (1 of their 3 is permanently closed) against `LocalStatus.threshold == 2` — the invariant would stay green even if the last open place in a designated Hood closed, since it's testing the wrong count. Fix: filter `PlaceCatalog.places(in:)` (or the test's own query) on `permanentlyClosed == false` before comparing to threshold. Small, isolated, no product/design judgment needed — the correct behavior is already stated in T-047/PAS-34's new req-5 pass/fail bullet in `prds/hood-dataset/hood-dataset.md`. No PRD/TRD needed, straight to `build`. |