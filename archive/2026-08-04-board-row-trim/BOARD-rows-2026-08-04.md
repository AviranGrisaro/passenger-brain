# BOARD.md row-detail archive — 2026-08-04

Full pre-trim cell text for rows that exceeded the ~1200-char row-bloat threshold (check J), trimmed by the nightly PM hygiene audit on 2026-08-04. Trim keeps only the current bolded status lead in the live row; this file holds the rest verbatim, in original order.

---

## T-036 — Places — Been & Saved (PRD) — main tracking table row (was line 91, ~20.7k chars)

| T-036 | Places — Been & Saved (PRD) | **aviran-review** | Aviran | 2026-08-04 | **Round 8 `product` acceptance: ACCEPT (2026-08-04). All 8 P0s met. → `aviran-review`; Linear `PAS-27` labelled `gate:awaiting-aviran-review`, `owner:product` kept.** Every P0 re-derived from `passenger-code` source at **`5e1f72f`**, not from qa's or the reviewer's verdicts, per the standing acceptance convention — and re-run at that commit rather than trusting qa's, because **two commits landed after qa's `92561a7` pass and one of them touched this feature** (`c1b8bc3`/PAS-48: `PlacesListOverlay` `maxHeight` 420→480). Independent runs on a detached worktree at `5e1f72f` (shared tree untouched, worktree + DerivedData removed after): **`PassengerTests` 401/401, `PassengerUITests/PlacesListInteractionTests` 6/6, 0 failures, TEST SUCCEEDED both.** **Scope gate re-run on inherited work (Job 0 item 4) — PASSED by exact-string grep, not memory:** both of the PRD's Motivation quotes are verbatim in `strategy/passenger-strategy.md` line 43 (`grep -c` = 1 each), and that same line independently authorises req 3's 20-minute dwell and its already-known-place guard. No social/onboarding/Phase-2 surface anywhere in the shipped feature. **Per-requirement:** req 1 — `PlacesListOverlay` titles the list "Places", `PlacesListEntry.provenance` is non-optional so "never two, never none" is structural, precedence is `saved`-first then `visits[id]`, one row per place by `compactMap` over a distinct catalog. req 2 — `SavedPlacesStore.toggle` has no `await`, so <400ms is met by construction; `isSaved` read directly off the store; un-save falls to `.been` or drops the row (both unit-tested). req 3 — bullets 3/4 met (no confirmation UI; duplicate `place_id` keeps the higher kind); **bullets 1/2 are not satisfiable in Build Phase 1 and now say so in the PRD** (see below). req 4 — save path has no closed-state condition; `closedBadge` renders iff `permanentlyClosed`; closed state is never copied into `saved-places.json` or the visit fixture, which is what makes "badges on next render" true regardless of T-044; `Directions` is `.disabled(availableApps.isEmpty)` **only** — confirmed live by UI test on `neve-nachum-gutman-museum`. req 5 — zero `CoreLocation`/authorization/permission-request symbols in the feature's files (re-grepped, not taken from the doc comments). req 6 — empty-state line names both ways in; no network path in the feature. req 7 — ring is a `Bool`, gated on the existing `showsNames`, dashed (shape not colour), `.allowsHitTesting(false)` outside an unchanged 44pt frame. req 8 — row tap calls `openPlace` only (Hood sheet skipped), `MapChromeState.toggle` swaps rather than stacks, camera/`selectedHour` neither read nor written. **Two PRD corrections landed in the same commit as the verdict (L-009/L-006):** req 3 and req 5 now carry explicit build-phase scope on the bullets a Phase-1 build cannot satisfy, naming T-046/`PAS-33` as where they get re-run — **req 5's version of this was already ruled "owed" on 2026-08-03 and never written into the Requirements section**, which is exactly how a later gate re-reads a bullet as verified. `Status:` flipped Draft v1 → **Accepted 2026-08-04**. **qa's D8 finding weighed and passed through, not silently accepted:** the TRD's D8 *rationale* text is wrong (a nav-row tap under a `.medium` sheet hits the sheet), but D8's *contract* — mutual exclusivity, nothing stranded — is what req 8 bullet 2 actually asks for, and it holds via `handlePresentedSurfaceChange`, covered by both unit and live UI tests. TRD prose is `architect`'s; filed as **T-058** rather than left in a worklog line. Full verdict: `PROGRESS.md`, this date. Prior: **Round 8 qa: PASS WITH MINORS (`passenger-code 92561a7`, 6 live UI tests, one non-blocking D8-rationale finding).** **Round 8 qa: PASS WITH MINORS (`passenger-code 92561a7`, 6 live UI tests, one non-blocking D8-rationale finding). Linear `PAS-27` reconciled this run (`owner:qa → owner:product`, verdict comment posted); `product` dispatched for acceptance — reply pending.** Prior: **Dispatched `qa` this run (2026-08-04) for the behavioral pass — reply pending.** Prior: **Round 7 code-review (`ios-code-reviewer`): APPROVE, no blocking findings.** Full-surface review (not diff-scoped), per this repo's blocking-REJECT-fix convention — the prior round-7 dispatch produced zero output and was treated as abandoned. `bc8e41a`'s two fixes confirmed real by direct read. D4/D5/D7/D8/§9-row-5 all re-verified against current `main`, not re-litigated. L-028 check clean (`permanentlyClosed` is `let`/non-optional on all three `Place` decode paths). Independent `xcodebuild build`+`test` on a dedicated simulator: BUILD SUCCEEDED, 422/422 tests passed, 0 failures — first attempt hit genuine host disk exhaustion (98% full, a shared-environment issue, not L-029 and not a code defect), fixed by deleting three orphaned `DerivedData` caches from a different closed session's vanished worktrees (freed 5.9GB), then retested clean. `/vibe-security` pass: no secrets, zero location/logging symbols in the new files, D6 holds. One Medium non-blocking security note, attributed to T-033 not this task: `SavedPlacesPersistence` writes `saved-places.json` with no file protection/backup exclusion, unlike this codebase's own `LocalQAAnswerStore.swift` which has both — flagged for whoever next touches `SavedPlacesStore`. Liquid Glass/HIG/contrast/VoiceOver all confirmed by direct read, consistent with already-cleared codebase idioms. **Moving to `qa`** — briefed on the three dismissal paths, the D8 co-presentation guard live, empty-state rendering, and the closed badge not disabling Directions. Full verdict: `PROGRESS.md` this run. Prior: **Fix landed: `passenger-code bc8e41a`.** Both items from `product`'s REJECT fixed — **F1**: `PlacesListEmptyState.swift`'s line now reads "Save a place from its detail card, or spend enough time at one, and it'll show up here." (icon + CTA kept, only the line changed, satisfying PRD req 6 bullet 1 and the already-landed §9 row 6 amendment). **F2**: `PlacesListRow.touristHeavySlot` and its call site removed, plus the three stale doc comments that referenced it — zero repo-wide hits confirmed after. `xcodebuild build`: BUILD SUCCEEDED. Full (not scoped) `xcodebuild test`: TEST SUCCEEDED — `PassengerTests` 343/343 (61 suites), `PassengerUITests` 8/8, zero failures, **zero flakes this run** (the usually-flaky `ColdOpenPerformanceTests` and siblings all passed clean). `git diff` confirms exactly the two intended hunks; T-032's in-flight files (`SettingsHint.swift`, `HeatRepaintSignpost.swift`) confirmed untouched. Moving to `code-review` — fresh pass needed since the prior verdict was a blocking REJECT, not a skip to `qa`. Prior: **`product` REJECT → `build`. One blocking finding, one string.** **F1 (blocking): PRD req 6 bullet 1 fails.** `PlacesListEmptyState.swift` renders icon + "Nothing here yet" + "Explore the map" — it names *nothing* that would fill the list. The requirement is "a plain empty state **naming what would fill it**." Fix: copy stating the ways a place gets here (save one from its detail modal, or spend time at one). Why this is blocking rather than polish: V1 forbids onboarding, so this empty state is the only surface where the feature can explain itself, and a first-run user opening Places sees exactly this. Why no earlier gate caught it: TRD §9 row 6's pass condition dropped the "naming what would fill it" clause and checked only icon + line + CTA, so `qa` had nothing to fail — spec defect as much as build defect (L-009); the PRD bullet is now sharpened and **`architect`'s §9 row 6 amendment LANDED 2026-08-03** — the pass condition now reads "…**and its copy names what would fill the list** (how a place gets here: save one, or spend time at one)… **a string that states emptiness alone — 'Nothing here yet.' — FAILS this row**", with a dated footnote under the table recording why the clause went missing. Documentation-only: no requirement, schema, contract or D-decision touched, **no re-review owed** (same class as the closed T-049 amendment). **The copy fix itself is still `ios-developer`'s and still open** — this amendment only restores the bar the next `qa` pass tests against. **F2 (fold in, non-blocking): remove `PlacesListRow.touristHeavySlot`** — dead `EmptyView()`; ruled at acceptance that the row never carries a tourist-heavy line (T-035 req 6 / `map-rendering-spec.md` §4 govern), so the slot has no future filler. **Everything else PASSES, re-derived from source** — `PlacesListComposition` precedence as a structural `max` over `PlaceProvenance: Comparable`; one row per place; `SavedPlacesStore.toggle` synchronous in memory with generation-guarded fire-and-forget persistence (400ms met by construction); closed badge from `place.permanentlyClosed`, never substituting for anything, never disabling the route; dashed ring drawn outside the 44pt frame at close zoom only, provenance-blind; three dismissal paths; row tap → `openPlace` with `chrome.presented` structurally untouched. **Four scope calls ruled, all CONFIRMED and unaffected by the REJECT: D4** (name-ascending — the PRD specifies no order, so this fills a gap; recency needs a schema change, not a sort change), **D5** (no offline banner — the PRD's own Technical design says V1 writes nothing server-side, so req 6 bullet 2's "syncs later" was the defect; struck), **D7** (bucket-2 chrome — `ux-flows.md` line 46 checked directly, T-032's D1 was wrong; the fade cost is accepted, three dismissal paths exist), **§9 row 5's framing** (honest — verified zero `LocationStore` refs and no `CoreLocation` use in `Places/` beyond coordinate types; the fixture populating regardless of permission is Build Phase 1's declared scope, re-verified at `T-046`). Prior: **Round 5: PAS-36 independently re-verified and closed `Done`. T-036/PAS-27 QA PASS confirmed, moved to `acceptance`, `product` dispatched (combined with T-034/T-035).** See V1 Build Phases table row above for full detail. Prior: **QA verdict: PASS on both `PAS-27` (T-036 contrast fix) and `PAS-36` (revert).** Verified `952cf2f` directly (confirmed still an ancestor of HEAD `3d8e3b8` with zero touches to the three changed files since — `git log 952cf2f..HEAD -- PlacesListOverlay.swift MapScreen.swift PlaceDetailModal.swift` returns nothing). Wrote `prds/places-been-saved/TEST-PLAN.md` (new, from TRD §9's 8-row table — did not exist before). Static: `Surface.colorset` alpha 1.000 both appearances; `PlacesListOverlay.swift:61` uses `Color("Surface")`, no `.thickMaterial`; zero repo-wide hits for `selectPlaceFromList`; `DetailRouter.closePlace()` clears only `place`, never `chrome.presented`, so `.places` structurally can't be disturbed by a depth-1 sheet closing over it. **Live, the reviewer's own specifically-requested repro:** isolated `git worktree` on `952cf2f`, built clean, installed+launched on iPhone 17 Pro sim — opened Places (opaque card, 4 fixture rows visible, no map bleed-through), tapped "Dr. Shakshuka" (place-detail sheet opened above it, also opaque/legible — the old "stacked messily" look is gone), dismissed, **list reappeared underneath unchanged, same 4 rows same order** — exactly TRD §5. Full `xcodebuild test` in the same worktree: 253/253 unit + 6/6 UI, zero failures, zero flakes this run. Worktree removed after; confirmed main tree's own uncommitted T-032 files untouched. Moved to `acceptance` per the QA lifecycle rule. `PAS-36` is a bug-labeled ticket (not gated by `acceptance`/`aviran-review`) — the repro no longer reproduces and the prevention (this same contrast fix + its test) holds, so it's ready for `Done`; not moved here, no Linear write access held by this session. Prior: **Round 4 update:** `ios-code-reviewer` cleared the contrast fix — **APPROVE, no blocking findings** (`passenger-brain 55c7f1c`) — confirming `Surface` colorset is genuinely opaque (alpha 1.0 both appearances, checked directly), no dangling `selectPlaceFromList` references from the PAS-36 revert, and the real D8 guard (`handlePresentedSurfaceChange`) untouched. `chief-of-staff` independently re-verified all three claims against source before advancing. Moved to `qa`, dispatched via relay alongside `PAS-36` (same repro path) — briefed to specifically re-confirm "open list → tap row → dismiss modal → list reappears unchanged" (TRD §5), the exact path the earlier wrong fix broke. Prior: blocking finding fixed in `passenger-code 952cf2f` — `.thickMaterial` → opaque `Color("Surface")` at `PlacesListOverlay.swift:61`; same commit reverts the wrong `PAS-36` fix and folds in T-035's two minors. Built+tested clean by `chief-of-staff` in an isolated `git worktree` (253/253 unit + 6/6 UI tests, zero failures) before advancing. Since the prior verdict was blocking REQUEST CHANGES, re-dispatched a fresh `ios-code-reviewer` pass rather than skipping to `qa`. `PAS-36` tied to this same review — its real root cause was this contrast bug, not the D8 co-presentation it was originally diagnosed as. Prior: **Build landed `passenger-code 7688834`+`46d2af2`, all 14 steps. Full build+test suite re-verified green by `chief-of-staff` this run** (only failure across the whole suite: one flaky `ColdOpenPerformanceTests` signpost test, environment-attributed not code). `ios-code-reviewer` dispatched via relay. Prior: **v2 delivered 2026-08-02 (`architect`) — the blocking `trd-review` finding is fixed.** `prds/places-been-saved/TRD.md` §4.5 now reproduces T-032 §4.1's `NavSurface`/`MapChromeState` contract **verbatim** (four cases — `search, heat, places, profile` — with `String, CaseIterable, Sendable, Identifiable`), verified byte-identical against T-032's live text rather than retyped; §2.2 is rewritten from "shared contract, either build order" to the real one-directional dependency (**T-032 owns the type, T-036 consumes it and adds no case**); C6 now reads "create `MapChromeState.swift` exactly per T-032 §4.1 if it doesn't exist, otherwise add nothing"; §10's risk row and §2.1's file list restated to match. **Both build orders are now safe** — T-036-first creates the real four-case type, T-032-first is a zero-edit reuse. Also folded in `ios-code-reviewer`'s non-blocking should-fix: §3.4 now pins the **keep-the-higher-`VisitKind`-on-revisit** update invariant the real Phase-2 detector inherits (PRD req 3 bullet 4 depends on it). **Two one-line edits owed to T-032's own TRD are deliberately NOT made** — its stale D1 "the rest slot into `MapNavRow`" prose (D7 corrects it for Places) and a symmetric no-op note on its C1: `prds/time-slider/TRD.md` had **another session's uncommitted v3 work in the shared tree**, so staging it would have repeated the `0dd3d21` cross-session attribution failure (`CLAUDE.md` rule 2). **Owed to T-032's in-flight v3 pass; neither blocks T-036.** Prior v1 context below, unchanged. TRD verified by `chief-of-staff` against `0051ef0`'s full diff before claiming. Four `product` findings not yet separately dispatched: D4, D5, D7, §9 row 5 framing. T-037/Passport now unblocked, not yet claimed. **TRD delivered: `prds/places-been-saved/TRD.md` (v1).** Routes to **`ios-developer` + `ios-code-reviewer` only** — iOS-only, all 14 steps `[iOS]`, no `[Backend]`/`[Algo/Data]`. Surface re-verified three ways, not assumed: PRD writes nothing server-side; the one server field it reads (`places.permanently_closed`) is already owned by T-042's approved TRD (§3 schema + migration `004`, held for Phase 2); Phase-1 data for both halves is client-bundle content. **The three items this row named are settled:** (1) `Place.permanentlyClosed` added non-optional and threaded through **all three** of `PlaceCatalog`'s source paths — seed, `PlacesAPI` incl. its `select=` string, and `PlacesCache` — because seed-only would compile, pass every Phase-1 test, and ship a silent `false` the moment Phase 2 flips the constant (**D1**); no cache migration needed and a test proves it. (2) Been/Visited ships as a bundled read-only fixture behind a **new** `BuildPhase.visitsAreSeeded`, with **no persistence and no `CoreLocation` import anywhere in the feature** (**D2**) — second constant, not a reuse of `seedIsAuthoritative`, because the axis differs. (3) The precedence merge is **not a class or actor** — a pure `enum PlacesListComposition` over two independent stores (**D3**); `PlaceProvenance: Comparable` makes Saved>Been>Visited a `max`, and a separate `VisitKind` makes it structurally impossible for the visit source to claim `.saved`. `SavedPlacesStore` is not rewritten — one added read accessor, shipped tests unmodified. **Five more found by reading the shipped code, not raised upstream:** **D7** the Places button is bucket-2 chrome, not a nav-row button (`ux-flows.md` §2's "4th icon, separate from the 3 nav buttons") — **this corrects T-032's D1**, and the accepted cost is that the icon fades under its own surface so re-tapping can't dismiss; **D8** a `NavSurface` and a system sheet are never co-presented in either direction (the shipped `.presentationBackgroundInteraction(.enabled(upThrough: .medium))` makes both orderings reachable), enforced by two calls with **no `DetailRouter` change**; **D4** name-ascending sort, recency rejected because it forces a versioned-payload migration on shipped accepted state; **D5** no offline banner ships (could never render in Phase 1, describes a sync V1 doesn't perform); **D6** closed state read from the column, never a client `MKMapItem` lookup. **§9 verification table, one row per P0 (8 rows), per L-018 — and it names what is *not* covered** rather than passing it by construction: req 3's 20-min threshold and known-place guard (no detector exists), req 4's three-element worst-case row (needs T-035) and the column's own freshness (T-044, ownerless), and req 5's Been/Visited bullets, checked in Phase 1 as the **absence of any sensor code path**, not as runtime behaviour — the fixture populates regardless of permission, stated openly and flagged rather than papered over. **`MapChromeState` is shared with T-032's C1** — §2.2 pins one contract and says who adds which case in either build order, so neither task blocks the other. **Four items flagged for `product` at review:** D4, D5, D7, and §9 row 5's req-5 framing. **One cross-check for T-042's reviewers:** this task widens `PlacesAPI`'s `select=` to add `permanently_closed` and only that column. Prior context below, unchanged: **Pre-code design gate retired 2026-08-02 — advanced straight to `trd`, `design-approval`/`design-review` skipped.** Design spec + mockup now **complete** — `design/phase-1/places-been-saved-design.md`, mockup https://claude.ai/code/artifact/2ab0b9c8-5d59-43dd-90e8-8163a7d0b93f — plus a dated addendum to `map-rendering-spec.md` §6 (dashed-ring shape pairing, filling a gap `ux-flows.md` §2.1 had flagged and PRD req 7 requires). Kept as valid reference input for the architect, same treatment as T-035, not a required gate. Covers the Places list, provenance-word rows with read-time precedence (Saved>Been>Visited), the row-density resolution the PRD explicitly left to `design-review` (now resolved in the spec regardless, §2.2), the permanently-closed badge, and the map ring accent. **Cross-spec check against `tourist-trap-flag-design.md` (T-035) caught and fixed a real drift before submission:** an early draft had invented a second amber for the tourist-heavy line instead of reusing T-035's already-specced `Color("Flag")` token — corrected in both the spec and the mockup. `prds/places-been-saved/places-been-saved.md` (Draft v1) by `product`, unblocked by decision #38. Depends on T-031 (done), sequenced before T-037. **Data-requirements sweep (2026-07-31):** closed-state refresh promoted to a named ownerless job (`T-044`/`PAS-22`), still unresolved — not this pass's to fix. Stale dependency line in the PRD (says `map-hoods-heat`, should say `hood-place-detail`) flagged for product to fix, not yet fixed. Real open items for the TRD (spec §8): `Place.permanentlyClosed` needs adding to the model (decoded from the seed JSON's already-authored, currently-unused `permanently_closed` key); a Been/Visited provenance store needs building (no live dwell detector in Build Phase 1 — recommend a bundled fixture, not a real detector this early); the read-time precedence merge needs a class/actor home. Linear `PAS-27`. Full history: `archive/2026-08-01-board-row-trim/BOARD-rows-2026-08-01.md`. **Round 7:** REJECT fix landed (`passenger-code bc8e41a`) — dispatched `ios-code-reviewer` for a fresh code-review, via relay. Linear `owner:ios-code-reviewer` (was stale `owner:ios-developer`). |

